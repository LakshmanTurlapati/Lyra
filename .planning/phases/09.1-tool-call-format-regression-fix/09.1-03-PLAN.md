---
phase: 09.1-tool-call-format-regression-fix
plan: 03
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - results/lyra_custom_diagnostic.json
autonomous: false
requirements: [EVAL-01]

must_haves:
  truths:
    - "Diagnostic eval produces a tool-call-format score for Lyra with the explicit template fix applied"
    - "The result is compared to previous score (0.1870) to determine if template loading was the issue"
    - "User has seen and interpreted the diagnostic result before proceeding"
  artifacts:
    - path: "results/lyra_custom_diagnostic.json"
      provides: "Diagnostic EvalResult JSON with tool-call-format and code-syntax scores"
  key_links:
    - from: "scripts/eval_inference.py"
      to: "results/lyra_custom_diagnostic.json"
      via: "CLI --model models/lyra-merged --output results/lyra_custom_diagnostic.json"
      pattern: "eval_inference.*lyra_custom_diagnostic"
---

<objective>
Run the diagnostic eval (Stage 1 per D-03) using the fixed eval_inference.py from Plan 01 on the existing Lyra merged model. This validates whether the chat_template loading was actually the cause of the regression. The result determines interpretation: if score stays at ~0.19, the template was already being loaded correctly and the real issue is model quality (confirming RESEARCH.md's finding). Either way, data scaling (Plans 02/04/05) proceeds.

Purpose: Empirically validate the D-01 diagnosis before investing in retraining. Per RESEARCH.md, the template IS already loaded by transformers 5.5.4 from chat_template.jinja, so the score likely stays at ~0.19 -- confirming model quality is the real issue.
Output: Diagnostic eval result JSON + user interpretation checkpoint.
</objective>

<execution_context>
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-CONTEXT.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-RESEARCH.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From scripts/eval_inference.py CLI:
  --model MODEL (required)
  --dataset-dir DATASET_DIR (default: datasets/assembled)
  --output OUTPUT (required, Path to output JSON)
  --device DEVICE (optional, auto-detects mps/cpu)

Previous eval results for comparison:
  results/lyra_custom.json: tool-call-format=0.1870, code-syntax=0.2000
  results/base_custom.json: tool-call-format=0.4065, code-syntax=0.2000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run diagnostic custom eval on existing Lyra model</name>
  <files>results/lyra_custom_diagnostic.json</files>
  <read_first>
    - scripts/eval_inference.py (verify the template loading fix from Plan 01 is present)
    - results/lyra_custom.json (get previous scores for comparison)
  </read_first>
  <action>
Run the custom inference eval on the existing Lyra merged model using the fixed eval_inference.py. Per D-11, this MUST run sequentially -- never in parallel with any other model-loading process.

```bash
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/python -m scripts.eval_inference \
    --model models/lyra-merged \
    --dataset-dir datasets/assembled \
    --output results/lyra_custom_diagnostic.json
```

Use a 600000ms timeout (10 minutes) since inference on 153 samples (123 tool-calling + 30 code) takes several minutes on MPS.

After the eval completes, compare the diagnostic score against the previous result:
- Previous: tool-call-format = 0.1870
- If diagnostic is similar (~0.19): confirms RESEARCH.md finding that the template WAS already loading correctly. The regression is model behavior, not template loading. Data scaling (Stage 3) is the actual fix.
- If diagnostic is significantly higher (>0.30): template fix helped. Record the improvement.

Print both scores for the checkpoint below.
  </action>
  <verify>
    <automated>test -f results/lyra_custom_diagnostic.json && .venv/bin/python -c "import json; r=json.load(open('results/lyra_custom_diagnostic.json')); cats=r['categories']; print('Diagnostic scores:'); [print(f'  {b[\"benchmark\"]}: {b[\"score\"]:.4f}') for c in cats for b in c['benchmarks']]"</automated>
  </verify>
  <acceptance_criteria>
    - results/lyra_custom_diagnostic.json exists and is valid JSON
    - JSON contains categories[0].benchmarks with tool-call-format and code-syntax scores
    - Score values are between 0.0 and 1.0
  </acceptance_criteria>
  <done>Diagnostic eval JSON produced with tool-call-format and code-syntax scores for the existing Lyra model using the template-fixed eval script.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <what-built>Diagnostic eval completed on existing Lyra model with template-fixed eval_inference.py. This validates whether the chat_template loading was the root cause of the tool-call-format regression (0.41 base -> 0.19 Lyra).</what-built>
  <how-to-verify>
    1. Check the diagnostic score in results/lyra_custom_diagnostic.json
    2. Compare to previous Lyra score (0.1870) and base model score (0.4065)
    3. Interpretation guide:
       - Score ~0.19 (same as before): Template was NOT the issue. RESEARCH.md was correct -- transformers 5.5.4 already loads .jinja files. The real issue is model quality/data quantity. Data scaling (Plans 02/04/05) is the correct fix.
       - Score >0.30: Template fix had real impact. Both the fix AND data scaling will help.
       - Score >0.41: Template fix alone solves the regression (unlikely per RESEARCH.md).
    4. Note: Per RESEARCH.md's eval methodology concern, ALL 123 tool-calling test samples expect TEXT output (not tool calls). So the metric measures "how often the model hallucinates tool-call format when it should produce text." Lower may actually be better. This is an interpretation question, not a blocking issue.
    5. Data scaling (Plans 02/04/05) proceeds regardless of this result.
  </how-to-verify>
  <resume-signal>Type "continue" to proceed (data scaling continues regardless). Optionally share your interpretation of the diagnostic result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> eval metrics | Untrusted generation scored via regex (existing T-09-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-05 | Denial of Service | MPS memory during eval | mitigate | Sequential execution only (D-11); gc.collect() after model deletion (existing code) |
</threat_model>

<verification>
- results/lyra_custom_diagnostic.json exists with valid EvalResult schema
- Score is between 0.0 and 1.0 for both benchmarks
- User has seen the diagnostic result
</verification>

<success_criteria>
Diagnostic eval validates or invalidates the D-01 template hypothesis. The result informs interpretation but does not block subsequent plans -- data scaling proceeds regardless.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-tool-call-format-regression-fix/09.1-03-SUMMARY.md`
</output>
