---
phase: 09.1-tool-call-format-regression-fix
plan: 01
subsystem: training, evaluation
tags: [chat_template, jinja, tokenizer, tool-calling, regression-fix]

# Dependency graph
requires:
  - phase: 08-fine-tuning
    provides: train.py merge step and eval_inference.py inference pipeline
provides:
  - Defensive chat_template loading in eval_inference.py with jinja fallback
  - Belt-and-suspenders template persistence in train.py merge step
  - Generation marker stripping for inference-safe templates
affects: [09.1-03-diagnostic-eval, 09.1-05-retrain, eval_inference, train]

# Tech tracking
tech-stack:
  added: []
  patterns: [jinja-fallback-template-loading, belt-and-suspenders-template-persistence, generation-marker-stripping]

key-files:
  created: []
  modified:
    - scripts/eval_inference.py
    - scripts/train.py
    - tests/test_eval_inference.py
    - tests/test_train.py

key-decisions:
  - "Jinja fallback reads chat_template.jinja when tokenizer.chat_template is None (D-03)"
  - "Template persistence writes inline to tokenizer_config.json after save_pretrained (D-04)"
  - "Strip {% generation %} and {% endgeneration %} TRL markers before writing inference template"

patterns-established:
  - "Template fallback: check tokenizer.chat_template, fall back to .jinja file, log warning"
  - "Template persistence: json.loads/dumps round-trip on tokenizer_config.json after merge"

requirements-completed: [EVAL-01, TOOL-01]

# Metrics
duration: 3min
completed: 2026-04-22
---

# Phase 09.1 Plan 01: Code Fixes Summary

**Defensive chat_template loading in eval_inference.py with jinja fallback plus belt-and-suspenders template persistence in train.py merge step**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-22T05:10:43Z
- **Completed:** 2026-04-22T05:13:40Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- eval_inference.py now explicitly verifies chat_template after tokenizer load and falls back to .jinja file when missing (D-03)
- train.py merge step persists chat_template inline into tokenizer_config.json with TRL generation markers stripped (D-04)
- Six new tests covering: jinja fallback, no-jinja-file behavior, existing template preservation, config write, marker stripping, tool_call format preservation

## Task Commits

Each task was committed atomically:

1. **Task 1: Add defensive chat_template loading to eval_inference.py** - `f59ca34` (feat)
2. **Task 2: Add chat_template persistence to train.py merge step** - `f46a44b` (feat)

## Files Created/Modified
- `scripts/eval_inference.py` - Added chat_template verification with jinja fallback in _do_load_model_and_tokenizer
- `scripts/train.py` - Added template persistence to tokenizer_config.json in merge step with generation marker stripping
- `tests/test_eval_inference.py` - Three new tests for template loading fallback behavior
- `tests/test_train.py` - Three new tests for template persistence and marker stripping

## Decisions Made
- Used Path(model_path) / "chat_template.jinja" for fallback path (consistent with transformers 5.5.4 .jinja file convention)
- Used `import json as _json` alias in merge block to avoid any future shadowing if json is imported at module level
- Kept template verification purely additive -- no changes to function signatures or return types

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- eval_inference.py ready for diagnostic eval (Plan 03) -- template will be loaded correctly
- train.py ready for retrain (Plan 05) -- future merges will persist template in tokenizer_config.json
- Both scripts are prerequisites for downstream plans in this phase

## Self-Check: PASSED

All files found, all commits verified, SUMMARY.md created.

---
*Phase: 09.1-tool-call-format-regression-fix*
*Completed: 2026-04-22*
