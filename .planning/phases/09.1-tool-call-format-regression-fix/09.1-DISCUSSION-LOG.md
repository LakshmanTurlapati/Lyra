# Phase 09.1: Tool-Call Format Regression Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 09.1-tool-call-format-regression-fix
**Areas discussed:** Fix strategy, Template fix location, Data balance, Generation method, Eval order, Success bar

---

## Fix Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Template fix first | Fix template, re-eval. If scores recover, skip retraining. | |
| Fix template + retrain + scale | Fix bug AND scale to 25K and retrain regardless. | ✓ |
| Scale data + retrain only | Skip template investigation, go straight to 25K retrain. | |

**User's choice:** Fix template + retrain + scale
**Notes:** User wants both the bug fix and data scaling, not one or the other.

---

## Template Fix Location

| Option | Description | Selected |
|--------|-------------|----------|
| Save in train.py merge step | After merge_and_unload(), write chat_template to tokenizer_config.json. Also fix eval. | ✓ |
| Patch eval_inference.py only | Have eval load template manually. Faster but doesn't fix root cause. | |

**User's choice:** Save in train.py merge step
**Notes:** Single source of truth in training pipeline.

---

## Data Balance

| Option | Description | Selected |
|--------|-------------|----------|
| Keep current ratios | Scale proportionally to 25K. ~68% tool, ~16.5% code, ~15.4% knowledge. | ✓ |
| Rebalance toward equal thirds | ~8.3K each domain. | |
| Heavy tool-call skew (80%+) | ~20K tool-calling, ~2.5K code, ~2.5K knowledge. | |

**User's choice:** Keep current ratios
**Notes:** Preserves tool-call focus while maintaining domain coverage.

---

## Generation Method

**User's clarification:** Generation scripts are template-based and run locally — no API calls. The Anthropic Batch API question was based on a misunderstanding of the data pipeline.

---

## Eval Order

| Option | Description | Selected |
|--------|-------------|----------|
| Quick eval first | Fix template in eval, re-run on existing model to validate diagnosis. | ✓ |
| Straight to retrain | Fix everything, retrain, then evaluate. | |

**User's choice:** Quick eval first
**Notes:** Validates the diagnosis before investing in full retraining.

---

## Success Bar

| Option | Description | Selected |
|--------|-------------|----------|
| Beat base model (>0.41) | Must outperform SmolLM2-1.7B-Instruct on tool-call-format. | ✓ |
| Significant improvement (>0.60) | Needs meaningful capability improvement. | |
| Any improvement over current (>0.19) | Just fix the regression. | |

**User's choice:** Beat base model (>0.41)
**Notes:** The whole point of fine-tuning.

---

## Claude's Discretion

- Eval template loading implementation details
- Batch count distribution across categories
- LoRA hyperparameter adjustments for larger dataset

## Deferred Ideas

None
