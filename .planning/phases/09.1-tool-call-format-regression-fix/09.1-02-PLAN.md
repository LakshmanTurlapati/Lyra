---
phase: 09.1-tool-call-format-regression-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - datasets/tool-calling/single-call-batch-24.jsonl
  - datasets/tool-calling/single-call-batch-25.jsonl
  - datasets/tool-calling/cli-batch-18.jsonl
  - datasets/tool-calling/multi-turn-batch-11.jsonl
  - datasets/tool-calling/parallel-batch-11.jsonl
  - datasets/tool-calling/mcp-batch-08.jsonl
  - datasets/code/utility-batch-35.jsonl
  - datasets/code/file-ops-batch-18.jsonl
  - datasets/code/debugging-batch-18.jsonl
  - datasets/knowledge/qa-batch-28.jsonl
  - datasets/knowledge/explanation-batch-24.jsonl
  - datasets/knowledge/reasoning-batch-18.jsonl
autonomous: true
requirements: [TOOL-01, EVAL-01]

must_haves:
  truths:
    - "Tool-calling raw directory contains at least 450 batch JSONL files total (68 existing + ~382 new)"
    - "Code raw directory contains at least 148 batch JSONL files total (68 existing + ~80 new)"
    - "Knowledge raw directory contains at least 145 batch JSONL files total (67 existing + ~78 new)"
    - "All generated batches pass Pydantic Conversation validation"
  artifacts:
    - path: "datasets/tool-calling/"
      provides: "Raw tool-calling JSONL batches at scale (~23K raw samples)"
    - path: "datasets/code/"
      provides: "Raw code JSONL batches at scale (~4K raw samples)"
    - path: "datasets/knowledge/"
      provides: "Raw knowledge JSONL batches at scale (~4K raw samples)"
  key_links:
    - from: "scripts/generate_tool_data.py"
      to: "datasets/tool-calling/*.jsonl"
      via: "batch generation loop with --batch flag"
      pattern: "generate_tool_data.*--batch"
    - from: "scripts/generate_code_data.py"
      to: "datasets/code/*.jsonl"
      via: "batch generation loop with --batch flag"
      pattern: "generate_code_data.*--batch"
    - from: "scripts/generate_knowledge_data.py"
      to: "datasets/knowledge/*.jsonl"
      via: "batch generation loop with --batch flag"
      pattern: "generate_knowledge_data.*--batch"
---

<objective>
Generate approximately 21,000 additional training samples across all three domains to reach the 25K total target (per D-05, D-06, D-07). Uses existing template-based generation scripts with new batch numbers, maintaining current domain ratios: ~68% tool-calling, ~16.5% code, ~15.4% knowledge.

Purpose: Per D-05, scaling from 3.6K to 25K samples is the primary fix for improving model quality. More diverse training data will improve the model's ability to generate correct tool calls.
Output: ~540 new JSONL batch files across 3 domain directories, ready for curation in Plan 04.
</objective>

<execution_context>
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-CONTEXT.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Generation script CLI interfaces -->

scripts/generate_tool_data.py:
  --category {single-call,cli,multi-turn,parallel,mcp}
  --count COUNT (default: 50)
  --batch BATCH (positive integer)
  --seed SEED (optional, derived from batch*100+offset if not specified)
  --output-dir OUTPUT_DIR (default: datasets/tool-calling/)

scripts/generate_code_data.py:
  --category {utility,file-ops,debugging}
  --count COUNT (default: 50)
  --batch BATCH (positive integer)
  --output-dir OUTPUT_DIR (default: datasets/code/)

scripts/generate_knowledge_data.py:
  --category {qa,explanation,reasoning}
  --count COUNT (default: 50)
  --batch BATCH (positive integer)
  --output-dir OUTPUT_DIR (default: datasets/knowledge/)

Existing batch counts (continue numbering from):
  Tool-calling: single-call 1-23, cli 1-17, multi-turn 1-10, parallel 1-10, mcp 1-7
  Code: utility 1-34, file-ops 1-17, debugging 1-17
  Knowledge: qa 1-27, explanation 1-23, reasoning 1-17
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate additional tool-calling data (~395 batches)</name>
  <files>datasets/tool-calling/*.jsonl (new batch files)</files>
  <read_first>
    - scripts/generate_tool_data.py (first 30 lines for CLI interface)
    - datasets/tool-calling/ (ls to confirm last batch numbers)
  </read_first>
  <action>
Generate additional tool-calling batches to reach ~19,750 raw samples (targeting ~17,000 after curation at 73.6% retention rate per RESEARCH.md). Per D-06, maintain the existing category distribution. Per D-07, use existing generate_tool_data.py with new batch numbers.

Current raw samples: ~3,350 (68 batches * ~50 samples each).
Target raw: ~23,100 total (~19,750 new).
Approximate new batches needed: ~395 (at 50 samples/batch).

Category distribution for new batches (matching existing ratios):
- single-call: 185 batches (batch 24 through 208)
- cli: 130 batches (batch 18 through 147)  
- multi-turn: 40 batches (batch 11 through 50)
- parallel: 30 batches (batch 11 through 40)
- mcp: 10 batches (batch 8 through 17)

Run each category as a shell for-loop. Example for single-call:
```bash
for batch in $(seq 24 208); do
    .venv/bin/python -m scripts.generate_tool_data --category single-call --count 50 --batch $batch
done
```

Repeat for each category with appropriate ranges. Run categories sequentially (not in parallel) to avoid filesystem contention and maintain predictable seed sequences.

After generation, verify:
1. Count total JSONL files in datasets/tool-calling/ (should be ~450+)
2. Spot-check last file has valid JSON lines: `head -1 datasets/tool-calling/single-call-batch-208.jsonl | python -m json.tool`

IMPORTANT: These are local template-based generators (no API calls). Each batch takes <1 second. Total time ~5-10 minutes for all 395 batches.
  </action>
  <verify>
    <automated>ls datasets/tool-calling/*.jsonl | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ls datasets/tool-calling/*.jsonl | wc -l shows at least 450 files (68 existing + ~382 new + tool-calling-raw.jsonl)
    - ls datasets/tool-calling/single-call-batch-208.jsonl confirms last single-call batch exists
    - ls datasets/tool-calling/cli-batch-147.jsonl confirms last cli batch exists
    - head -1 datasets/tool-calling/single-call-batch-100.jsonl | python -m json.tool parses successfully
  </acceptance_criteria>
  <done>Tool-calling directory contains 450+ JSONL batch files covering all 5 categories at the scaled-up batch counts.</done>
</task>

<task type="auto">
  <name>Task 2: Generate additional code and knowledge data (~158 batches)</name>
  <files>datasets/code/*.jsonl (new batch files), datasets/knowledge/*.jsonl (new batch files)</files>
  <read_first>
    - scripts/generate_code_data.py (first 30 lines for CLI interface)
    - scripts/generate_knowledge_data.py (first 30 lines for CLI interface)
    - datasets/code/ (ls to confirm last batch numbers)
    - datasets/knowledge/ (ls to confirm last batch numbers)
  </read_first>
  <action>
Generate additional code and knowledge batches to reach scaled targets per D-06.

**Code domain** (targeting ~4,125 curated, ~4,680 raw at 88.2% retention):
Current raw: ~3,400 (68 batches * ~50). Target raw: ~4,680 total (~1,280 new = ~26 batches).
- utility: 10 batches (batch 35 through 44)
- file-ops: 8 batches (batch 18 through 25)
- debugging: 8 batches (batch 18 through 25)

```bash
for batch in $(seq 35 44); do
    .venv/bin/python -m scripts.generate_code_data --category utility --count 50 --batch $batch
done
for batch in $(seq 18 25); do
    .venv/bin/python -m scripts.generate_code_data --category file-ops --count 50 --batch $batch
done
for batch in $(seq 18 25); do
    .venv/bin/python -m scripts.generate_code_data --category debugging --count 50 --batch $batch
done
```

**Knowledge domain** (targeting ~3,875 curated, ~4,570 raw at 84.8% retention):
Current raw: ~3,350 (67 batches * ~50). Target raw: ~4,570 total (~1,220 new = ~25 batches).
- qa: 10 batches (batch 28 through 37)
- explanation: 8 batches (batch 24 through 31)
- reasoning: 7 batches (batch 18 through 24)

```bash
for batch in $(seq 28 37); do
    .venv/bin/python -m scripts.generate_knowledge_data --category qa --count 50 --batch $batch
done
for batch in $(seq 24 31); do
    .venv/bin/python -m scripts.generate_knowledge_data --category explanation --count 50 --batch $batch
done
for batch in $(seq 18 24); do
    .venv/bin/python -m scripts.generate_knowledge_data --category reasoning --count 50 --batch $batch
done
```

Run all loops sequentially. Each batch takes <1 second. Total ~3-5 minutes.

After generation, verify file counts and spot-check JSON validity.
  </action>
  <verify>
    <automated>echo "Code batches:"; ls datasets/code/*.jsonl | wc -l; echo "Knowledge batches:"; ls datasets/knowledge/*.jsonl | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ls datasets/code/*.jsonl | wc -l shows at least 94 files (68 existing + 26 new)
    - ls datasets/knowledge/*.jsonl | wc -l shows at least 92 files (67 existing + 25 new)
    - ls datasets/code/utility-batch-44.jsonl confirms last code batch exists
    - ls datasets/knowledge/qa-batch-37.jsonl confirms last knowledge batch exists
  </acceptance_criteria>
  <done>Code directory contains 94+ and knowledge directory contains 92+ JSONL batch files at scaled-up counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template pool -> JSONL output | Template-generated data written to filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-03 | Tampering | JSONL batch files | accept | Template-based generation produces deterministic output from seeded random; Pydantic validation runs during generation |
| T-09.1-04 | Denial of Service | Disk space from 540 new files | accept | ~50 samples * 540 batches * ~2KB/sample = ~54MB total; negligible |
</threat_model>

<verification>
- Tool-calling: 450+ JSONL files in datasets/tool-calling/
- Code: 94+ JSONL files in datasets/code/
- Knowledge: 92+ JSONL files in datasets/knowledge/
- Spot-check: random batch files contain valid JSON lines
</verification>

<success_criteria>
All three domain directories contain scaled-up raw JSONL batches ready for the curation pipeline (Plan 04). No API calls made -- all data generated locally from templates.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-tool-call-format-regression-fix/09.1-02-SUMMARY.md`
</output>
