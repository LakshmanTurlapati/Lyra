---
phase: 09.1-tool-call-format-regression-fix
plan: 05
type: execute
wave: 3
depends_on: [09.1-01, 09.1-04]
files_modified:
  - models/lyra-adapter/adapter_model.safetensors
  - models/lyra-adapter/adapter_config.json
  - models/lyra-merged/model.safetensors
  - models/lyra-merged/tokenizer_config.json
  - models/lyra-merged/chat_template.jinja
autonomous: true
requirements: [EVAL-01, TOOL-01]

must_haves:
  truths:
    - "Lyra is retrained on the 25K-scale dataset with adjusted hyperparameters (lr=1e-4, epochs=2)"
    - "The merged model directory contains chat_template in tokenizer_config.json (per D-04 template persistence fix)"
    - "Training completes without MPS memory errors"
  artifacts:
    - path: "models/lyra-adapter/"
      provides: "LoRA adapter weights from 25K training"
    - path: "models/lyra-merged/model.safetensors"
      provides: "Merged full model weights for evaluation"
    - path: "models/lyra-merged/tokenizer_config.json"
      provides: "Tokenizer config WITH chat_template inline (per D-04)"
      contains: "chat_template"
  key_links:
    - from: "scripts/train.py"
      to: "models/lyra-merged/"
      via: "--dataset-dir datasets/assembled --lr 1e-4 --epochs 2"
      pattern: "train\\.py.*--lr.*--epochs"
    - from: "models/lyra-merged/tokenizer_config.json"
      to: "scripts/eval_inference.py"
      via: "AutoTokenizer.from_pretrained loads chat_template from config"
      pattern: "from_pretrained.*lyra-merged"
---

<objective>
Retrain SmolLM2-1.7B on the 25K-scale assembled dataset using the template-fixed train.py from Plan 01. Uses adjusted hyperparameters for the larger dataset per RESEARCH.md: learning rate 1e-4 (down from 2e-4) and 2 epochs (down from 3).

Purpose: Per D-05, data scaling is the primary fix for improving model quality. The 7x larger dataset with the template persistence fix should produce a model that outperforms the base on tool-call-format (>0.41 per D-09) while maintaining knowledge benchmarks within 5% (per D-10).
Output: Retrained LoRA adapter + merged model weights with chat_template persisted in tokenizer_config.json.
</objective>

<execution_context>
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-CONTEXT.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-RESEARCH.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-01-SUMMARY.md
@.planning/phases/09.1-tool-call-format-regression-fix/09.1-04-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From scripts/train.py CLI:
  --model-name MODEL (default: HuggingFaceTB/SmolLM2-1.7B-Instruct)
  --dataset-dir DIR (default: datasets/assembled)
  --output-dir DIR (default: models/lyra-adapter)
  --merged-dir DIR (default: models/lyra-merged)
  --lr FLOAT (default: 2e-4)
  --epochs INT (default: 3)
  --batch-size INT (default: 4)
  --grad-accum INT (default: 4)
  --max-length INT (default: 4096)
  --lora-r INT (default: 16)
  --lora-alpha INT (default: 32)
  --max-steps INT (default: -1, use epochs)
  --no-merge (flag)

Phase 8 training decisions (still apply):
  - MPS primary (D-01), CPU fallback (D-02)
  - LoRA rank 16, alpha 32, all 7 projection layers (D-04, D-05)
  - 16-bit LoRA on MPS (no bitsandbytes QLoRA on MPS)
  - Batch size 4, gradient accumulation 4 (effective batch 16)

Adjusted for 25K per RESEARCH.md:
  - Learning rate: 1e-4 (down from 2e-4 -- larger dataset needs slower convergence)
  - Epochs: 2 (down from 3 -- 7x more data per epoch compensates)
  - Estimated training time: 2.5-5 hours on MPS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test training on 25K dataset (10 steps)</name>
  <files>models/lyra-adapter/ (temporary smoke test output)</files>
  <read_first>
    - scripts/train.py (full file -- verify template persistence fix from Plan 01 is present)
    - datasets/assembled/ (verify dataset loads with updated counts)
  </read_first>
  <action>
Before committing to a multi-hour training run, do a quick smoke test with --max-steps 10 to verify:
1. The 25K dataset loads correctly
2. Training starts without errors on MPS
3. Loss is decreasing (not NaN or exploding)

```bash
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/python scripts/train.py \
    --lr 1e-4 \
    --epochs 2 \
    --max-steps 10 \
    --output-dir models/lyra-adapter-smoke \
    --merged-dir models/lyra-merged-smoke \
    --no-merge
```

Use a 600000ms timeout. The smoke test should complete in under 5 minutes.

Check the output for:
- "[Lyra] Dataset loaded: train=XXXXX" -- should show the scaled-up count (>14000)
- Training loss values in the progress output -- should be finite numbers decreasing
- No MPS memory errors or CUDA out-of-memory

If smoke test passes, clean up: `rm -rf models/lyra-adapter-smoke models/lyra-merged-smoke`

If smoke test fails with OOM: the 25K dataset may need --max-length 2048 (halving sequence length to fit). Try with --max-length 2048 before proceeding.
  </action>
  <verify>
    <automated>echo "Smoke test artifacts cleaned up (or never created if inline)"; ls models/lyra-adapter-smoke 2>/dev/null && echo "WARN: smoke test dir still exists" || echo "OK: smoke test dir cleaned"</automated>
  </verify>
  <acceptance_criteria>
    - Smoke test completes 10 steps without errors
    - Training loss is finite and decreasing
    - models/lyra-adapter-smoke cleaned up after success
  </acceptance_criteria>
  <done>25K dataset verified loadable and trainable on MPS with adjusted hyperparameters. Ready for full training run.</done>
</task>

<task type="auto">
  <name>Task 2: Full retrain on 25K dataset with adjusted hyperparameters</name>
  <files>
    models/lyra-adapter/adapter_model.safetensors,
    models/lyra-adapter/adapter_config.json,
    models/lyra-merged/model.safetensors,
    models/lyra-merged/tokenizer_config.json,
    models/lyra-merged/chat_template.jinja
  </files>
  <read_first>
    - models/lyra-merged/tokenizer_config.json (check current state before overwrite)
  </read_first>
  <action>
Run the full training with adjusted hyperparameters for the 25K dataset. Per RESEARCH.md: lr=1e-4, epochs=2, all other params at Phase 8 defaults.

CRITICAL MEMORY CONSTRAINT (D-11): This command MUST NOT run in parallel with any other model-loading process. It will consume significant MPS memory for 2.5-5 hours.

```bash
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/python scripts/train.py \
    --lr 1e-4 \
    --epochs 2
```

Use a 600000ms timeout (10 minutes max per tool call, but training takes 2.5-5 hours total -- the executor should use run_in_background or extend timeout as needed for long training runs).

NOTE: Training on 25K samples at batch_size=4, grad_accum=4, epochs=2 produces approximately 2,812 gradient steps (25000 * 0.9 / 16 * 2). At ~1 step/sec on MPS, this takes ~47 minutes minimum, likely 2-4 hours with overhead.

After training completes, verify:
1. `models/lyra-adapter/adapter_model.safetensors` exists (LoRA weights)
2. `models/lyra-merged/model.safetensors` exists (merged weights)
3. `models/lyra-merged/tokenizer_config.json` contains `"chat_template"` key (D-04 persistence fix)
4. The chat_template value in tokenizer_config.json does NOT contain `{% generation %}` markers

Verification command:
```bash
.venv/bin/python -c "
import json
config = json.load(open('models/lyra-merged/tokenizer_config.json'))
assert 'chat_template' in config, 'FAIL: chat_template not in tokenizer_config.json'
assert '{% generation %}' not in config['chat_template'], 'FAIL: generation markers leaked'
assert '<tool_call>' in config['chat_template'], 'FAIL: tool_call handling missing'
print('Template persistence verified OK')
print(f'Template length: {len(config[\"chat_template\"])} chars')
"
```
  </action>
  <verify>
    <automated>test -f models/lyra-merged/model.safetensors && .venv/bin/python -c "import json; c=json.load(open('models/lyra-merged/tokenizer_config.json')); assert 'chat_template' in c; assert '{% generation %}' not in c['chat_template']; print('Retrain + template persistence verified OK')"</automated>
  </verify>
  <acceptance_criteria>
    - models/lyra-adapter/adapter_model.safetensors exists (LoRA weights)
    - models/lyra-merged/model.safetensors exists (merged full model)
    - models/lyra-merged/tokenizer_config.json contains "chat_template" key
    - chat_template value does NOT contain "{% generation %}" markers
    - chat_template value DOES contain "<tool_call>" formatting
  </acceptance_criteria>
  <done>Full retrain completed on 25K dataset. Merged model saved with chat_template persisted in tokenizer_config.json. Model ready for re-evaluation in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training data -> model weights | Model learns from curated data (all validated via curation pipeline) |
| model weights -> filesystem | Safetensors format prevents arbitrary code execution on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-08 | Denial of Service | MPS memory during training | mitigate | Single training process only (D-11); smoke test first (Task 1); gradient checkpointing enabled |
| T-09.1-09 | Tampering | Model weights integrity | accept | Safetensors format validates tensor metadata on load; no external model weights involved |
</threat_model>

<verification>
- LoRA adapter weights exist in models/lyra-adapter/
- Merged model weights exist in models/lyra-merged/
- tokenizer_config.json contains chat_template WITHOUT generation markers
- chat_template includes tool_call XML format handling
</verification>

<success_criteria>
Retrained Lyra model on 25K-scale dataset with template persistence fix baked into the merge step. Model is ready for comprehensive re-evaluation against base model baselines.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-tool-call-format-regression-fix/09.1-05-SUMMARY.md`
</output>
