---
phase: 09.2-tool-call-regression-diagnosis
plan: 05
subsystem: diagnosis
tags: [diagnosis, decision, synthesis, human-gate, checkpoint-pending, partial]

# Dependency graph
requires:
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 01
    provides: D07-REPRODUCIBILITY.md (PASS amended)
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 02
    provides: tests/test_phase_09_2/test_template_parity.py (2 GREEN; H5 eliminated)
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 03
    provides: D03-D05-CATEGORIZATION.md + .json (mode-collapse evidence)
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 04
    provides: D06-TRAINING-AUDIT.md (0%/46%/90.22% source-side evidence)
provides:
  - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md (committed, draft status awaiting user sign-off)
affects: [09.2-06-plan-dispatch]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Evidence-based hypothesis ranking: prior P + posterior P + supporting evidence per hypothesis"
    - "Three-branch decision table: each branch has when-to-pick / why / risk / wall-clock estimate"
    - "Explicit caveat surfacing: the D-09-vs-current-split gap flagged inside the 06C branch (prevents later surprise)"
    - "Checkpoint-type decision plan: draft DIAGNOSIS.md, commit it, return decision checkpoint to orchestrator; user picks branch before Plan 06 dispatches"

key-files:
  created:
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md
  modified: []

key-decisions:
  - "Recommended 06A (retrain with rebalanced data + adjusted hyperparameters) because H1 is confirmed at posterior ~90% and 06A is the only branch that directly addresses training-distribution mode collapse"
  - "Flagged 06C's hidden D-09 problem: base model's tool-call-format against the current post-9bec343 test split is 0.3422, not 0.4065. Shipping base does not automatically clear D-09 as literally written — user must consciously choose renegotiation, split swap, or D-02 acceptance"
  - "Posterior probabilities: H1 ~90%, H2 ~3%, H3 ~5%, H4 ~2%, H5 0% (sum to 100%). H1 is strongly supported by both behavioral (Plan 03) and source-side (Plan 04) quantitative evidence; H5 is mechanically refuted by Plan 02's pytest parity proof"
  - "Did NOT populate Plan 06's branch selection; DIAGNOSIS.md ends with a sign-off section and an explicit note that Plan 06 will refuse to dispatch without user approval"

patterns-established:
  - "DIAGNOSIS.md template structure: Executive Summary → Evidence Summary table → Causal Chain → Hypothesis Ranking → Root Cause → Three-branch analysis (each with when-to-pick, why, risk, wall-clock) → Recommendation rationale (cites evidence docs) → Alternative framings → Sign-off checklist"
  - "Plan dispatch guard: DIAGNOSIS.md's Sign-off section is the load-bearing artifact Plan 06 reads to pick a branch; Plan 06 refuses to run without an explicit approval signal"

requirements-completed: [TOOL-01, EVAL-01]

# Metrics (partial — plan not yet complete; awaiting user sign-off)
duration: ~30min (synthesis + draft)
completed: null (awaiting user sign-off on Chosen Action)
status: partial-awaiting-checkpoint
---

# Phase 09.2 Plan 05: DIAGNOSIS Synthesis — PARTIAL (awaiting user sign-off)

**Wave 1-2 evidence has been synthesized into a committed 09.2-DIAGNOSIS.md. Root cause named: H1 training-distribution mode collapse (posterior ~90%). Three corrective-action branches (06A retrain, 06B patch, 06C revert) documented with explicit pros/cons and wall-clock estimates. Recommended branch: 06A with 06C as a pre-committed fall-back. Plan is PAUSED at the checkpoint:decision gate pending user sign-off. The diagnosis is a draft until the user replies with "approve 06A", "approve 06B", "approve 06C", or a redirect/revision request.**

## Status

**PARTIAL.** This SUMMARY is written at the checkpoint handoff. The plan is not complete until:

1. User reviews `09.2-DIAGNOSIS.md` and approves one of the three branches (or redirects to a different corrective action).
2. The Sign-off section checkboxes in `09.2-DIAGNOSIS.md` are filled in with the approved branch circled and the date recorded.
3. A final SUMMARY (replacing this partial) is written with the approved branch, timestamp, and explicit next step.

Orchestrator note: a fresh executor will be spawned after user sign-off to finalize the SUMMARY and dispatch Plan 06.

## What Was Produced

### `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md` (committed, `c068400`)

A 200-line diagnosis document with the following sections, each populated from real Wave 1-2 evidence:

1. **Executive Summary** — Root cause in two paragraphs: H1 mode collapse, coupled with domain skew; three branches available but none obviously dominant.
2. **Wave 1-2 complete: YES** — Table of four upstream plan outputs with status.
3. **Evidence Summary** — Five-row table crossing D-07 / D-04 / D-03 / D-05 / D-06 with specific findings and measured values.
4. **Cross-Evidence Causal Chain** — Five-step narrative linking training distribution numbers to observed behavior at inference.
5. **Hypothesis Ranking** — H1 / H2 / H3 / H4 / H5 with prior P, posterior P, evidence column, and status. H1 confirmed at ~90%; H5 refuted at 0%.
6. **Root Cause** — Explicit H1 identification with the two coupled pathologies (suffix memorization + domain skew) and the data-generation-layer locus.
7. **Chosen Action** — Three branches (06A / 06B / 06C) with full detail:
   - 06A **(recommended)**: data changes + hyperparameter menu, success gate, risk, wall-clock.
   - 06B **(not selected)**: when it would apply, why evidence doesn't support it.
   - 06C **(documented)**: scope + the D-09-against-current-split caveat + three sub-options.
8. **Rationale for 06A Recommendation** — Five numbered points citing all three evidence documents (D07-REPRODUCIBILITY.md, D03-D05-CATEGORIZATION.md, D06-TRAINING-AUDIT.md).
9. **Alternative Recommendations** — Two reasonable alternative framings (start with 06C now; cap 06A at one attempt).
10. **Sign-off** — 7-item checklist ending with explicit "approve 06A / 06B / 06C" prompt.
11. **Appendix — Key Numbers Quick-Reference** — 15-row table with every measured value cited in the document and its source file.

The document's sign-off section is the load-bearing artifact for Plan 06 dispatch: Plan 06 reads the approved branch from this file.

## Evidence Consolidation Summary

| Source | Key Finding | Hypothesis Impact |
|--------|-------------|-------------------|
| D-07 (Plan 01) amended | PASS — base re-baseline is 0.3422 on current split (not environment drift; 9bec343 test-set change). | Eliminates "eval pipeline broken" explanation; baseline math is internally consistent. |
| D-04 (Plan 02) | PASS — 2 GREEN pytests: stripped train template == persisted template; identical token IDs on fixture. | H5 mechanically REFUTED. |
| D-03 (Plan 03) | 85% of dataset-mode generations are one of two memorized response shells. | H1 behaviorally confirmed (downstream shape). |
| D-05 (Plan 03) | 7/8 single-turn generations collapse onto `get_weather(Tokyo)` or `git_commit("Fix typo in docs")`. | H1 behaviorally confirmed (trigger + semantic selection). |
| D-06 (Plan 04) | 0.00% tool-call-ending / 46.0% top-5 canned suffix / 90.22% tool-calling skew. | H1 source-side confirmed; quantitatively predicts every D-03/D-05 signature. |

## Hypothesis Posterior Probabilities (from DIAGNOSIS.md)

| Hypothesis | Prior | Posterior | Status |
|------------|------:|----------:|--------|
| H1 training-distribution mode collapse | ~70% | **~90%** | STRONGLY CONFIRMED |
| H2 assistant_only_loss mask interaction | ~15% | ~3% | WEAK / UNLIKELY |
| H3 eval methodology artifact | ~10% | ~5% | PARTIALLY CONTRIBUTORY, NOT ROOT CAUSE |
| H4 MPS merge-time numerics drift | ~4% | ~2% | LOW / NOT INVESTIGATED |
| H5 chat template persistence bug | ~1% | 0% | REFUTED |

## Recommended Branch (Not Yet Approved)

**06A — Retrain v3 with rebalanced data + adjusted hyperparameters, fall back to 06C if v3 fails D-09.**

Recommendation rationale (full text in DIAGNOSIS.md):

1. H1 is the confirmed root cause and lives in training-data distribution; 06A is the only branch that can directly change those numbers.
2. 06B requires single-point-bug evidence that Wave 1-2 did not produce.
3. 06C has a hidden D-09 problem (base 0.3422 vs bar 0.41 on current split) that requires conscious user choice among three sub-options.
4. 06A with 06C as a pre-committed fall-back bounds retrain cost at 1–2 attempts (~5–11 h).
5. 06A is the only path that yields a Lyra-branded artifact clearing D-09.

## Awaiting

**User sign-off on the corrective-action branch.** Acceptable responses:

- `approve 06A` — proceed to Plan 06 Branch A (retrain v3). User should also confirm (or adjust) the specific data + hyperparameter menu in the DIAGNOSIS.md.
- `approve 06B` — proceed to Plan 06 Branch B (targeted patch). User should supply the specific file + line + transformation to patch, since Wave 1-2 did not surface one.
- `approve 06C` — proceed to Plan 06 Branch C (ship base as v1.0). User should pick one of the three D-09 sub-options (06C.i renegotiate bar / 06C.ii swap split / 06C.iii accept D-09 miss per D-02).
- `redirect: <description>` — any other response describing a different path forward or requesting a revision to the DIAGNOSIS.

Once the user replies, a continuation executor will:

1. Append the approved branch + date to the DIAGNOSIS.md Sign-off section (check the boxes, fill in the date).
2. Write the final (non-partial) SUMMARY.md replacing this one, recording: approved branch, timestamp, next step.
3. Commit the sign-off update atomically.
4. Return the completion message to the orchestrator, who will then dispatch Plan 06 on the approved branch.

## Commits So Far

1. **Task 1 (draft DIAGNOSIS.md)** — `c068400` (docs)
2. **This partial SUMMARY** — to be committed next.

No other files modified. No model loads. No orchestrator-shared files (STATE.md, ROADMAP.md) touched.

## Deviations from Plan

### Rule 2 — Added explicit D-09-against-current-split caveat to 06C

- **Found during:** Drafting the 06C section of DIAGNOSIS.md.
- **Issue:** The plan's 06C option description says "base model's 0.4065 score satisfies D-09 trivially" — but 0.4065 is the pre-9bec343 test-split score, archived in `results/base_custom_phase09_original.json`. The current authoritative `results/base_custom.json` is 0.3422 against the post-9bec343 test split (per D-07 amended). If DIAGNOSIS.md had implied that 06C auto-satisfies D-09, the user would be making a decision on false pretenses.
- **Fix:** Added a "CRITICAL CAVEAT — D-09 against the current test split" subsection inside Branch 06C, surfacing the 0.3422 vs 0.41 gap and enumerating three sub-options (renegotiate bar / swap measurement split / accept D-09 miss under D-02) for the user to pick among.
- **Rule type:** Rule 2 (missing critical information) — not strictly a bug, but essential for the user to make an informed decision. Without this caveat, the user could approve 06C expecting a clean D-09 pass and then discover mid-execution that they actually need a D-09 sub-decision.
- **Commit:** folded into the `c068400` DIAGNOSIS.md commit.

No other deviations. No model loads per `critical_user_constraint`. No orchestrator-shared files modified per `parallel_execution` contract.

## Self-Check: PARTIAL (checkpoint)

- `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md` exists — VERIFIED (committed at c068400).
- Commit `c068400` reachable via `git log --oneline -3` — VERIFIED.
- DIAGNOSIS.md contains all required sections (Evidence Summary, Hypothesis Ranking, Root Cause, Chosen Action with three branches, Rationale, Sign-off) — VERIFIED.
- No STATE.md / ROADMAP.md edits — VERIFIED (git status shows only DIAGNOSIS.md and this SUMMARY).
- No model loads — VERIFIED (no inference, no training, no eval scripts invoked).
- User sign-off awaited — PENDING (checkpoint returned to orchestrator).

## Next Step

**Checkpoint:decision returned to orchestrator.** User must reply with approve 06A / approve 06B / approve 06C (or redirect). On approval, a continuation executor finalizes sign-off + writes the full SUMMARY and Plan 06 dispatches the approved branch.

---
*Phase: 09.2-tool-call-regression-diagnosis*
*Plan: 05 (PARTIAL — awaiting user sign-off)*
*Last commit: c068400*
