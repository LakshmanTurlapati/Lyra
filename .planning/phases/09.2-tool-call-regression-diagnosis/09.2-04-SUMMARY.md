---
phase: 09.2-tool-call-regression-diagnosis
plan: 04
subsystem: testing
tags: [training-data, audit, domain-skew, canned-suffixes, d-06, pytest, xfail]

# Dependency graph
requires:
  - phase: 09.2-tool-call-regression-diagnosis
    provides: Plan 01 RED stub at tests/test_phase_09_2/test_training_audit.py awaiting a GREEN implementation
  - phase: 09.1-tool-call-format-regression-fix
    provides: datasets/assembled (re-assemblable from curated JSONL) and 09.1-05 loss curve (epoch1 0.1613 -> epoch2 0.1384)
provides:
  - "Four pytest gates on datasets/assembled/train: archival xfail (pct_end_in_tool_call == 0.0) + three forward-looking ceilings (< 0.10, <= 0.50, <= 0.92)"
  - "D06-TRAINING-AUDIT.md: consolidated loss-curve review + distribution fingerprint + canned-suffix frequency table for Plan 05's DIAGNOSIS.md"
  - "Quantitative source-side evidence for H1 mode-collapse: 0.00% tool-call endings, 46.0% top-5 suffix coverage, 90.22% tool-calling skew"
affects: [09.2-05-diagnosis, 09.2-06-plan-06A-retrain]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "pytest.mark.xfail(strict=False) as archival record of a present pathology that a future retrain is allowed to resolve without breaking CI"
    - "module-scoped fixtures to load HuggingFace datasets once per pytest run"
    - "ceiling-style thresholds (< ceiling) that pass before AND after the intended remediation"

key-files:
  created:
    - .planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md
  modified:
    - tests/test_phase_09_2/test_training_audit.py

key-decisions:
  - "Split the ending-type test into two: an xfail archival test for the 09.1 0.0% state + a forward-looking < 0.10 ceiling, so Plan 06A retrain can silently pass the archival test (strict=False) while the ceiling actively guards against over-correction"
  - "Ceiling-style thresholds (0.10 / 0.50 / 0.92) chosen so current values pass AND realistic Plan 06A target values (~4% tool-call endings, <46% coverage, <90% skew) also pass -- the gates only fire on pathology, not on planned remediation"
  - "Used top-5 60-char prefix coverage (not exact-string) to match RESEARCH.md Pitfall 3 semantics -- canned suffixes vary in tail but share openers"

patterns-established:
  - "Archival xfail pattern: document a current-state pathology with strict=False xfail so the test has a CI-visible artifact AND the same test will stay green after the intended fix"
  - "Distribution-audit script piped to tmp then pasted into markdown doc gives reproducible, checkable numbers -- re-run reproduces the fingerprint"

requirements-completed: [TOOL-01]

# Metrics
duration: 9min
completed: 2026-04-24
---

# Phase 09.2 Plan 04: Training-Data Audit (D-06) Summary

**Locked three numeric thresholds on datasets/assembled/train as pytest gates (1 archival xfail + 3 forward-looking ceilings) and consolidated the H1 mode-collapse source-side evidence into D06-TRAINING-AUDIT.md — confirming 0.00% tool-call endings, 46.0% top-5 canned-suffix coverage, and 90.22% tool-calling domain skew.**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-04-24T03:03:00Z (approximate)
- **Completed:** 2026-04-24T03:12:00Z (approximate)
- **Tasks:** 2
- **Files modified:** 2 (1 test rewritten GREEN, 1 new markdown audit)

## Accomplishments

- Turned the Plan 01 RED stub at `tests/test_phase_09_2/test_training_audit.py` GREEN with four tests: one archival xfail on the 09.1 0.0% tool-call-ending state and three forward-looking ceilings (< 0.10, <= 0.50, <= 0.92).
- Full pytest result: `3 passed, 1 xpassed in 1.56s`. Phase-wide run: `5 passed, 1 xpassed` (2 from Plan 02 + 4 from this plan).
- Ran the distribution audit on datasets/assembled/train (11,963 samples) and wrote D06-TRAINING-AUDIT.md (84 lines) with the six required sections: Loss Curve Review, Distribution Fingerprint, Canned-Suffix Prevalence, Findings, Locked Thresholds, Feeds Plan 05.
- Produced the three measured values that Plan 03 had predicted behaviorally:
  - **pct_end_in_tool_call = 0.00%** (0 of 10,793 tool-calling train samples end in a tool_call; 100% end in assistant text)
  - **Top-5 canned-suffix coverage = 46.0%** (4,965 of 10,793 tool-calling train samples share one of 5 openers; `"I've gathered all the information..."` alone is 12.8%)
  - **Tool-calling domain fraction = 90.22%** (10,793 of 11,963 train samples)

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement test_training_audit.py (TDD RED -> GREEN)** - `76c9c0c` (test)
2. **Task 2: Write D06-TRAINING-AUDIT.md** - `6d05295` (docs)

**Plan metadata (SUMMARY):** forthcoming in the commit that includes this file.

## Files Created/Modified

- `tests/test_phase_09_2/test_training_audit.py` — 95 insertions / 19 deletions; 102 lines total. Contains SPDX header, `_pct_end_in_tool_call` helper, two module-scoped fixtures (`assembled_train`, `tool_calling_train`), and four `@pytest.mark.slow` tests. The three literal thresholds (`0.10`, `0.50`, `0.92`) appear as source-level float constants.
- `.planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md` — 84 lines. Cites loss values `0.1613` and `0.1384` from 09.1-05. Cross-references `tests/test_phase_09_2/test_training_audit.py` as the CI gate for the numbers it reports.

## Decisions Made

- **Archival xfail with strict=False** — lets Plan 06A's retrain silently flip the xfail from XPASS to XFAIL without breaking CI, while the separate forward-looking ceiling stays active.
- **Ceiling-style thresholds** (less-than, not equal-to) — tolerate the intended Plan 06A remediation (~4% tool-call endings, moderate suffix-pool expansion) while still catching pathological drift.
- **60-char prefix slicing for top-5 coverage** — matches RESEARCH.md Pitfall 3 semantics (canned suffixes vary in tail but share the opening clause).
- **Module-scoped dataset fixture** — loads `datasets/assembled` once per pytest module run, cheap on the 13,292-sample Arrow dataset (<1 s).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Re-assembled datasets/assembled from curated JSONL sources**
- **Found during:** Pre-Task 1 setup (discovering `datasets/assembled/` absent in worktree filesystem).
- **Issue:** `datasets/assembled/` is a gitignored binary Arrow directory (per 09.1-04-SUMMARY). A fresh parallel-executor worktree does not carry ignored paths, so `datasets.load_from_disk("datasets/assembled")` would have raised `FileNotFoundError` before any test could run.
- **Fix:** Ran `.venv/bin/python -m scripts.assemble_dataset assemble --output-dir datasets/assembled --seed 42`, which re-materializes the Arrow dataset from the three committed curated JSONL files (tool-calling, code, knowledge). The assembled counts exactly match 09.1-04 (train 11,963 / val 666 / test 663; tool-calling 90.22% / code 4.88% / knowledge 4.90%).
- **Files modified:** none committed (Arrow output is gitignored; the committed curated JSONL inputs are unchanged).
- **Verification:** Both tasks ran against the re-assembled dataset and produced numbers that match 09.1-04 and RESEARCH.md predictions.
- **Committed in:** n/a (no source-controlled files changed; the fix is a reproducible build step)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix is a one-line reproducible build step already documented as a Phase 09.1 convention (curated JSONL → assembly script → Arrow dataset). No plan logic changed; no scope creep.

## Issues Encountered

- `.venv` lives at the Lyra root, not in the worktree. Resolved by invoking `/Users/lakshman/Documents/Lyra/.venv/bin/python` / `…/.venv/bin/pytest` directly. All test runs succeeded.

## User Setup Required

None — no external service configuration required. All measurements are on-disk against the assembled dataset.

## H1 Evidence Consolidation (per plan <output> spec)

**H1 (mode collapse via suffix memorization + domain skew) evidence consolidated; Plan 05 has D-04 (template ruled out), D-03/D-05 (probe evidence), and D-06 (this plan) to synthesize.**

The three measured values confirm H1's source-side signature:
- 0.00% of tool-calling train samples end in a tool_call → predicts single-turn mode collapse (Plan 03 D-05: 7/8 prompts → canned tool calls).
- 46.0% top-5 canned-suffix coverage → predicts canned-summary-template dominance (Plan 03 D-03: 45% `I've completed the task...` + 40% `[Tool result from X]:` echo).
- 90.22% tool-calling domain fraction → predicts MMLU −7.5% regression (only ~5% of gradient updates touched non-tool-calling content).

## Verification Evidence

```
$ .venv/bin/pytest tests/test_phase_09_2/test_training_audit.py -v --tb=short
collected 4 items

tests/test_phase_09_2/test_training_audit.py::test_tool_calling_assistant_ending_type_distribution_archival XPASS [ 25%]
tests/test_phase_09_2/test_training_audit.py::test_tool_calling_assistant_ending_type_ceiling_is_bounded PASSED [ 50%]
tests/test_phase_09_2/test_training_audit.py::test_top5_canned_suffix_coverage_is_bounded PASSED [ 75%]
tests/test_phase_09_2/test_training_audit.py::test_domain_skew_bounds PASSED [100%]

========================= 3 passed, 1 xpassed in 1.56s =========================
```

Phase-wide run (`tests/test_phase_09_2/`): `5 passed, 1 xpassed` — matches plan success criteria.

## Next Phase Readiness

- Plan 05 (DIAGNOSIS.md) can now cite D06-TRAINING-AUDIT.md for the H1 source-side evidence alongside Plan 02's H5-eliminated finding and Plan 03's D-03/D-05 behavioral evidence.
- Plan 06A (future retrain) has a CI-visible safety net: any retrain whose curated training set exceeds the three ceilings (pct_end_in_tool_call >= 0.10, top-5 suffix coverage > 0.50, tool-calling fraction > 0.92) will fail the pytest gates loudly.

## Self-Check: PASSED

- `tests/test_phase_09_2/test_training_audit.py` exists, 102 lines, contains four named test functions and three literal float thresholds (0.10, 0.50, 0.92).
- `.planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md` exists, 84 lines, contains all six required section headers and the two loss-value citations.
- Commit `76c9c0c` (Task 1 test) and commit `6d05295` (Task 2 docs) verified via `git log --oneline -5`.
- `pytest tests/test_phase_09_2/` reports `5 passed, 1 xpassed` as expected by plan success criteria.

---
*Phase: 09.2-tool-call-regression-diagnosis*
*Completed: 2026-04-24*
