---
phase: 09.2
slug: tool-call-regression-diagnosis
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-23
---

# Phase 09.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing in repo; see tests/) |
| **Config file** | pytest.ini / pyproject.toml (existing) |
| **Quick run command** | `.venv/bin/pytest tests/test_phase_09_2/ -x -q` |
| **Full suite command** | `.venv/bin/pytest tests/ -q` |
| **Estimated runtime** | ~60 seconds for phase-specific tests; longer for full suite |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest tests/test_phase_09_2/ -x -q`
- **After every plan wave:** Run `.venv/bin/pytest tests/ -q`
- **Before `/gsd-verify-work`:** Full suite must be green; base-model eval must reproduce (±0.02 of results/base_custom.json)
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 09.2-01-XX | 01 | 1 | TOOL-01 / EVAL-01 | eval reproduce | `.venv/bin/python -m scripts.eval_inference --model HuggingFaceTB/SmolLM2-1.7B-Instruct --dataset-dir datasets/assembled --output results/base_custom_repro.json --device mps && .venv/bin/python -c "import json,math; a=json.load(open('results/base_custom.json'))['scores']['tool-call-format']; b=json.load(open('results/base_custom_repro.json'))['scores']['tool-call-format']; assert math.isclose(a,b,abs_tol=0.02), (a,b)"` | ⬜ pending |
| 09.2-02-XX | 02 | 1 | TOOL-01 | template parity pytest | `.venv/bin/pytest tests/test_phase_09_2/test_template_parity.py -x -q` | ⬜ pending |
| 09.2-03-XX | 03 | 2 | TOOL-01 | human inspection (categorized table) | `.venv/bin/python scripts/probe_generations.py --model models/lyra-merged --n 10 > results/lyra_v2_probe.json` + human review | ⬜ pending (autonomous=false) |
| 09.2-04-XX | 04 | 2 | TOOL-01 | training data audit pytest | `.venv/bin/pytest tests/test_phase_09_2/test_training_audit.py -x -q` | ⬜ pending |
| 09.2-05-XX | 05 | 3 | TOOL-01 | decision gate (human) | N/A — decision artifact: `.planning/phases/09.2-tool-call-regression-diagnosis/DIAGNOSIS.md` | ⬜ pending (autonomous=false) |
| 09.2-06-XX | 06 | 4 | TOOL-01 / EVAL-01 | end-to-end eval (D-09 thresholds) | `.venv/bin/python -m scripts.eval_compare --baseline results/base_full.json --candidate results/lyra_v3_full.json --assert-min-format 0.41 --assert-max-knowledge-drop 0.05` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: exact task IDs and plan numbers will be finalized by the planner. The mapping above follows the D-03 through D-09 decision structure in CONTEXT.md.*

---

## Wave 0 Requirements

- [ ] `tests/test_phase_09_2/__init__.py` — new test package for this phase
- [ ] `tests/test_phase_09_2/conftest.py` — shared fixtures: canonical tool-call prompt, train-template loader, persisted-template loader
- [ ] `tests/test_phase_09_2/test_template_parity.py` — RED stub asserting `tokenizer.apply_chat_template(fixture, ...)` with train-time template equals tokenization with persisted template (token-ID list equality on 5 fixture prompts)
- [ ] `tests/test_phase_09_2/test_training_audit.py` — RED stub asserting training data properties: (a) distribution of assistant turn types (tool-call vs text), (b) canned-suffix prevalence ≤ threshold
- [ ] `scripts/probe_generations.py` — new standalone diagnostic (created in Wave 2, not test infra)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Categorize v2's raw generations into failure modes | TOOL-01 | Hand inspection of free-text outputs; categories emerge from the data | Run `probe_generations.py` on 10-20 tool-call prompts; visually group outputs into: empty, plain text, malformed XML, wrong function, correct-format-wrong-args, echoed tool-result, mode-collapsed-single-output |
| Decide corrective action (retrain vs revert vs patch) | TOOL-01 | Decision depends on subjective read of evidence; write DIAGNOSIS.md with the chosen path | Read all Wave 1-2 outputs; commit DIAGNOSIS.md with root cause, chosen action, and rationale; downstream retrain plan reads this file |
| Accept/reject a retrain attempt's metrics | EVAL-01 | D-09 thresholds are numeric, but whether to iterate or accept is a human call | Compare new candidate's eval_compare output against D-09 bars; if below, iterate per D-10 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter (set by planner when wave-0 tests are written)

**Approval:** pending (draft written 2026-04-23)
