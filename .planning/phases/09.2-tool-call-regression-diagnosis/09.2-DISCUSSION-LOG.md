# Phase 09.2: Tool-Call Regression Diagnosis - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-23
**Phase:** 09.2-tool-call-regression-diagnosis
**Areas discussed:** Diagnostic scope, Evidence to collect, Eval methodology sanity check, Exit criteria

---

## Diagnostic Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Diagnose only | Phase 09.2 ends with root-cause report + recommended fix strategy; fix applied in a later phase. Keeps phase small (hours, not days). | |
| Diagnose + minimal fix | Smallest fix that restores baseline (e.g., revert model or 1-line patch); stops short of a full retrain. | |
| Diagnose + full fix (may include retrain) | End-to-end: diagnose, patch, retrain if needed, re-evaluate. Similar scope to 09.1. | ✓ |

**User's choice:** Diagnose + full fix
**Notes:** User wants the phase to carry through to a corrective action, not just produce a report. Acknowledged this may involve another 3.5h retrain but that's acceptable given the ambition to close out a working Lyra release.

---

### Follow-up: Fallback acceptability

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — revert is a valid outcome | If Phase 9 checkpoint (0.4065) beats any v2 recovery, accept reverting as the shipping artifact. 09.2 writes the revert plan. | ✓ |
| No — v2 must be recovered | Keep iterating on the 13,292-sample retrain until it matches or beats the pre-09.1 model. Revert off the table. | |

**User's choice:** Revert is a valid outcome
**Notes:** Pragmatic floor — prevents an open-ended loop if the retrain path keeps regressing.

---

## Evidence to Collect

| Option | Description | Selected |
|--------|-------------|----------|
| Inspect raw v2 generations | Read results/lyra_v2_custom.json; categorize failures. | ✓ |
| Compare train vs eval template | Tokenize a sample with train-time template vs persisted template, diff token IDs. | ✓ |
| Fresh inference probe | Run 5-10 tool-call prompts through a standalone Python probe, not the eval harness. | ✓ |
| Loss curve / training log review | Re-inspect epoch 1/2 eval_loss and per-step training loss vs Phase 8 curves. | ✓ |

**User's choice:** All four
**Notes:** Thorough diagnosis was the point of the phase, so all evidence sources are in scope. The planner will sequence them (D-07 first per specifics section of CONTEXT.md).

---

## Eval Methodology Sanity Check

| Option | Description | Selected |
|--------|-------------|----------|
| Re-verify eval first | Re-run eval_inference.py on base model; confirm scores match Phase 9 baselines before drawing any conclusions about the v2 model. Cheap (~10 min). | ✓ |
| Trust the metric | Assume 0.0634 is real and go straight to model-side diagnosis. | |

**User's choice:** Re-verify eval first
**Notes:** Low-cost insurance against chasing a ghost if a Plan-05 eval_inference.py change silently broke the scorer.

---

### Follow-up: Pre-09.1 Lyra checkpoint recovery

| Option | Description | Selected |
|--------|-------------|----------|
| Accept — diagnose from v2 alone | models/lyra-merged was overwritten by Plan 05; work with (v2, base) only. | ✓ |
| Reconstruct old checkpoint | Rebuild pre-09.1 Lyra from pre-scaled dataset + Phase 8 hyperparameters. | |

**User's choice:** Accept — diagnose from v2 alone
**Notes:** Rebuilding old Lyra would consume another retrain slot without proportional diagnostic value.

---

## Exit Criteria

| Option | Description | Selected |
|--------|-------------|----------|
| Metric beats original target (>0.41 and D-10 passes) | Same bar as 09.1 — fine-tuned model must beat base on format AND stay within 5% on knowledge. | ✓ |
| Metric restored to or above base (≥0.41, D-10 within 5%) | Lyra merely matches base; still ship, proves fine-tuning didn't hurt. | |
| Root cause + executed recovery (metric not required) | Done once diagnosed + executed, regardless of final number. | |

**User's choice:** Metric beats original target
**Notes:** Holds the original project ambition — fine-tuning must demonstrably help.

---

### Follow-up: Retrain budget

| Option | Description | Selected |
|--------|-------------|----------|
| 1 additional retrain max | Single attempt, then revert. | |
| 2 retrains max | One retrain + one correction cycle. | |
| Unlimited retrains | Iterate until exit criterion met. | ✓ |

**User's choice:** Unlimited retrains
**Notes:** Paired with D-02 (revert is acceptable fallback) and D-11 (sequential evals) — unlimited doesn't mean reckless; each retrain must be justified by a changed hypothesis.

---

## Claude's Discretion

- Exact structure of the fresh inference probe script (one-off diagnostic)
- Specific hyperparameter changes in any retrain attempt (must flow from evidence)
- Whether to introduce domain rebalancing vs accepting 90.2% tool-calling skew
- Fix sequencing: whether to try an eval-side patch before a retrain when diagnosis is ambiguous

## Deferred Ideas

- MinHash LSH dedup replacement — future phase only if 09.2 evidence implicates dedup as a cause
- Dataset versioning / GGUF / Gradio demo — Phase 10 scope
- Broader eval suite (BFCL, HumanEval+) — out of scope; keep apples-to-apples comparison with Phase 9 / 09.1
