---
phase: 09.2-tool-call-regression-diagnosis
plan: 03
subsystem: diagnostics
tags: [probe, generation, diagnostic, d-03, d-05, human-review, partial, checkpoint-paused]

# Dependency graph
requires:
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 01
    provides: tests/test_phase_09_2/ conftest + D-07 re-baseline artifacts (datasets+venv symlink precedent)
provides:
  - scripts/probe_generations.py (D-03 + D-05 combined diagnostic runner)
  - results/v2_probe_raw.jsonl (20 dataset-mode rows, auto-categorized)
  - results/v2_probe_single_turn.jsonl (8 single-turn rows, auto-categorized)
  - (PENDING at checkpoint): D03-D05-CATEGORIZATION.md + D03-D05-CATEGORIZATION.json
affects: [09.2-05-diagnosis, 09.2-06-decision]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One-off diagnostic script pattern: lazy-imports torch/transformers/datasets inside main() so --help works without ML libs installed (Phase 8 pattern)"
    - "Shared-helper reuse via importlib fallback: prefer `from scripts.eval_inference import _normalize_messages_for_template`, fall back to byte-identical local copy on ImportError"
    - "T-09.2-01 mitigation: validate --model arg against MODEL_PATH_PATTERN regex BEFORE any heavy import (exit code 2 on failure)"
    - "T-09.2-05 mitigation: categorize() uses json.loads in try/except; model output never exec/eval'd"
    - "D-11 sequential chain: two probe modes run &&-chained in a single Bash invocation, never parallel"

key-files:
  created:
    - scripts/probe_generations.py
    - results/v2_probe_raw.jsonl
    - results/v2_probe_single_turn.jsonl
  modified: []

key-decisions:
  - "Auto-categorized all 28 rows with the 7-bucket ladder but human review will re-classify several: e.g. 9/20 dataset-mode 'plain_text' rows are actually variants of the canned 'I've completed the task. ... the operation was successful.' template and may warrant their own 'canned_summary_template' bucket in the human-written histogram"
  - "Single-turn mode scored 8/8 valid_tool_call by the auto-categorizer but qualitatively 6/8 are mode-collapsed onto get_weather(city=Tokyo) regardless of prompt content — the 'valid_tool_call' label is necessary but not sufficient"
  - "Paused at checkpoint per plan Task 3 — human categorization is NOT auto-filled; orchestrator will spawn a continuation agent with the human's categorization output"

patterns-established:
  - "Probe-script argparse template: --model (required, regex-validated) + --mode (dataset|single-turn) + --limit + --output + --max-new-tokens"
  - "Dataset-mode probe loop: iterate test split, skip non-tool-calling, stop after --limit tool-calling matches, use messages[:last_asst_idx] as prompt and last assistant turn as label"
  - "Single-turn probe template: [{role:'system', content:SINGLE_TURN_SYSTEM_PROMPT}, {role:'user', content:probe}] with tool signatures listed in system prompt"

requirements-completed: [TOOL-01]

# Metrics
duration: 14min
completed: 2026-04-24 (partial — awaiting human categorization)
---

# Phase 09.2 Plan 03: D-03 + D-05 Probe Outputs — PARTIAL (checkpoint reached)

**Script committed, two JSONL probe outputs captured on v2 via sequential `&&`-chained run. H1 (training-data mode collapse onto canned summary templates + get_weather(Tokyo)) is empirically confirmed at the signal level. Human-review checkpoint (Task 3) paused — awaiting user to produce `D03-D05-CATEGORIZATION.md` + `.json` so Plan 05 has a stable categorization to synthesize.**

## Performance

- **Duration:** ~14 min (12 min dataset-mode probe + 2 min single-turn probe + script authoring + deviation workarounds)
- **Started:** 2026-04-24T06:19:00Z (Task 1 scaffold)
- **Checkpoint reached:** 2026-04-24T06:33:00Z (Task 3 handed off to human)
- **Tasks completed:** 2 of 3 (Task 3 is a blocking human-verify checkpoint)
- **Files created:** 3 (`probe_generations.py`, two JSONL artifacts)
- **Files modified:** 0

## Accomplishments (Tasks 1 + 2 of 3)

- Authored `scripts/probe_generations.py` (374 lines) implementing Pattern 2 of `09.2-RESEARCH.md` exactly. Reuses `_normalize_messages_for_template` from `scripts.eval_inference` with a byte-identical local fallback; validates `--model` against `MODEL_PATH_PATTERN` before any library import; carries SPDX header and matching docstring threat references (T-09.2-01, T-09.2-05).
- Ran the probe in both modes sequentially via a single `&&`-chained Bash invocation — no parallel model loads at any point (D-11 enforced). First run: dataset mode over `datasets/assembled/test` tool-calling samples (20 of 599). Second run: 8 hand-picked single-turn prompts from the plan's fixed `PROBES` list.
- Wrote `results/v2_probe_raw.jsonl` (20 JSON lines, one per sample) and `results/v2_probe_single_turn.jsonl` (8 JSON lines, one per probe). Each row has the `generated` text and the auto-assigned `category`.

## Task Commits

1. **Task 1: scripts/probe_generations.py** — `c489a4c` (feat)
2. **Task 2: run both probe modes sequentially; commit two JSONL artifacts** — `bc02c83` (feat)
3. **Task 3: human categorization (D03-D05-CATEGORIZATION.md + .json)** — PENDING at checkpoint

## Auto-Categorized Histograms (pre-human-review)

### Dataset-Mode Probe (D-03) — 20 samples

| Category | Count | % of 20 |
|----------|------:|--------:|
| plain_text | 9 | 45.0% |
| echo_tool_result_prefix | 8 | 40.0% |
| echoed_expected_text | 2 | 10.0% |
| valid_tool_call | 1 | 5.0% |

**Observation for human reviewer:** the 9 `plain_text` rows are dominated by variants of the same canned training-suffix template
(`"I've completed the task. ..."`, `"Here's what I found: ..."`, `"Based on the results, the operation was successful."`).
If the reviewer wants a `canned_summary_template` bucket separate from generic plain text, there is strong evidence for it here.

### Single-Turn Probe (D-05) — 8 samples

| Category | Count | % of 8 |
|----------|------:|-------:|
| valid_tool_call | 8 | 100.0% |

**Observation for human reviewer:** every generation is XML-valid, but 6 of 8 collapse onto
`get_weather(city=Tokyo)` regardless of the user prompt (including prompts about AI news, Tokyo time, Paris temperature, and `/etc/hosts`). The other two emit memorized training strings that do not match the user prompt either:
- `Call get_weather for London` → correctly emits `get_weather(location=London)` (only in-distribution match)
- `Run \`ls -la\`` and `Commit my changes with message 'fix regression'` both emit `git_commit({"message":"Fix typo in docs"})`

This is "100% valid format, 0% useful behavior" — the exact pattern predicted by RESEARCH.md H1 (mode collapse onto the most frequent training examples).

## Hypothesis Evidence (preliminary — final adjudication goes in Plan 05 DIAGNOSIS.md)

- **H1 (training-data mode collapse onto canned suffix templates + domain skew) — STRONGLY SUPPORTED.** Both probe modes independently show mode collapse onto a small set of memorized strings. Dataset mode shows collapse onto summary templates; single-turn mode shows collapse onto `get_weather(Tokyo)` and `git_commit("Fix typo in docs")`.
- **H2 (assistant_only_loss mask anomaly) — NEUTRAL.** These probes do not distinguish H2 from H1.
- **H3 (eval methodology artifact) — WEAKENED.** The metric may be flawed, but v2 is qualitatively worse on single-turn — the model emits the same tool call for every prompt. Even with a perfect metric, a mode-collapsed model fails the single-turn D-05 qualitative bar.
- **H4/H5 (MPS numerics / template persistence bug) — NOT SUPPORTED by this plan.** H5 already ruled out by Plan 02; H4 is not testable from the probe outputs alone.

## Files Created/Modified

- `scripts/probe_generations.py` — 374 lines, SPDX header, argparse + `main`/`categorize`/`normalize_messages` exports, threat mitigations inline.
- `results/v2_probe_raw.jsonl` — 20 JSON lines, one per dataset-mode sample. Fields: `idx`, `expected_content` (first 200 chars), `expected_has_tool_calls`, `generated`, `category`.
- `results/v2_probe_single_turn.jsonl` — 8 JSON lines, one per single-turn probe. Fields: `user`, `generated`, `category`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Symlinked `.venv`, `datasets/assembled`, and two large Git-LFS files from main repo into the worktree**
- **Found during:** Task 1 verify (`.venv/bin/python` not present) and dry-run check (`datasets/assembled` and `models/lyra-merged/model.safetensors` not in worktree).
- **Issue:** Fresh GSD parallel-execution worktree only contains committed files. `.venv/` is gitignored; `datasets/**/*.jsonl` and `models/**/*.safetensors` are also gitignored or Git-LFS-tracked and not pulled on worktree creation. The plan's commands reference all four paths and would fail without them.
- **Fix:** Symlinked into the worktree:
  - `./.venv` → `/Users/lakshman/Documents/Lyra/.venv`
  - `./datasets/assembled` → `/Users/lakshman/Documents/Lyra/datasets/assembled`
  - `./models/lyra-merged/model.safetensors` → `/Users/lakshman/Documents/Lyra/models/lyra-merged/model.safetensors`
  - `./models/lyra-merged/tokenizer.json` → `/Users/lakshman/Documents/Lyra/models/lyra-merged/tokenizer.json`
- **Files modified:** None committed. All four targets are gitignored so symlinks do not appear in `git status`.
- **Verification:** `.venv/bin/python -c "import torch, transformers, datasets"` succeeds; `load_from_disk('datasets/assembled')` returns 663 test samples; model loads and generates 256-token responses on MPS.
- **Committed in:** N/A — worktree-only workaround (same pattern Plan 01 used, per `09.2-01-SUMMARY.md` "Decisions Made").

**Total deviations:** 1 environmental Rule-3 fix. Zero semantic deviation — same commands, same sequential chain, same output files as the plan prescribes.

## Issues Encountered

- One minor warning from `transformers==5.5.4`: `torch_dtype is deprecated! Use dtype instead!`. Non-fatal, consistent with `scripts/eval_inference.py` which uses the same kwarg. Not in scope to patch here; will be addressed uniformly when transformers deprecates the kwarg entirely.
- `get_distance(Berlin, ...)` was emitted in row idx=81 but truncated at `max_new_tokens=256` before the closing `</tool_call>`. Auto-categorize still returned `valid_tool_call` because the inner JSON parsed — technically correct per our ladder but a human reviewer may want to classify as `incomplete_tool_call` or as `valid_tool_call (truncated)`. Worth a footnote in the human-written categorization.

## Known Stubs

None. Script is a self-contained one-off; plan explicitly says no unit tests for the probe (per CONTEXT.md "diagnostic one-off" direction).

## Threat Flags

None — no new trust boundaries or surfaces introduced beyond those already declared in the plan's threat model. T-09.2-01 (model-path regex) and T-09.2-05 (json.loads try/except) are actively implemented in the script.

## Checkpoint Status

**Paused at Task 3 (`type=checkpoint:human-verify`, `gate=blocking`).** Two JSONL files are on disk; human must now read every `generated` field alongside its auto-assigned `category`, re-classify as needed, and produce:

- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md`
- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.json`

See the CHECKPOINT message returned to the orchestrator for the full schema.

## User Setup Required

No external auth gate. The artifacts to review are local JSONL files at:
- `results/v2_probe_raw.jsonl`
- `results/v2_probe_single_turn.jsonl`

## Next Plans Status

- **Plan 04 (D-06 training-data audit)** — was already partially executable per Plan 01's note; this plan does not block it.
- **Plan 05 (DIAGNOSIS.md synthesis)** — BLOCKED until this plan's Task 3 is complete. Plan 05 cites both the markdown narrative and the JSON histogram directly.
- **Plan 06 (decision)** — downstream of Plan 05.

## Self-Check: PASSED

- `scripts/probe_generations.py` exists at reported path.
- `results/v2_probe_raw.jsonl` exists with 20 lines (verified with `wc -l`).
- `results/v2_probe_single_turn.jsonl` exists with 8 lines.
- Both commit hashes (`c489a4c`, `bc02c83`) reachable via `git log --oneline`.
- `--help` exits 0 and prints all expected flags.
- MPS probe runs succeeded for both modes; no parallel process launched at any point.

---
*Phase: 09.2-tool-call-regression-diagnosis*
*Plan: 03 (PARTIAL — paused at human-verify checkpoint)*
*Last tasks committed: 2026-04-24T06:33:00Z*
