---
phase: 09.2-tool-call-regression-diagnosis
plan: 03
subsystem: diagnostics
tags: [probe, generation, diagnostic, d-03, d-05, human-review, complete]

# Dependency graph
requires:
  - phase: 09.2-tool-call-regression-diagnosis
    plan: 01
    provides: tests/test_phase_09_2/ conftest + D-07 re-baseline artifacts (datasets+venv symlink precedent)
provides:
  - scripts/probe_generations.py (D-03 + D-05 combined diagnostic runner)
  - results/v2_probe_raw.jsonl (20 dataset-mode rows, auto-categorized)
  - results/v2_probe_single_turn.jsonl (8 single-turn rows, auto-categorized)
  - .planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md (human-reviewed narrative + histograms)
  - .planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.json (fixed-schema machine-readable output for Plan 05)
affects: [09.2-05-diagnosis, 09.2-06-decision]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One-off diagnostic script pattern: lazy-imports torch/transformers/datasets inside main() so --help works without ML libs installed (Phase 8 pattern)"
    - "Shared-helper reuse via importlib fallback: prefer `from scripts.eval_inference import _normalize_messages_for_template`, fall back to byte-identical local copy on ImportError"
    - "T-09.2-01 mitigation: validate --model arg against MODEL_PATH_PATTERN regex BEFORE any heavy import (exit code 2 on failure)"
    - "T-09.2-05 mitigation: categorize() uses json.loads in try/except; model output never exec/eval'd"
    - "D-11 sequential chain: two probe modes run &&-chained in a single Bash invocation, never parallel"
    - "Human-verify checkpoint output pairing: fixed-schema JSON + narrative Markdown, both committed in the same task commit so Plan 05 can cite either without version drift"

key-files:
  created:
    - scripts/probe_generations.py
    - results/v2_probe_raw.jsonl
    - results/v2_probe_single_turn.jsonl
    - .planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md
    - .planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.json
  modified: []

key-decisions:
  - "Human reviewer kept the 7-bucket fixed-schema for JSON but refined the Markdown histograms with behaviorally meaningful sub-buckets: `canned_summary_template` split out of generic `plain_text` (9/20 rows), and `mode_collapsed_get_weather_tokyo` / `mode_collapsed_git_commit_canned` split out of generic `valid_tool_call` on single-turn (7/8 rows). The schema invariants (histogram sums 20/8, `human_confirmed: true`, `mode_collapsed: true`) are preserved for Plan 05 consumption."
  - "Dataset-mode dominant category is `plain_text` (45%, 9/20) — all 9 rows follow the memorized two-slot template `I've completed the task. [CLAUSE] the operation was successful.` This is treated as a learned response-shell reflex, not random text."
  - "Single-turn dominant category is `valid_tool_call` (100%, 8/8) but semantic correctness is 12.5% (1/8). The XML format machinery is intact; the regression is trigger-and-semantic-selection, NOT format emission. Any fix must target training-data distribution or SFT reward signal, not the template/tokenizer layers."
  - "Idx 81 (truncated `get_distance(Berlin,...)` at max_new_tokens=256) re-categorized from auto's `valid_tool_call` to `incomplete_tool_call` per the plan's footnote guidance. This shifts the dataset-mode `valid_tool_call` count from auto's 1 → human's 0 — a useful clarification since v2 emits zero fully-formed tool calls in dataset mode."

patterns-established:
  - "Probe-script argparse template: --model (required, regex-validated) + --mode (dataset|single-turn) + --limit + --output + --max-new-tokens"
  - "Dataset-mode probe loop: iterate test split, skip non-tool-calling, stop after --limit tool-calling matches, use messages[:last_asst_idx] as prompt and last assistant turn as label"
  - "Single-turn probe template: [{role:'system', content:SINGLE_TURN_SYSTEM_PROMPT}, {role:'user', content:probe}] with tool signatures listed in system prompt"
  - "Human categorization output pairing: narrative `.md` with refined behavioral buckets + fixed-schema `.json` with invariant keys (probe_date, model, dataset_mode, single_turn_mode, human_confirmed, feeds_plan_05) — both committed together"

requirements-completed: [TOOL-01]

# Metrics
duration: ~18min (14min Tasks 1+2 + ~4min checkpoint review & finalization)
completed: 2026-04-24
status: complete
---

# Phase 09.2 Plan 03: D-03 + D-05 Probe Outputs — COMPLETE

**v2 mode collapse empirically confirmed. Dataset-mode (20 samples) is dominated by two memorized response shells totaling 85%: `canned_summary_template` (45%) and `echo_tool_result_prefix` (40%). Single-turn mode (8 samples) is 100% syntactically valid `<tool_call>` XML but only 12.5% semantically correct — 7/8 prompts collapse onto `get_weather(city=Tokyo)` (6x) or `git_commit(message="Fix typo in docs")` (2x) regardless of prompt intent. H1 (training-distribution mode collapse) is strongly supported. H5 (template persistence) is already eliminated by Plan 02. Plan 05 DIAGNOSIS.md can now rank H1 / H2 / H3.**

## Performance

- **Duration:** ~18 min (12 min dataset-mode probe + 2 min single-turn probe + script authoring + ~4 min checkpoint review & finalization)
- **Started:** 2026-04-24T06:19:00Z (Task 1 scaffold)
- **Checkpoint reached:** 2026-04-24T06:33:00Z (Task 3 handed off to human)
- **Checkpoint resolved + finalized:** 2026-04-24 (human-produced categorization approved)
- **Tasks completed:** 3 of 3
- **Files created:** 5 (`probe_generations.py`, two JSONL probe artifacts, CATEGORIZATION.md, CATEGORIZATION.json)
- **Files modified:** 0

## Accomplishments

### Task 1: probe_generations.py (commit `c489a4c`)

Authored `scripts/probe_generations.py` (374 lines) implementing Pattern 2 of `09.2-RESEARCH.md` exactly. Reuses `_normalize_messages_for_template` from `scripts.eval_inference` with a byte-identical local fallback; validates `--model` against `MODEL_PATH_PATTERN` before any library import; carries SPDX header and matching docstring threat references (T-09.2-01, T-09.2-05).

### Task 2: two-mode sequential probe run (commit `bc02c83`)

Ran the probe in both modes sequentially via a single `&&`-chained Bash invocation — no parallel model loads at any point (D-11 enforced). First run: dataset mode over `datasets/assembled/test` tool-calling samples (20 of 599). Second run: 8 hand-picked single-turn prompts from the plan's fixed `PROBES` list. Output: `results/v2_probe_raw.jsonl` (20 rows) and `results/v2_probe_single_turn.jsonl` (8 rows).

### Task 3: human categorization (commit `1e73cad`)

Human reviewer read all 28 generations alongside their auto-assigned categories and produced both required artifacts:

- **`D03-D05-CATEGORIZATION.md`** — four-section narrative with refined behavioral histograms. Dataset-Mode Probe section splits `canned_summary_template` out of `plain_text`. Single-Turn Probe section adds a behavioral breakdown exposing the `get_weather(Tokyo)` and `git_commit("Fix typo in docs")` mode-collapse signatures. Qualitative Findings section addresses all four RESEARCH.md review questions. Feeds Plan 05 section provides four numbered findings ready for direct citation in DIAGNOSIS.md.
- **`D03-D05-CATEGORIZATION.json`** — fixed-schema output for Plan 05 programmatic consumption. Histograms sum to 20 (dataset) and 8 (single-turn). `human_confirmed: true`. `single_turn_mode.mode_collapsed: true`. Dominant categories: `plain_text` (dataset-mode, 45%) and `valid_tool_call` (single-turn, 100% syntactically — with a `semantic_correctness_percent: 12.5` companion field to avoid misreading).

## Task Commits

1. **Task 1: scripts/probe_generations.py** — `c489a4c` (feat)
2. **Task 2: run both probe modes sequentially; commit two JSONL artifacts** — `bc02c83` (feat)
3. **Task 3: D-03/D-05 human categorization (Markdown + JSON)** — `1e73cad` (docs)
4. **Partial SUMMARY.md at checkpoint (now superseded by this final version)** — `c05db0d` (docs)

## Final Histograms (human-reviewed)

### Dataset-Mode Probe (D-03) — 20 samples

Refined behavioral histogram (from CATEGORIZATION.md):

| Category | Count | % of 20 |
|----------|------:|--------:|
| canned_summary_template | 9 | 45% |
| echo_tool_result_prefix | 8 | 40% |
| echoed_expected_text | 2 | 10% |
| incomplete_tool_call (truncated) | 1 | 5% |
| valid_tool_call (well-formed) | 0 | 0% |

Every one of the 9 `canned_summary_template` rows matches the two-slot schema `I've completed the task. [CLAUSE] the operation was successful.` with `CLAUSE ∈ {"Here's what I found:", "Based on the results,"}`. That's 45% of dataset-mode generations firing a single memorized template regardless of the underlying tool or domain.

### Single-Turn Probe (D-05) — 8 samples

Fixed-schema histogram: `valid_tool_call: 8/8 (100%)` — the XML format machinery emits syntactically valid `<tool_call>` on every prompt.

Behavioral breakdown:

| Behavior | Count | % of 8 |
|----------|------:|-------:|
| valid_and_semantically_correct | 1 | 12.5% |
| mode_collapsed_get_weather_tokyo | 6 | 75.0% |
| mode_collapsed_git_commit_canned | 2 | 25.0% |

Only 1/8 (`Call get_weather for London`) produces a semantically appropriate call, and even that uses `location` kwarg instead of the schema-defined `city`. Prompts about New York weather, Tokyo time, AI news, Paris temperature, shell `ls -la`, generic git-commit, and `/etc/hosts` all collapse onto two memorized outputs.

## Hypothesis Status After This Plan

This plan refines the preliminary assessments from the partial SUMMARY with human-confirmed evidence. Final adjudication is Plan 05's job; this is the evidence feed.

- **H1 (training-distribution mode collapse) — STRONGLY SUPPORTED.** Two independent behavioral signatures confirm it: (a) 85% of dataset-mode generations fit one of two memorized response shells (`canned_summary_template` 45% + `echo_tool_result_prefix` 40%); (b) 7/8 single-turn generations collapse onto two specific memorized outputs (`get_weather(city=Tokyo)` 6x, `git_commit(message="Fix typo in docs")` 2x). Both shells and both canned outputs are almost certainly over-represented in the training distribution.
- **H2 (assistant_only_loss mask anomaly) — PENDING.** These probes do not distinguish H2 from H1. Plan 04's training-data audit (D-06) will quantify training-side imbalance and mask behavior.
- **H3 (eval methodology artifact) — PENDING but weakened.** The metric may be flawed, but v2 is qualitatively worse on single-turn — it emits the same tool call for every prompt. A perfect metric would not rescue a mode-collapsed model; it would just describe the collapse more faithfully.
- **H4 (MPS numerics) — PENDING.** Not testable from probe outputs alone; stays open for Plan 05 to consider if Plan 04 fails to fully explain the behavior.
- **H5 (template persistence bug) — ELIMINATED.** Plan 02's D-04 template-parity test passed. H5 is off the table for DIAGNOSIS.md.

Plan 05's DIAGNOSIS.md should rank root-cause hypotheses in the order **H1 / H2 / H3** with H4 parked pending Plan 04 and H5 eliminated.

## Files Created/Modified

Created:

- `scripts/probe_generations.py` — 374 lines, SPDX header, argparse + `main`/`categorize`/`normalize_messages` exports, threat mitigations inline.
- `results/v2_probe_raw.jsonl` — 20 JSON lines, one per dataset-mode sample.
- `results/v2_probe_single_turn.jsonl` — 8 JSON lines, one per single-turn probe.
- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md` — four-section narrative with refined histograms.
- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.json` — fixed-schema machine-readable probe summary for Plan 05.

Modified: none.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Symlinked `.venv`, `datasets/assembled`, and two large Git-LFS files from main repo into the worktree**
- **Found during:** Task 1 verify (`.venv/bin/python` not present) and dry-run check (`datasets/assembled` and `models/lyra-merged/model.safetensors` not in worktree).
- **Issue:** Fresh GSD parallel-execution worktree only contains committed files. `.venv/` is gitignored; `datasets/**/*.jsonl` and `models/**/*.safetensors` are also gitignored or Git-LFS-tracked and not pulled on worktree creation. The plan's commands reference all four paths and would fail without them.
- **Fix:** Symlinked into the worktree:
  - `./.venv` → `/Users/lakshman/Documents/Lyra/.venv`
  - `./datasets/assembled` → `/Users/lakshman/Documents/Lyra/datasets/assembled`
  - `./models/lyra-merged/model.safetensors` → `/Users/lakshman/Documents/Lyra/models/lyra-merged/model.safetensors`
  - `./models/lyra-merged/tokenizer.json` → `/Users/lakshman/Documents/Lyra/models/lyra-merged/tokenizer.json`
- **Files modified:** None committed. All four targets are gitignored so symlinks do not appear in `git status`.
- **Verification:** `.venv/bin/python -c "import torch, transformers, datasets"` succeeds; `load_from_disk('datasets/assembled')` returns 663 test samples; model loads and generates 256-token responses on MPS.
- **Committed in:** N/A — worktree-only workaround (same pattern Plan 01 used, per `09.2-01-SUMMARY.md` "Decisions Made").

**2. [Rule 1 — Category refinement] Idx 81 reclassified from auto's `valid_tool_call` to human's `incomplete_tool_call`**
- **Found during:** Task 3 human review.
- **Issue:** Row idx 81 was emitted as `<tool_call>[{"type":"function","function":{"name":"get_distance","arguments":...` and truncated at `max_new_tokens=256` before the closing `</tool_call>`. The auto-categorizer scored it `valid_tool_call` because the inner JSON parsed — technically correct per the ladder but behaviorally misleading because no complete tool call was actually emitted.
- **Fix:** Reclassified under `incomplete_tool_call` in both Markdown (refined bucket) and JSON (fixed-schema key). Net effect: dataset-mode `valid_tool_call` goes from auto's 1 → human's 0, which is the more accurate signal for DIAGNOSIS.md (v2 emits ZERO fully-formed tool calls across 20 dataset-mode samples).
- **Files modified:** `D03-D05-CATEGORIZATION.md`, `D03-D05-CATEGORIZATION.json`.
- **Committed in:** `1e73cad`.

**Total deviations:** 1 environmental Rule-3 fix (in Tasks 1-2) + 1 Rule-1 category refinement (in Task 3). Zero structural or scope deviation — same commands, same sequential chain, same output files and schema keys as the plan prescribes.

## Issues Encountered

- One minor warning from `transformers==5.5.4`: `torch_dtype is deprecated! Use dtype instead!`. Non-fatal, consistent with `scripts/eval_inference.py` which uses the same kwarg. Not in scope to patch here; will be addressed uniformly when transformers deprecates the kwarg entirely.
- Auto-categorizer over-generously labeled idx 81 as `valid_tool_call` despite truncation. Resolved in Task 3 as documented under Deviations #2.

## Known Stubs

None. Script is a self-contained one-off; plan explicitly says no unit tests for the probe (per CONTEXT.md "diagnostic one-off" direction). CATEGORIZATION outputs are complete with no placeholder rows or TODO markers.

## Threat Flags

None — no new trust boundaries or surfaces introduced beyond those already declared in the plan's threat model. T-09.2-01 (model-path regex) and T-09.2-05 (json.loads try/except) are actively implemented in the script. Categorization artifacts are static Markdown + JSON and introduce no new surface.

## Checkpoint Status

**RESOLVED.** Task 3's `checkpoint:human-verify` gate was paused at `c05db0d`, handed to the user, approved, and now closed with commit `1e73cad`. Both CATEGORIZATION artifacts are on disk and committed.

## User Setup Required

None. All artifacts are local files; no external auth or infrastructure needed for downstream plans to consume them.

## Next Plans Status

- **Plan 04 (D-06 training-data audit)** — unblocked; can run in parallel with Plan 05's synthesis or feed into it.
- **Plan 05 (DIAGNOSIS.md synthesis)** — UNBLOCKED. Plan 05 can now cite both `D03-D05-CATEGORIZATION.md` (narrative + refined histograms) and `D03-D05-CATEGORIZATION.json` (fixed-schema histograms for programmatic access) directly. Hypothesis ranking guidance: H1 strongly supported, H5 eliminated, H2/H3 pending Plan 04, H4 parked.
- **Plan 06 (decision)** — downstream of Plan 05; unchanged.

## Self-Check: PASSED

- `scripts/probe_generations.py` exists at reported path.
- `results/v2_probe_raw.jsonl` exists with 20 lines.
- `results/v2_probe_single_turn.jsonl` exists with 8 lines.
- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md` exists with all four required sections (Dataset-Mode Probe, Single-Turn Probe, Qualitative Findings, Feeds Plan 05).
- `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.json` parses; all 6 required keys (`probe_date`, `model`, `dataset_mode`, `single_turn_mode`, `human_confirmed`, `feeds_plan_05`) present; `dataset_mode.histogram` sums to 20; `single_turn_mode.histogram` sums to 8; `human_confirmed: true`; `single_turn_mode.mode_collapsed: true`; dominant categories match (`plain_text` / `valid_tool_call`).
- All four commit hashes (`c489a4c`, `bc02c83`, `c05db0d`, `1e73cad`) reachable via `git log --oneline`.

---
*Phase: 09.2-tool-call-regression-diagnosis*
*Plan: 03 (COMPLETE)*
*Last task committed: 2026-04-24 (Task 3, `1e73cad`)*
