---
phase: 09.2-tool-call-regression-diagnosis
verified: 2026-04-24T06:30:00Z
status: human_needed
score: 10/10 must-haves verified (automated)
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Does 09.2-DIAGNOSIS.md root-cause the regression correctly?"
    expected: "Evidence Summary table + Hypothesis Ranking + Root Cause section support a unique dominant cause (H1 mode collapse) rather than a hand-wave; user should read the DIAGNOSIS in full and confirm the causal chain is compelling."
    why_human: "Root-cause-adequacy is a judgment call. A verifier can confirm the document exists, ranks hypotheses, and picks a branch — but cannot evaluate whether the reasoning actually explains the 0.06 tool-call-format score."
  - test: "Does the 06A retrain (downstream of this phase) actually clear D-09 on the rebalanced data?"
    expected: "tool-call-format > 0.41 AND |Δ MMLU/ARC/HellaSwag| <= 5% vs base. This is the final victory condition stated in DIAGNOSIS.md."
    why_human: "Plan 06A retrain is checkpointed at pre-flight (documented in 09.2-06-SUMMARY.md) and is the next-phase activity per the orchestrator prompt. This phase's scope ends at diagnosis + rebalanced data + audit gate; a full D-09 clearance requires a ~5.5h training+eval cycle that is downstream of the current verification window."
  - test: "Confirm the rebalanced dataset's qualitative content is sane (manual spot-check)"
    expected: "Randomly sample ~20 tool-calling, 5 code, 5 knowledge rows from datasets/assembled/train and confirm each looks like a legitimate, non-degenerate training example (no obvious generation artifacts, prompt leakage, or malformed roles beyond what the validator already enforces)."
    why_human: "Schema validity is automated (validator + audit tests); semantic quality of subagent-generated samples is not. With 5,100 new samples produced in ~85 min by 31 parallel subagents, a small human eyeball pass is prudent before spending 3.5 h retraining on them."
---

# Phase 09.2: Tool-Call Regression Diagnosis — Verification Report

**Phase Goal:** Diagnose the v2 tool-call-format regression (0.41 -> 0.06), produce a root-cause DIAGNOSIS.md, and execute the corrective action — which DIAGNOSIS chose to be a targeted data rework (Plan 07) delivering a rebalanced training corpus that satisfies three locked CI thresholds. Plan 06A retrain is scoped to RUN on this dataset but is downstream (currently checkpointed at pre-flight).
**Verified:** 2026-04-24T06:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | DIAGNOSIS.md exists, identifies root cause, ranks hypotheses, picks corrective branch | VERIFIED | `09.2-DIAGNOSIS.md` (223 lines). Executive Summary names H1 (training-distribution mode collapse) with posterior ~90%. Hypothesis Ranking table covers H1-H5 with prior/posterior/evidence columns. Root Cause section gives a mechanical explanation. Chosen Action: 06A with 06C fallback, user-approved 2026-04-24. Machine-parsable `lr: 5e-5` / `epochs: 1` on lines 188-189. |
| 2 | Training audit threshold `pct_end_in_tool_call >= 0.05` passes on `datasets/assembled/` | VERIFIED | `pytest tests/test_phase_09_2/test_training_audit.py::test_tool_calling_assistant_ending_floor` PASSED. Measured value 0.0937 (from 07-GENERATION-MANIFEST.md). Test source: `tests/test_phase_09_2/test_training_audit.py:40`. |
| 3 | Training audit threshold `top_5_canned_suffix_coverage <= 0.20` passes | VERIFIED | `pytest ...::test_top5_canned_suffix_coverage_is_bounded` PASSED. Measured value 0.1695. Test source: `tests/test_phase_09_2/test_training_audit.py:76`. |
| 4 | Training audit threshold tool-calling domain ratio <= 0.80 passes | VERIFIED | `pytest ...::test_domain_skew_bounds` PASSED. Measured value 0.6529 (4,909 / 7,519 train rows). Test source: `tests/test_phase_09_2/test_training_audit.py:94`. |
| 5 | `scripts/validate_format.py` supports both multi-turn tool-result shape AND single-turn ends-in-tool_call shape | VERIFIED | `scripts/validate_format.py:121-137` — Rule 5 explicitly permits `assistant` with `tool_calls` to be the final message with zero trailing tool-result turns; mid-conversation tool_calls still require matching results. Regression tests `test_final_assistant_tool_call_without_result_passes` + `test_mid_conversation_tool_call_still_needs_result` at `tests/test_format_validator.py:102,131` both PASS. |
| 6 | `07-GENERATION-MANIFEST.md` records per-wave subagent counts, drop rates, and downsample detail | VERIFIED | File exists (162 lines). Documents Wave A (1 subagent / 30 phrases), Wave B (10 subagents / 500 single-turn / 0% drop), Wave C (10 subagents / 3,000 multi-turn / 0% drop), Wave D (10 subagents / 1,600 supplementary / 0% drop). Explicit downsample section: 10,038 legacy canned-ending samples dropped, 4,954 legacy multi-turn retained, 500 new single-turn retained. |
| 7 | `09.2-07-SUMMARY.md` documents handoff and links to Plan 06A | VERIFIED | File exists (144 lines). "Handoff to Plan 06A" section at the tail lists 4 concrete handoff artifacts and cross-references DIAGNOSIS.md, D06-TRAINING-AUDIT.md, 07-GENERATION-MANIFEST.md, and 09.2-06-PLAN.md. |
| 8 | No orphaned or invalid samples in `datasets/assembled/` (format validator clean) | VERIFIED | All 13 `tests/test_format_validator.py` tests PASS including the 2 new regression tests. Assembled DatasetDict loads cleanly (7,519 train / 419 validation / 416 test). Stratified balance matches 07-GENERATION-MANIFEST.md (tool-calling 65.3%, knowledge 17.4%, code 17.3% in train). |
| 9 | Requirement TOOL-01 satisfied | VERIFIED | Dataset includes structured JSON function calling samples in OpenAI-compatible format: 500 single-turn tool-call-ending samples (Wave B) + 3,000 multi-turn tool-call samples (Wave C) + 4,954 legacy non-canned multi-turn samples = 8,454 tool-calling samples across splits. Schema validation passes. Claimed in `09.2-07-SUMMARY.md` `requirements-completed: [TOOL-01]` and `09.2-05-SUMMARY.md` `requirements-completed: [TOOL-01, EVAL-01]`. |
| 10 | Requirement EVAL-01 addressed (diagnosis-only, not final eval) | VERIFIED | EVAL-01 ("Model evaluated on standard benchmarks with pass@1 scores") is the REASON this phase exists — the v2 eval produced tool-call-format 0.0634, which this phase diagnosed. The phase's EVAL-01 contribution is the diagnosis + rebalanced data + CI gates that unblock 06A; the final re-evaluation post-06A is downstream. Evidence: D07-REPRODUCIBILITY.md (eval harness re-baselined), D03-D05-CATEGORIZATION.md (inference probe), D06-TRAINING-AUDIT.md (training-side source numbers), DIAGNOSIS.md root-cause synthesis. Claimed in `09.2-05-SUMMARY.md` `requirements-completed`. |

**Score:** 10/10 truths verified programmatically.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md` | Root-cause diagnosis, hypothesis ranking, branch recommendation, user sign-off | VERIFIED | 223 lines; status=approved 2026-04-24; chosen action 06A with 06C fallback; machine-parsable hyperparameters present. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/07-GENERATION-MANIFEST.md` | Per-wave subagent counts + drop rates + downsample detail | VERIFIED | 162 lines; 4 wave tables; explicit merge+downsample section. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/07-SUFFIX-POOL.md` | 30-variant ending-phrase pool (Wave A artifact) | VERIFIED | 43 lines. Referenced by Wave C generation spec. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md` | Training-data threshold source numbers | VERIFIED | 84 lines; provides target numbers encoded as CI tests. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md` | Base-model eval re-baseline | VERIFIED | 83 lines; D-07 PASS amended. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md` | Inference-probe mode-collapse evidence | VERIFIED | 98 lines; human-confirmed categorization of 20 multi-turn + 8 single-turn generations. |
| `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-07-SUMMARY.md` | Plan 07 execution summary + handoff to 06A | VERIFIED | 144 lines; contains handoff section and 4 cross-references. |
| `tests/test_phase_09_2/test_training_audit.py` | 4 CI-gated threshold tests | VERIFIED | All 4 tests PASS (pct_end_in_tool_call floor + ceiling, top-5 suffix coverage, domain skew). |
| `tests/test_phase_09_2/test_template_parity.py` | D-04 H5 elimination proof | VERIFIED | All 2 tests PASS. |
| `tests/test_format_validator.py` | Format-validator regression tests including 2 new ones | VERIFIED | All 13 tests PASS including new `test_final_assistant_tool_call_without_result_passes` + `test_mid_conversation_tool_call_still_needs_result`. |
| `datasets/assembled/{train,validation,test}/` | Rebalanced Arrow dataset | VERIFIED | All three splits present, Arrow files dated 2026-04-24 05:39 (post-Plan 07), load cleanly, domain distribution matches 07-SUMMARY. |
| `datasets/tool-calling/v2-single-turn/batch-*.jsonl` | Wave B outputs | VERIFIED | 10 batches present. |
| `datasets/tool-calling/v2-diverse-endings/batch-*.jsonl` | Wave C outputs | VERIFIED | 10 batches present. |
| `datasets/code/v2-supplementary/batch-code-*.jsonl` | Wave D code outputs | VERIFIED | 5 batches present. |
| `datasets/knowledge/v2-supplementary/batch-knowledge-*.jsonl` | Wave D knowledge outputs | VERIFIED | 5 batches present. |
| `scripts/validate_format.py` (modified) | Rule 5 relaxation for final-message tool_call | VERIFIED | Source lines 121-137 implement the relaxation with explicit D-06 comment. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| DIAGNOSIS.md root cause (H1) | Plan 07 data rework scope | `pre-requisite` section lines 194-196 of DIAGNOSIS.md | WIRED | DIAGNOSIS says Plan 06A requires Plan 07; Plan 07 front-matter `requires` points at DIAGNOSIS; Plan 07 SUMMARY handoff section points back to DIAGNOSIS. Three-way link closed. |
| D06-TRAINING-AUDIT.md target numbers | test_training_audit.py thresholds | pytest assertions | WIRED | 3 thresholds from D-06 (0.05 floor / 0.20 coverage / 0.80 domain) are literal numeric assertions in 3 test functions; all pass against `datasets/assembled/`. |
| Plan 07 SUMMARY rebalanced dataset | Plan 06A retrain input | `datasets/assembled/` on disk | WIRED | Assembled Arrow files dated 2026-04-24 05:39, consumed by `scripts/train.py` via `load_from_disk('datasets/assembled')`. |
| Validator Rule 5 relaxation | Wave B generation output acceptance | Regression test `test_final_assistant_tool_call_without_result_passes` | WIRED | Without the Rule 5 relaxation, all 500 Wave B samples would have been dropped as invalid. Test locks the behavior as CI gate. |
| DIAGNOSIS.md hyperparameters | Plan 06A pre-flight grep | `^lr:\s` / `^epochs:\s` | WIRED | Lines 188-189 of DIAGNOSIS.md now contain `lr: 5e-5` and `epochs: 1` in the machine-parsable form Plan 06A Task A1 pre-flight requires. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `datasets/assembled/train` | training rows | `scripts/assemble_dataset.py assemble --seed 42` reading `datasets/tool-calling/curated/tool-calling-curated.jsonl` + `datasets/code/curated/code-curated.jsonl` + `datasets/knowledge/curated/knowledge-curated.jsonl` | YES (7,519 rows, 3 domains, all validator-clean) | FLOWING |
| `test_training_audit.py` assertions | `assembled_train` fixture | `load_from_disk('datasets/assembled')` | YES (rows loaded, domain + message-shape checks run on real data) | FLOWING |
| Wave B / C / D batch JSONL | ShareGPT conversations | Claude Code subagents writing directly to disk | YES (5,100 samples, 0% drop, 421+365 distinct tool names) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Training audit thresholds enforced | `pytest tests/test_phase_09_2/test_training_audit.py -v` | 4 passed in 0.85s | PASS |
| Template parity H5 elimination | `pytest tests/test_phase_09_2/test_template_parity.py -v` | 2 passed in 2.64s (together with audit tests, 6 passed) | PASS |
| Format validator regression tests | `pytest tests/test_format_validator.py -v` | 13 passed in 0.04s including the 2 new tests | PASS |
| Dataset splits load + domain composition | `load_from_disk('datasets/assembled')` + Counter per split | train=7519 {tool-calling:4909, knowledge:1306, code:1304}; validation=419; test=416; domain ratios match 07-SUMMARY.md exactly | PASS |
| DIAGNOSIS.md machine-parsable hyperparameters | `grep -E '^(lr\|epochs):\\s' 09.2-DIAGNOSIS.md` | Returns `188:lr: 5e-5` and `189:epochs: 1` | PASS |
| Git log trail of Plan 07 work | `git log --oneline -15` | Shows 7 consecutive Plan 07 commits (Wave A through Task 5-6 merge through Task 7 SUMMARY) ending at `aeb3c1e docs(09.2-07): Task 7 — SUMMARY + handoff to Plan 06A` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TOOL-01 | 09.2-07-PLAN.md, 09.2-05-SUMMARY.md | Dataset includes structured JSON function calling samples in OpenAI-compatible format | SATISFIED | 4,909 tool-calling train rows, all format-validator-clean; Wave B/C/D generation covered 5 tool-call patterns (single-turn, multi-turn, parallel on some subagents, MCP-style on cloud/devops, CLI on file/git/shell). Audit ends-in-tool_call floor ensures at least ~5% of tool-calling samples are explicit OpenAI-style function calls. |
| EVAL-01 | 09.2-05-SUMMARY.md, ROADMAP phase 09.2 | Model evaluated on standard benchmarks with pass@1 scores (in this phase: v2 diagnosis + 06A unblocking) | SATISFIED (phase-scoped) | v2 eval results (0.0634) drove this phase; D-07 re-baselined the eval harness; D-06 provided training-side evidence; DIAGNOSIS selected corrective action. Final re-evaluation post-06A is the next-phase activity (see Human Verification #2). |

No orphaned requirements identified in REQUIREMENTS.md for phase 09.2 beyond TOOL-01 and EVAL-01.

### Anti-Patterns Found

None in the files modified by Plan 07. Sampled files checked:
- `scripts/validate_format.py` — Rule 5 relaxation carries an explicit D-06 comment, no TODO/FIXME, no stubbed `return None`. `return results` and `sys.exit` flow correctly.
- `tests/test_phase_09_2/test_training_audit.py` — Four production-grade tests with informative assertion messages and real-data fixtures (not mocked).
- `tests/test_format_validator.py` — Two new regression tests both exercise real Conversation.model_validate calls; no skip markers, no xfail.

### Deferred Items

The checkpointed Plan 06A retrain (training run + eval) is explicitly out of scope for this phase per the orchestrator prompt:

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | v3 Lyra model trained on rebalanced dataset | Plan 06A (pending re-dispatch per 09.2-06-SUMMARY.md) | 09.2-06-SUMMARY.md documents the checkpoint + four blockers; ROADMAP phase 09.2 lists 09.2-06-PLAN.md as `Execute chosen branch (06A retrain / 06B patch / 06C revert to base)`. |
| 2 | D-09 gate clearance (tool-call-format > 0.41 and |Δ knowledge| <= 5%) | Plan 06A post-retrain eval | DIAGNOSIS.md "Success gate (enforced by Plan 06A)". This phase's success condition is diagnosis + rebalanced data + audit gate. |

### Human Verification Required

See frontmatter `human_verification` list. Three items (repeated here for readability):

1. **DIAGNOSIS adequacy.** Read `09.2-DIAGNOSIS.md` end-to-end and confirm the Evidence Summary + Hypothesis Ranking + Root Cause trio actually root-causes the regression rather than hand-waving. The document is thorough and self-consistent, but "does the argument convince you" is a judgment call only the developer can make.

2. **Downstream D-09 clearance.** The phase scope ends at diagnosis + rebalanced data + audit gate; the final tool-call-format > 0.41 + |Δknowledge| <= 5% verification requires running Plan 06A (a ~5.5 h training + eval cycle currently checkpointed at pre-flight per 09.2-06-SUMMARY.md). This is expected per the orchestrator prompt but flagged so the developer knows the phase is not fully "done" in the end-to-end product sense.

3. **Qualitative content sanity-check.** 5,100 new samples were produced in ~85 min by 31 parallel Claude Code subagents. Schema correctness is automated; semantic content quality is not. A small eyeball pass on ~20 rows of `datasets/assembled/train` (sampled across the three domains) is prudent before committing ~3.5 h of compute to retrain on this data.

### Gaps Summary

No blocking gaps. Every automated must-have passes: DIAGNOSIS.md exists and is approved, the three audit thresholds are green on `datasets/assembled/`, the validator supports both conversation shapes with regression tests, the generation manifest + summary document the rework, and format validation is clean.

The verification status is `human_needed` (not `passed`) because three non-automatable judgment calls remain — diagnosis adequacy, downstream D-09 clearance, and qualitative sample quality — none of which block the phase's formal completion per the orchestrator-stated scope ("diagnosis + rebalanced data + audit gate, not the final eval").

The pre-existing `tests/test_release_artifacts.py::test_gitattributes_lfs` failure from commit `5b9c69d` is unrelated to this phase and confirmed out of scope per the orchestrator prompt.

---

_Verified: 2026-04-24T06:30:00Z_
_Verifier: Claude (gsd-verifier)_
