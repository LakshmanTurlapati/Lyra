---
phase: 09.2-tool-call-regression-diagnosis
plan: 05
type: execute
wave: 3
depends_on:
  - 09.2-02
  - 09.2-03
  - 09.2-04
files_modified:
  - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md
autonomous: false
requirements:
  - TOOL-01
  - EVAL-01
tags: [diagnosis, decision, synthesis, human-gate]

user_setup: []

must_haves:
  truths:
    - "A single synthesized DIAGNOSIS.md exists consolidating all Wave 1-2 evidence into a root-cause hypothesis ranking and a chosen corrective action"
    - "User has reviewed and approved DIAGNOSIS.md before any retrain or revert is authorized"
    - "DIAGNOSIS.md specifies which of the three Plan 06 branches (A retrain, B targeted patch, C revert to base) executes next, with a cite-by-cite rationale"
  artifacts:
    - path: ".planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md"
      provides: "Consolidated evidence → root cause → chosen action with rationale"
      contains: "## Root Cause"
  key_links:
    - from: ".planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md"
      to: ".planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md"
      via: "Findings citation"
      pattern: "D06-TRAINING-AUDIT"
    - from: ".planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md"
      to: ".planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md"
      via: "Probe findings citation"
      pattern: "D03-D05-CATEGORIZATION"
---

<objective>
Synthesize the Wave 1-2 evidence (Plan 01 D-07 reproducibility, Plan 02 D-04 template parity, Plan 03 D-03/D-05 probe categorization, Plan 04 D-06 training audit) into a single DIAGNOSIS.md document. Identify the root cause, rank hypotheses by supported evidence, pick one of three corrective-action branches (retrain / targeted patch / revert), and document rationale. User signs off before Plan 06 executes.

Purpose: This is the pivot of the phase. Every Wave 1-2 plan produces evidence; every Wave 4 path is expensive (3.5h retrain or a release-artifact revert). The synthesis must be sharp enough that the user can make a confident go/no-go decision. Per D-10, each retrain attempt must cite a changed hypothesis — this document is the starting hypothesis any retrain cites.

Output: `09.2-DIAGNOSIS.md` consolidating all Wave 1-2 evidence, with a user-approved "chosen action" section that Plan 06 branches on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-CONTEXT.md
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-RESEARCH.md
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-01-SUMMARY.md
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-02-SUMMARY.md
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-03-SUMMARY.md
@.planning/phases/09.2-tool-call-regression-diagnosis/09.2-04-SUMMARY.md
@.planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md
@.planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md
@.planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Write 09.2-DIAGNOSIS.md and get user sign-off on chosen action</name>
  <files>.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md</files>
  <read_first>
    - .planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md (was D-07 PASS or FAIL?)
    - .planning/phases/09.2-tool-call-regression-diagnosis/D03-D05-CATEGORIZATION.md (probe findings, user-approved)
    - .planning/phases/09.2-tool-call-regression-diagnosis/D06-TRAINING-AUDIT.md (measured distribution + loss review)
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-RESEARCH.md (Ranked Hypothesis List section — H1-H5)
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-CONTEXT.md (D-01, D-02, D-08, D-09, D-10)
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-02-SUMMARY.md (template ruled out)
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-03-SUMMARY.md (which hypothesis probe evidence supports)
    - .planning/phases/09.2-tool-call-regression-diagnosis/09.2-04-SUMMARY.md (H1 consolidated evidence)
  </read_first>
  <decision>
    **What is being decided:** Which corrective action to execute in Plan 06 — branch 06A (retrain with evidence-justified changes), 06B (targeted patch only — unlikely given evidence), or 06C (revert: ship base SmolLM2-1.7B-Instruct as the v1.0 release artifact per D-02 + D-08).
  </decision>
  <context>
    Per D-01, Phase 09.2 is a full diagnose-and-fix phase — it must carry through to corrective action. Per D-02, reverting to base is a valid shipping outcome. Per D-08, pre-09.1 Lyra is lost, so "revert" means "ship base model." Per D-10, retrains cost ~3.5h wall-clock each and must cite a changed hypothesis. The D-09 success bar is `tool-call-format > 0.41` AND |Δknowledge| < 5%.

    The evidence landscape (from Wave 1-2 artifacts):
    - D-07 status: <PASS/FAIL from D07-REPRODUCIBILITY.md> — if FAIL, eval is suspect and diagnosis is compromised
    - D-04 template parity: <PASS — H5 ruled out per 09.2-02-SUMMARY.md>
    - D-03 + D-05 probe: <which failure mode dominated per D03-D05-CATEGORIZATION.md>
    - D-06 training audit: <the three measured numbers from 09.2-04-SUMMARY.md>

    RESEARCH.md primary recommendation was to REVERT — noting that neither Phase 9 Lyra nor v2 Lyra beat the base on tool-call-format, and D-08 makes the base model the only artifact above 0.41 that is actually on disk.
  </context>
  <action>
    Write `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md` with this exact structure. Populate EVERY section with real values from the Wave 1-2 artifacts — no placeholders.

    ```markdown
    # Phase 09.2 — Diagnosis

    Date: <run date>
    Status: <draft|approved>
    Wave 1-2 complete: <YES/NO>

    ## Evidence Summary

    | Source | Finding | Status |
    |--------|---------|--------|
    | D-07 (base re-baseline, Plan 01) | <PASS|FAIL — cite deltas from D07-REPRODUCIBILITY.md> | <PASS/FAIL> |
    | D-04 (template parity, Plan 02) | Train-template (stripped) == persisted template; identical token IDs on fixture prompt | PASS (H5 ruled out) |
    | D-03 (dataset probe, Plan 03) | <category histogram dominant mode from D03-D05-CATEGORIZATION.md> | <observation> |
    | D-05 (single-turn probe, Plan 03) | <single-turn behavior from D03-D05-CATEGORIZATION.md> | <observation> |
    | D-06 (training audit, Plan 04) | 0% tool-call-ending train samples; ~46% top-5 canned-suffix coverage; 90.2% domain skew | confirmed |

    ## Hypothesis Ranking (post-evidence)

    Starting from RESEARCH.md pre-research ranking, updated with Plan 02/03/04 evidence:

    | Hypothesis | Prior P | Posterior P | Supporting Evidence | Status |
    |------------|---------|-------------|---------------------|--------|
    | H1: Mode collapse from suffix memorization + domain skew | ~70% | <measured>% | D-06 shows 0%/46%/90.2%; D-03/D-05 probe shows <mode collapse> | <confirmed/partial/refuted> |
    | H2: assistant_only_loss mask interaction | ~15% | <measured>% | <cite any new evidence; likely unchanged> | <...> |
    | H3: Eval methodology artifact | ~10% | <measured>% | <if D-07 passed, eval is not the root cause; cite> | <...> |
    | H4: MPS merge-time numerics drift | ~4% | <measured>% | <probably unchanged unless D-05 probe hinted> | <...> |
    | H5: Chat template persistence bug | ~1% | 0% | D-04 token-ID parity passed | REFUTED |

    ## Root Cause

    <1-3 paragraphs naming the root cause, citing specific numbers from the evidence table. Expected based on RESEARCH.md: "v2 suffers from mode collapse driven by H1 — training-data suffix memorization combined with 90.2% tool-calling domain skew. Neither the template nor the eval methodology explains the 0.0634 score." If evidence points elsewhere, state so explicitly and cite.>

    ## Chosen Action

    Select exactly ONE branch and write it in the heading below. Populate the justification with cite-by-cite rationale.

    ### Branch 06A — Retrain with rebalanced data + adjusted hyperparameters
    **When to pick:** H1 is confirmed AND the user accepts another ~3.5h retrain cycle AND the data generation pipeline can produce less-canned tool-calling samples (expanded suffix pool + some tool-call-only-ending samples).
    **Changed hypothesis for D-10 compliance:** "The previous retrain memorized 5 canned suffixes. This retrain rebalances domains (oversample code/knowledge to reduce skew below 0.92 → ~0.70 if data permits) and expands the ending-phrase pool to drive top-5 coverage below 0.20."
    **Hyperparameter menu (pick specific values in Plan 06A):**
      - lr: 5e-5 (down from 1e-4, per RESEARCH.md Assumption A1 — still needs validation)
      - epochs: 1 (down from 2, limit overfitting surface)
      - Add N single-turn tool-call-ending samples to training (target ≥500)
    **Risk:** 3.5h wall-clock; may produce a slightly-less-collapsed model that still fails D-09.

    ### Branch 06B — Targeted patch (unlikely)
    **When to pick:** Evidence points at a single-point bug (not mode collapse); for example, if D-05 probe showed v2 emits `<tool_call>` correctly but a post-processing step strips it. RESEARCH.md assigns this ~low probability.
    **Required evidence:** Must cite a specific file + line + transformation that, if fixed, would recover > 0.20 tool-call-format without any retrain.

    ### Branch 06C — Revert (ship base SmolLM2-1.7B-Instruct as v1.0)
    **When to pick:** H1 is confirmed AND any of:
      (a) user judges 3.5h retrain cost not worth the risk, or
      (b) probability of retrain success < 50% in the user's estimation, or
      (c) data-pipeline rework cost exceeds revert cost.
    **Per D-08:** "Revert to Phase 9 checkpoint" means SHIP THE BASE MODEL, because the pre-09.1 Lyra was overwritten. This is not a code-level revert; it's a release-artifact decision.
    **Scope of Plan 06C:** update README.md, MODEL_CARD.md, DATASET_CARD.md, BENCHMARK.md to point at base model as v1.0 release; add a "why" note citing this DIAGNOSIS.md; keep `models/lyra-adapter/` published as an experimental artifact but not primary.
    **Risk:** low (documentation + tagging); D-09 bar is satisfied by the base model's 0.4065 score on tool-call-format plus its pre-existing knowledge metrics (by definition |Δknowledge| = 0 since base IS base).

    ## Rationale

    <3-5 bullet points explaining why the chosen branch was picked, citing specific evidence values. Must reference at least two of: D07-REPRODUCIBILITY.md, D03-D05-CATEGORIZATION.md, D06-TRAINING-AUDIT.md.>

    ## Sign-off

    - [ ] User reviewed the Evidence Summary table
    - [ ] User reviewed the Hypothesis Ranking table
    - [ ] User approves the chosen action (circle one: 06A / 06B / 06C)
    - [ ] If 06A: user approves the specific hyperparameter changes
    - [ ] Date approved: ____________

    *This document is the input to Plan 06. Plan 06 MUST branch based on the approved section above.*
    ```

    Fill in every `<placeholder>` with the real values from the Wave 1-2 SUMMARYs. Do NOT commit with placeholders remaining.

    Once written, present the document to the user with an explicit summary message. Example:

    > I have synthesized Wave 1-2 evidence into `09.2-DIAGNOSIS.md`. Key findings:
    > - D-07 eval reproducibility: <PASS|FAIL>
    > - D-04 template parity: PASS — H5 ruled out
    > - D-03/D-05 probe: <dominant mode>
    > - D-06 training audit: <3 key numbers>
    > - Recommended action: <06A|06B|06C> because <one-sentence reason>
    >
    > Please review the DIAGNOSIS.md and approve one of the three branches. Reply with "approve 06A" / "approve 06B" / "approve 06C" or specify concerns.
  </action>
  <options>
    <option id="branch-06A">
      <name>06A — Retrain with rebalanced data + adjusted hyperparameters</name>
      <pros>Could actually clear the D-09 bar with a Lyra-branded artifact; preserves the investment in the fine-tuning pipeline.</pros>
      <cons>3.5h wall-clock cost; RESEARCH.md rates retrain-success probability below revert; requires new data generation to expand ending-phrase pool; MMLU may not recover.</cons>
    </option>
    <option id="branch-06B">
      <name>06B — Targeted patch (no retrain)</name>
      <pros>Fastest path to ship if a specific fix exists.</pros>
      <cons>Probability this branch applies is low per RESEARCH.md — evidence points at distribution, not a localized bug. Not a viable path unless specific single-point evidence emerges.</cons>
    </option>
    <option id="branch-06C">
      <name>06C — Revert: ship base SmolLM2-1.7B-Instruct as v1.0</name>
      <pros>Lowest risk; D-09 automatically satisfied (base IS the reference); per D-02 this is an explicitly allowed exit; RESEARCH.md primary recommendation.</pros>
      <cons>No Lyra-branded wins to publish; requires rewriting model/dataset cards; no improvement over base to demonstrate. User memory: project value is a fine-tuned model — revert is a retreat.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-DIAGNOSIS.md` exists.
    - File contains all 7 required sections: Evidence Summary, Hypothesis Ranking, Root Cause, Chosen Action, (three branch sub-sections), Rationale, Sign-off.
    - Every placeholder `<...>` in the template has been replaced with concrete evidence or citation.
    - The Root Cause section names a specific hypothesis from H1-H5.
    - The Chosen Action section has exactly ONE selected branch (the other two are either struck out, marked "not selected," or moved into a "considered alternatives" footer).
    - User has responded with "approve 06A" / "approve 06B" / "approve 06C" (or equivalent explicit approval).
    - Rationale cites at least TWO of the three evidence documents (D07-REPRODUCIBILITY.md, D03-D05-CATEGORIZATION.md, D06-TRAINING-AUDIT.md).
  </acceptance_criteria>
  <resume-signal>Reply with "approve 06A", "approve 06B", "approve 06C", or describe required changes to the diagnosis.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User approval signal → Plan 06 branch dispatch | Plan 06 keys off the Chosen Action section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-07 | Repudiation | User approval recorded only verbally | mitigate | Sign-off checklist inside DIAGNOSIS.md requires user to explicitly check boxes + enter date. Plan 06 MUST read this file and refuse to proceed unless at least one branch is explicitly approved. |
</threat_model>

<verification>
- Automated verification intentionally omitted (no `<automated>` command) — this is a pure decision/documentation plan with autonomous=false, per Nyquist exception for human-review tasks documented in `<validation_architecture>` of planning_context.
- Sign-off is verified by the presence of an explicit approval string from the user in chat + checked boxes in the DIAGNOSIS.md file.
- No model loaded — D-11 N/A.
</verification>

<success_criteria>
- 09.2-DIAGNOSIS.md exists with all sections populated from real evidence.
- User has approved exactly one of the three branches.
- Plan 06 knows which branch to execute (the answer is on disk, not just in memory).
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-tool-call-regression-diagnosis/09.2-05-SUMMARY.md`. Include:
- The approved branch (06A / 06B / 06C)
- The root cause named in DIAGNOSIS.md
- Posterior probabilities for H1-H5 (from DIAGNOSIS.md)
- Timestamp of user approval
- Explicit next step: "Plan 06 executes branch <X>"
</output>
