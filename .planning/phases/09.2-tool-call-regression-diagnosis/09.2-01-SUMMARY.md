---
phase: 09.2-tool-call-regression-diagnosis
plan: 01
subsystem: eval
tags: [eval, baseline, sanity-check, pytest, mps, lm-eval, smollm2]

# Dependency graph
requires:
  - phase: 09-benchmarking-and-core-release
    provides: results/base_custom.json + results/base_knowledge.json (canonical baselines)
provides:
  - tests/test_phase_09_2/ package scaffolded with 3 fixtures + 2 RED stubs
  - results/base_custom_repro.json (re-baseline for D-07 gate)
  - results/base_knowledge_repro.json (re-baseline for D-07 gate)
  - .planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md (FAIL status)
affects: [09.2-02-template-parity, 09.2-04-training-audit, 09.2-06-decision, any future eval work]

# Tech tracking
tech-stack:
  added: [pytest 9.0.3 (already installed in .venv)]
  patterns:
    - "D-07 reproducibility gate: `abs(new - old) < 0.02` per-benchmark on re-baseline before downstream diagnosis"
    - "Wave 0 test scaffolding: RED stubs with @pytest.mark.skip + descriptive reason pointing at future plan"
    - "Shared fixtures loaded via conftest.py with pytest.skip fallback for missing Git LFS artifacts"

key-files:
  created:
    - tests/test_phase_09_2/__init__.py
    - tests/test_phase_09_2/conftest.py
    - tests/test_phase_09_2/test_template_parity.py
    - tests/test_phase_09_2/test_training_audit.py
    - results/base_custom_repro.json
    - results/base_knowledge_repro.json
    - .planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md
  modified: []

key-decisions:
  - "Symlinked /Users/lakshman/Documents/Lyra/.venv into worktree so .venv/bin/pip path in plan works (ephemeral workaround, not committed; .venv/ is gitignored)"
  - "Symlinked datasets/assembled into worktree (gitignored as large binary) so eval_inference could load the test split"
  - "Declared D-07 gate FAIL with only tool-call-format drifting (>0.02); knowledge reproduces exactly so drift is isolated to the custom eval path"
  - "Did NOT halt execution inside this plan — all three plan tasks committed, escalation is documented in D07-REPRODUCIBILITY.md for the orchestrator/user to act on"

patterns-established:
  - "Test RED stub pattern: @pytest.mark.skip with reason mentioning the plan number that will turn it GREEN"
  - "Per-benchmark reproducibility assertion with 0.02 threshold and abort-on-fail"

requirements-completed: [TOOL-01, EVAL-01]

# Metrics
duration: 118min
completed: 2026-04-24
---

# Phase 09.2 Plan 01: Wave 0 Scaffolding + D-07 Re-baseline Summary

**D-07 FAIL — tool-call-format drifted -0.0643 (0.4065 → 0.3422) vs Phase 9 baseline; knowledge benchmarks reproduce bit-exactly; Wave 0 pytest package + 2 RED stubs + 3 fixtures scaffolded and collect cleanly.**

## Performance

- **Duration:** ~118 min (almost entirely in the two sequential model evals)
- **Started:** 2026-04-24T02:02:00Z (Task 1 scaffold)
- **Completed:** 2026-04-24T04:03:00Z (D07 gate committed)
- **Tasks:** 3 of 3 executed and committed
- **Files created:** 7 (4 test files, 2 result JSONs, 1 D07 report)
- **Files modified:** 0

## Accomplishments

- Installed & verified pytest 9.0.3 is available in the project `.venv`, collection-tested the new test package (2 tests collected, both skipped).
- Re-ran the base SmolLM2-1.7B-Instruct custom + knowledge evals sequentially on MPS against the pinned `31b70e2e...` HuggingFace snapshot. 663-sample custom eval in ~20 min; 23,600-request knowledge eval (MMLU + ARC-Challenge + HellaSwag at `--limit 100`) in ~39 min. No parallel model loads at any point.
- Computed per-benchmark deltas and found `tool-call-format` drifted 3x the D-07 threshold while every other benchmark reproduced within threshold (knowledge exactly, code-syntax within one sample). Documented the gate FAIL with interpretation, commands, and a three-option escalation to the user in `D07-REPRODUCIBILITY.md`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold tests/test_phase_09_2/** — `77f0012` (test)
2. **Task 2: D-07 re-baseline custom + knowledge evals** — `f75f150` (feat)
3. **Task 3: D-07 reproducibility gate + D07-REPRODUCIBILITY.md** — `37c5f46` (docs)

## Files Created/Modified

- `tests/test_phase_09_2/__init__.py` — Empty package marker with SPDX header.
- `tests/test_phase_09_2/conftest.py` — 3 fixtures: `train_template_string` (verbatim copy of scripts/train.py lines 436-456 with `{% generation %}` markers intact), `persisted_template_string` (loads models/lyra-merged/tokenizer_config.json, pytest.skip if absent per T-09.2-03 threat mitigation), `canonical_tool_call_prompt` (standard [system,user] weather query).
- `tests/test_phase_09_2/test_template_parity.py` — RED stub for D-04 template parity proof; `@pytest.mark.skip(reason="RED stub: filled in by Plan 02")`.
- `tests/test_phase_09_2/test_training_audit.py` — RED stub for D-06 training-data distribution audit; `@pytest.mark.skip(reason="RED stub: filled in by Plan 04")`.
- `results/base_custom_repro.json` — Re-baseline of `scripts.eval_inference` on base SmolLM2-1.7B. `tool-call-format=0.3422`, `code-syntax=0.1875`.
- `results/base_knowledge_repro.json` — Re-baseline of `scripts.eval_runner --benchmarks knowledge --limit 100`. `mmlu=0.5012`, `arc_challenge=0.45`, `hellaswag=0.66` (all bit-identical to canonical).
- `.planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md` — Gate record with `Exit status: FAIL`, per-benchmark delta table, commands used, interpretation, and escalation message.

## Decisions Made

- Flagged D-07 FAIL cleanly via the numeric script rather than rerunning or re-calibrating. The 0.0643 drift on tool-call-format is too big to hand-wave and the knowledge deltas being exactly 0.0000 confirm the problem is localized to the custom-eval inference path, not the environment or weights in general. This maximizes the information the downstream user has to choose a recovery path.
- Wrote substantive context into D07-REPRODUCIBILITY.md rather than a terse pass/fail — three explicit remediation options (investigate drift / adopt new baseline / git-revert environment) give the user decision leverage without burying them in details.

## Results Table

| Benchmark | Old (canonical) | New (repro) | Delta | Status |
|-----------|-----------------|-------------|-------|--------|
| custom.code-syntax | 0.2000 | 0.1875 | 0.0125 | PASS |
| custom.tool-call-format | 0.4065 | 0.3422 | 0.0643 | FAIL |
| knowledge.arc_challenge | 0.4500 | 0.4500 | 0.0000 | PASS |
| knowledge.hellaswag | 0.6600 | 0.6600 | 0.0000 | PASS |
| knowledge.mmlu | 0.5012 | 0.5012 | 0.0000 | PASS |

**D-07 Gate: FAIL** (tool-call-format delta 0.0643 > 0.02 threshold).

## Commands Executed

```bash
# Task 1 verification (both exit 0)
.venv/bin/pytest tests/test_phase_09_2/ --collect-only -q   # 2 tests collected
.venv/bin/pytest tests/test_phase_09_2/ -q                   # 2 skipped

# Task 2 (single &&-chained invocation, D-11 compliant)
PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/python -m scripts.eval_inference \
    --model HuggingFaceTB/SmolLM2-1.7B-Instruct \
    --dataset-dir datasets/assembled \
    --output results/base_custom_repro.json \
  && PYTORCH_ENABLE_MPS_FALLBACK=1 .venv/bin/python -m scripts.eval_runner \
       --model HuggingFaceTB/SmolLM2-1.7B-Instruct \
       --benchmarks knowledge \
       --output results/base_knowledge_repro.json \
       --limit 100
# Exit: 0 (both), wall-clock: ~59 min end-to-end

# Task 3 gate script (inline Python, exit-1 on any FAIL)
.venv/bin/python - <<PY
# ... flatten + compare deltas ...
PY
# Exit: 1 (tool-call-format drifted); written to D07-REPRODUCIBILITY.md
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Symlinked .venv and datasets/assembled into the worktree**
- **Found during:** Task 1 (pytest install check) / Task 2 (eval_inference dataset load)
- **Issue:** The GSD parallel-worktree was a fresh checkout without the project `.venv/` (gitignored) or `datasets/assembled/` (gitignored large binary). The plan's commands reference `.venv/bin/pip` and `datasets/assembled` directly, so they would fail in the worktree.
- **Fix:** Created symlinks `./.venv -> /Users/lakshman/Documents/Lyra/.venv` and `./datasets/assembled -> /Users/lakshman/Documents/Lyra/datasets/assembled`. Both paths are gitignored so the symlinks are not committed.
- **Files modified:** None committed. Symlinks are worktree-local state only.
- **Verification:** `.venv/bin/python --version` returns Python 3.14.3 and `from datasets import load_from_disk; load_from_disk('datasets/assembled')` succeeds with 663 test samples.
- **Committed in:** N/A — worktree-only workaround; not in git.

**2. [Rule 2 - Missing critical pre-check] Plan's 15-minute knowledge eval estimate was off by ~2.5x**
- **Found during:** Task 2 (while waiting on eval_runner)
- **Issue:** The plan estimated ~15 min for `eval_runner --benchmarks knowledge --limit 100`, but lm-eval-harness applies `--limit 100` *per subtask*. MMLU has 57 subjects × 100 samples × 4 answer choices = 22,800 loglikelihood requests. Plus ARC-Challenge and HellaSwag. Total 23,600 requests at ~6-10 it/s on MPS = ~39 min, not 15.
- **Fix:** Waited for completion — no corrective action needed beyond calibrating the ETA. The `--limit 100` behavior is correct per D-12.
- **Files modified:** None (documentation of actual runtime added to D07-REPRODUCIBILITY.md).
- **Verification:** Eval completed cleanly at 04:02:19Z with exit 0.
- **Committed in:** Documented in D07 report committed at `37c5f46`.

---

**Total deviations:** 2 environment/runtime issues auto-handled (1 Rule 3 blocking + 1 Rule 2 estimate calibration). **Impact on plan:** Zero semantic deviation from the plan's contract — same commands, same sequential chain, same output files. The symlink is invisible to git; the 59-min total runtime is 19 min over plan estimate but well within the 3h bash timeout.

## Issues Encountered

- **D-07 tool-call-format drift (0.0643).** This is not a deviation — it is the literal outcome the gate was designed to catch. The plan's Task 3 prescribes halting Plans 02-04 if this happens, which is now documented in D07-REPRODUCIBILITY.md. Three remediation options are laid out for the user; none are auto-resolvable.
- The knowledge eval runtime fooled my initial ETA: lm-eval's `--limit` is per-task not global, and MMLU's 57 subjects × 4-choice loglikelihood structure makes it much heavier than the plan's "~15 min" estimate implied. Final wall-clock was ~39 min for knowledge + ~20 min for custom = ~59 min total. Not a bug, just a documentation lesson for future phases.
- The pytest-9.x warning `PytestRemovedIn10Warning: The @pytest.mark.skip marker was called with positional argument` was observed — irrelevant to this plan's behavior (our markers use keyword `reason=`).

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. D-07 eval simply re-runs existing `scripts/eval_inference.py` and `scripts/eval_runner.py` on public HuggingFace weights (anonymous read, `T-09.2-02` accepted disposition).

## Known Stubs

The two RED test stubs in `tests/test_phase_09_2/test_template_parity.py` and `tests/test_phase_09_2/test_training_audit.py` are **intentional** per plan Task 1. Each is explicitly gated by `@pytest.mark.skip(reason="RED stub: filled in by Plan 02|04")` and will be turned GREEN by its named downstream plan. These are not implementation gaps — they are scaffolding for Plans 02 and 04 to extend.

## User Setup Required

**External auth gate:** None. Model pulled anonymously from HuggingFace Hub (T-09.2-02 accepted disposition). `HF_TOKEN` is optional for rate-limit headroom but never needed for this plan.

**D-07 FAIL escalation required.** Before Plans 02-06 proceed, the user must decide between:

1. **Investigate the eval drift.** Pin torch/transformers/tokenizers versions, regenerate `results/base_custom.json` from the fresh run, or bisect git history to find the drift source.
2. **Adopt 0.3422 as the new baseline.** Renegotiate the D-09 exit bar in CONTEXT.md (it currently pins `tool-call-format > 0.41`; if the environment's true base is 0.34, that bar may be unreachable regardless of model quality).
3. **Git-revert the environment.** Check out the Phase 9 commit and re-run to confirm 0.4065 reproduces there — if yes, something in Phase 09.1's dependency changes caused the drift.

See `.planning/phases/09.2-tool-call-regression-diagnosis/D07-REPRODUCIBILITY.md` for the full escalation message.

## Next Plans Status

**BLOCKED pending user decision on D-07 gate failure.**

- Plan 02 (template-parity test, D-04) — **BLOCKED**: would turn the RED stub in `test_template_parity.py` GREEN, but any template-diagnosis conclusions are invalid while the eval itself is drifting.
- Plan 03 (raw-generation inspection, D-03) — **BLOCKED**: reads `results/lyra_v2_custom.json` and categorizes v2 failures vs an assumed-stable base, which is exactly the assumption D-07 just invalidated.
- Plan 04 (training-data audit, D-06) — **POTENTIALLY UNBLOCKED**: purely operates on `datasets/assembled/train` without touching the model or eval path, so in principle it can run. However, its conclusions feed into the retrain-vs-revert decision in Plan 06, which must wait for D-07 resolution.
- Plan 05 (fresh-inference probe, D-05) — **BLOCKED**: one-off probe against models/lyra-merged; probe outputs interpretable only relative to a stable base-model benchmark.
- Plan 06 (decision: retrain / eval-patch / revert, D-02/D-10) — **BLOCKED**: three of the three branches depend on a trustworthy base-model baseline to compare against.

**Wave 0 scaffolding (tests/test_phase_09_2/) IS unblocked.** Downstream plans can add implementations to the RED stubs whenever the phase re-starts.

## Self-Check: PASSED

- All 8 claimed files exist on disk (4 test files, 2 result JSONs, D07 report, this SUMMARY).
- All 3 task commits (77f0012, f75f150, 37c5f46) reachable via `git log --oneline --all`.
- D07-REPRODUCIBILITY.md contains both `Exit status: FAIL` and the full per-benchmark delta table.
- `tests/test_phase_09_2/` collects 2 tests, both skip cleanly (0 failures).

---
*Phase: 09.2-tool-call-regression-diagnosis*
*Plan: 01*
*Completed: 2026-04-24*
