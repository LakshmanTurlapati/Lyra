---
context: phase
phase: 09.3-dataset-scale-and-retrain
plan: 3
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-05-13T05:22:24Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No parallel model evaluations — parallel evals crashed a 64GB machine; always run sequentially. (From memory; reconfirmed this session — every eval was run as a single background bash.)

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-pivot on FAIL verdict | D-14 in 09.3-CONTEXT.md is explicit: a FAIL D-09 verdict does NOT trigger automatic v6/06C/accept-partial. User must pick A/B/C. | blocking | Do not run /gsd-execute-phase or any retrain command before user explicitly states the chosen path. |
| Wrong model path in eval CLI | Plan 03 hard-coded `models/lyra-merged` (single path), but user kept v4 and v5 side-by-side (`models/lyra-merged` = v4, `models/lyra-merged-v5` = v5). Running plan-as-written would have eval'd v4 a second time and silently produced "v5" results identical to v4. | blocking | Always use `models/lyra-merged-v5` for v5 eval. Always use `models/lyra-adapter-v5` for v5 adapter operations. |
| Wrong CLI flag in plan | Plan 03 used `--model-path` but the actual `eval_runner.py` / `eval_inference.py` scripts accept `--model`. Plan was written from memory, never run end-to-end. | advisory | Verify flags via `--help` before pasting plan commands. |

<current_state>
Phase 09.3 Plan 03 Task 4 is complete-but-not-committed.

The eval suite has produced all four required artifacts:
- `results/lyra_v5_knowledge.json`  — MMLU 0.4882, ARC 0.46, HellaSwag 0.65
- `results/lyra_v5_custom.json`     — tool-call-format 0.2353, code-syntax 0.5694
- `results/lyra_v5_full.json`       — merged
- `.planning/phases/09.3-dataset-scale-and-retrain/D09-GATE-V5.md`  — verdict written

**Verdict: FAIL-below-parity.** v5 tool-call-format = 0.2353 vs base 0.4065 vs v4 0.4044 — a 17-point catastrophic regression on the exact metric the phase set out to fix. Knowledge held (D-10 ✓). Code improved big (D-13 ✓). But the headline gate failed.

Nothing has been committed yet. Per global commit rules, waiting for explicit user direction before `git add` / `git commit`.

Task 5 (CHECKPOINT 2 — user ship-decision per D-14) is the blocking gate. Three options on the table: A=v6 retrain, B=accept-partial ship of code-strong v5, C=06C revert-to-base.

**Strong recommendation before deciding A/B/C:** run a 10-sample probe of v5 outputs on the 272 tool-calling test samples to see WHAT v5 actually produces. If outputs are valid tool calls in a slightly-wrong format (e.g. dropped `<tool_call>` wrapper, different JSON shape), this is a cheap regex fix or training-data tweak. If outputs are refusals/garbage/text answers, the regression is structural and the decision tree changes.
</current_state>

<completed_work>

Completed Tasks (this session):
- Knowledge eval ran sequentially in background (~30 min on MPS, --limit 100): `results/lyra_v5_knowledge.json`
- Custom eval ran sequentially in background (~9 min on MPS, 416-sample test split): `results/lyra_v5_custom.json`
- Merge step: `results/lyra_v5_full.json`
- Compare-vs-base and compare-vs-v4 tables generated → `/tmp/lyra-v5-compare-vs-base.log`, `/tmp/lyra-v5-compare-vs-v4.log`
- D09-GATE-V5.md verdict file written with all three gate analyses (D-09, D-10, D-13) + ship-decision options

Completed from prior sessions (Plan 03 history):
- Task 1: v4 adapter snapshot (skipped — user kept v4 side-by-side instead of overwriting)
- Task 2: `scripts/train.py --resume-from-adapter` flag (commit 3b6b7bd)
- Task 3: v5 continue-train on 42K new samples at lr=1e-5 × 1 epoch (commit 3b6b7bd produced models/lyra-adapter-v5 and models/lyra-merged-v5)

</completed_work>

<remaining_work>

- Plan 03 Task 4 — COMMIT the eval artifacts + verdict file (`feat(09.3-03): v5 eval results + D09-GATE-V5 verdict`)
- Plan 03 Task 5 — CHECKPOINT 2 — user picks A/B/C ship-decision per D-14
- Plan 03 SUMMARY — write 09.3-03-SUMMARY.md after ship-decision recorded
- Phase 09.3 close-out — update STATE.md when SUMMARY exists
- Plan 02 SUMMARY (orphan) — `.planning/phases/09.3-dataset-scale-and-retrain/09.3-02-SUMMARY.md` is missing despite Plan 02 work being committed. STATE.md still says "1 of 3 plans complete" partially because of this. Worth backfilling at close-out.

</remaining_work>

<decisions_made>

- Used `models/lyra-merged-v5` for eval (not plan-default `models/lyra-merged` which is v4) — user kept both side-by-side
- Used `datasets/assembled` for custom eval test split (not `datasets/assembled-v5`) — apples-to-apples comparison with v4 and base, both of which were eval'd on this 416-sample split
- Knowledge eval ran with `--limit 100` per the plan — same regime as v4
- Did NOT auto-commit per global commit-only-when-asked rule; surfaced FAIL verdict for user direction first
- Did NOT auto-pivot to v6/06C per D-14 — explicitly the user's call

</decisions_made>

<blockers>
- Tool-call regression to 0.2353: ROOT CAUSE UNKNOWN. Three working hypotheses (CONTEXT.md D-14 mentions): (1) 42K data uses a different tool-call format than the 272-sample test split; (2) tool-call signal diluted by code/knowledge mix in 42K; (3) catastrophic forgetting despite lr=1e-5. Hypothesis 1 is fastest to test via a probe.
- D-14 ship-decision blocked on user (by design — no auto-pivot)
</blockers>

## Required Reading (in order)
1. `.planning/phases/09.3-dataset-scale-and-retrain/D09-GATE-V5.md` — the verdict file, complete tables, ship-options
2. `.planning/phases/09.3-dataset-scale-and-retrain/09.3-CONTEXT.md` D-09 / D-10 / D-13 / D-14 — gate definitions and escalation rules
3. `.planning/phases/09.3-dataset-scale-and-retrain/09.3-03-PLAN.md` Task 4 (line 548+) and Task 5 (line 718+) — original eval recipe and checkpoint criteria
4. `results/lyra_v5_full.json` — raw v5 scores
5. `results/lyra_v4_full.json` — v4 reference
6. `/tmp/lyra-v5-compare-vs-base.log`, `/tmp/lyra-v5-compare-vs-v4.log` — formatted delta tables

## Critical Anti-Patterns (do NOT repeat these)
- AUTO-PIVOT ON FAIL: D-14 says FAIL ≠ auto-pivot. Do not start v6 training, 06C revert, or accept-partial ship pipeline without explicit user direction.
- WRONG MODEL PATH: Plan refers to `models/lyra-merged` (single path). Reality: v4 = `models/lyra-merged`, v5 = `models/lyra-merged-v5`. Always specify -v5 suffix for v5 work.
- WRONG CLI FLAG: scripts use `--model` not `--model-path`. Plan-as-written is incorrect.
- PARALLEL EVALS: 64GB machine crashes on parallel evals. Always sequential, always single background bash at a time.

## Infrastructure State
- venv: `.venv/` — torch 2.11.0 (MPS available), lm-eval 0.4.11
- v4 artifacts (preserved): `models/lyra-adapter/`, `models/lyra-merged/`
- v5 artifacts (this phase): `models/lyra-adapter-v5/`, `models/lyra-merged-v5/` — both with chat_template.jinja
- v4 /tmp snapshot at `/tmp/lyra-v4-snapshot/`: DOES NOT EXIST. Not needed because v4 was not overwritten.
- Datasets: `datasets/assembled` (v4 era, 416-sample test split), `datasets/assembled-v5` (50,502 total, 2,524 test) — eval uses the former for fair comparison
- All four eval result JSONs sit in `results/` uncommitted

## Pre-Execution Critique Required
Not applicable — this is a pause between eval and ship-decision, not between design and execution.

<context>
The Phase 09.3 thesis was: "expand the 09.2-07 rebalanced 7,519-sample corpus to ~50K samples via subagent-batched generation, then continue-train v4's LoRA adapter on the 42K NEW samples at lr=1e-5 × 1 epoch to surgically reinforce tool-call format without catastrophic forgetting." The continue-train ran cleanly. The eval came back and the thesis BROKE on its central claim: tool-call-format collapsed by 17 points despite the careful low-lr single-epoch protocol.

The interesting wrinkle: code-syntax IMPROVED by 37 points over base (and 8 over v4), and knowledge held. So the model DID learn something useful from the 42K — just not what the phase was designed for.

When resuming: the most informative single next action is a 10-sample probe, not a v6 plan. Load `models/lyra-merged-v5`, run 10 tool-calling samples from `datasets/assembled` test split, dump v5's raw output alongside v4's expected output. The diff will collapse the hypothesis space:
- If v5 outputs valid `{"name":..., "arguments":...}` JSON but without the `<tool_call>` wrapper, the fix is a chat-template tweak.
- If v5 outputs the wrapper but with the TRL list-format `[{"type":"function",...}]` shape, the format-checker (eval_inference.py:46) should already accept it — check the regex/parse path.
- If v5 outputs prose answers instead of tool calls, the 42K data's tool-call ratio was too low and the model lost the habit — D-14 option A (v6 retrain with corrected mix) becomes the leading path.
- If v5 outputs refusals or garbage, something is deeply wrong with the training run — verify the adapter actually loaded, check for tokenizer drift.

Only after the probe should we engage D-14 A/B/C seriously.
</context>

<next_action>
Start with: read `.planning/phases/09.3-dataset-scale-and-retrain/D09-GATE-V5.md` end-to-end, then run a 10-sample tool-call probe on `models/lyra-merged-v5` against `datasets/assembled` test split (limit to `domain == 'tool-calling'`), dump raw outputs to `/tmp/lyra-v5-toolcall-probe.txt`, and surface the failure-mode pattern. After that, the user picks D-14 ship-decision A/B/C.
</next_action>
