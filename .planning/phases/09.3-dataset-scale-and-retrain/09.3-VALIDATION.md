---
phase: 09.3
slug: dataset-scale-and-retrain
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-24
---

# Phase 09.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Populated by the planner from RESEARCH.md §Validation Architecture; per-task map filled after plan creation.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (existing project convention) |
| **Config file** | `pytest.ini` (existing) |
| **Quick run command** | `pytest tests/test_batch_generation.py tests/test_continue_train.py tests/test_knowledge_pool.py -x -q` |
| **Full suite command** | `pytest -x -q` |
| **Estimated runtime** | ~15 seconds (quick) / ~90 seconds (full — excludes slow-marker integration tests) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_batch_generation.py tests/test_continue_train.py tests/test_knowledge_pool.py -x -q`
- **After every plan wave:** Run `pytest -x -q`
- **Before `/gsd-verify-work`:** Full suite green + Plan 09.3-01's probe batch succeeded before production batches submitted; Plan 09.3-03's `--max-steps 1` smoke succeeded before full retrain
- **Max feedback latency:** 15 seconds (quick) / 90 seconds (full)

---

## Per-Task Verification Map

*Populated by planner when plans are created. Each task maps to a T-09.3-XX threat ref + concrete automated command.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|--------|
| T-09.3-01-01 | 01 | 1 | D-07, TOOL-01 | T-09.3-01, T-09.3-04 | YAML topic pool + generate_knowledge_data migration + Wave 0 test scaffolding | unit | `.venv/bin/pytest tests/test_knowledge_pool.py tests/test_batch_generation.py::test_module_importable tests/test_continue_train.py::test_module_importable tests/test_generate_knowledge_data.py -x -q` | ⬜ pending |
| T-09.3-01-02 | 01 | 1 | D-02, D-03, D-15, TOOL-01 | T-09.3-01, T-09.3-02, T-09.3-04, T-09.3-05 | gen_batch_09_3.py with lazy anthropic import, forecast cap, idempotency ledger, path-safety guards | unit | `.venv/bin/pytest tests/test_batch_generation.py -x -q && .venv/bin/python -m scripts.gen_batch_09_3 forecast --domain tool-call --samples 1000 --dry-run` | ⬜ pending |
| T-09.3-01-03 | 01 | 1 | D-15 | T-09.3-02, T-09.3-05 | [CHECKPOINT 1] Probe batch 10 samples — cache-hit + ShareGPT validation | manual (probe batch output) | `wc -l datasets/tool-calling/raw-09.3/probe-batch.jsonl && cat .planning/phases/09.3-dataset-scale-and-retrain/BATCH-IDS.json` | ⬜ pending |
| T-09.3-01-04 | 01 | 1 | D-02, D-03, D-05, D-15, TOOL-01 | T-09.3-02, T-09.3-05 | [CHECKPOINT 2] Production batches (3×{tool-call,code,knowledge}) user-confirmed per batch | manual (per-batch user gate) | `wc -l datasets/*/raw-09.3/batch-*.jsonl` expected ~42K total | ⬜ pending |
| T-09.3-01-05 | 01 | 1 | D-14, TOOL-01 | T-09.3-03 | [CHECKPOINT 3] 50-sample spot-check review before Plan 02 curation | manual (user review) | Random 50 sample review + canned-suffix audit script | ⬜ pending |
| T-09.3-02-01 | 02 | 2 | D-05, D-06, TOOL-01 | T-09.3-04 | curate_pipeline per-domain + retention audit | integration | `wc -l datasets/*/raw-09.3/curated-*-09.3.jsonl && .venv/bin/python -c "import json; json.load(open('.planning/phases/09.3-dataset-scale-and-retrain/09.3-CURATE-STATS.json'))"` | ⬜ pending |
| T-09.3-02-02 | 02 | 2 | D-05, D-06, TOOL-01 | T-09.3-04 | merge + reassemble + HARD audit gates (tool-call ≤0.80, canned-suffix ≤0.20, end-in-tool-call ≥0.15) | integration | `.venv/bin/pytest tests/test_phase_09_2/test_training_audit.py -x -q && .venv/bin/python -c "import json; a=json.load(open('.planning/phases/09.3-dataset-scale-and-retrain/09.3-AUDIT.json')); assert a['overall']=='PASS'"` | ⬜ pending |
| T-09.3-03-01 | 03 | 3 | D-09, D-16, TOOL-01 | T-09.3-01, T-09.3-04 | v4 snapshot + train.py --resume-from-adapter patch + flip Wave 0 xfails to GREEN | unit + regression | `.venv/bin/pytest tests/test_continue_train.py tests/test_train.py -x -q && .venv/bin/python -m scripts.train --help \| grep -q -- "--resume-from-adapter"` | ⬜ pending |
| T-09.3-03-02 | 03 | 3 | D-09, D-10 | T-09.3-04 | [CHECKPOINT 1] Smoke test --max-steps 1 + user confirms adapter loaded (loss in 0.6-1.1 region) | manual (log inspection) | `grep -q "Continue-training from adapter" /tmp/lyra-v5-smoke.log` | ⬜ pending |
| T-09.3-03-03 | 03 | 3 | D-09, D-10, TOOL-01 | - | Full continue-train on 42K new samples at lr=1e-5 × 1 epoch | integration (background) | `grep -q "Continue-training from adapter: models/lyra-adapter" /tmp/lyra-v5-train.log && test -f models/lyra-adapter/adapter_config.json` | ⬜ pending |
| T-09.3-03-04 | 03 | 3 | D-11, D-12, D-13, EVAL-01 | - | Eval runner + inference + merge + compare + D09-GATE-V5.md verdict | integration | `test -f results/lyra_v5_full.json && grep -qE 'Verdict:\s*(PASS\|FAIL-parity\|FAIL-below-parity)' .planning/phases/09.3-dataset-scale-and-retrain/D09-GATE-V5.md` | ⬜ pending |
| T-09.3-03-05 | 03 | 3 | D-14, EVAL-01, TOOL-01 | - | [CHECKPOINT 2] User ship-decision per D-14 (PASS / FAIL-parity A\|B\|C / FAIL-below-parity diagnose) | manual (user decision) | grep for `Ship Decision` section in D09-GATE-V5.md | ⬜ pending |
| T-09.3-03-06 | 03 | 3 | EVAL-01, TOOL-01 | - | SUMMARY + STATE + ROADMAP updates | docs | `test -f .planning/phases/09.3-dataset-scale-and-retrain/09.3-03-SUMMARY.md && grep -qE '\[x\] 09.3-0[123]-PLAN.md' .planning/ROADMAP.md` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_knowledge_pool.py` — NEW: validates `templates/knowledge-topics-v2.yaml` structure (≥600 topics, each with ≥5 sub-angles, no duplicates, valid domain tags); xfail until Plan 09.3-01 creates the file
- [ ] `tests/test_batch_generation.py` — NEW: pre-submission cost forecast gate (computes projected cost from sample count × avg tokens, asserts under $500/batch); idempotency test (batch fingerprint → BATCH-IDS.json round-trip); dry-run mode test (no API call); xfail for live-API integration tests that only run with `--live-api` marker
- [ ] `tests/test_continue_train.py` — NEW: `--resume-from-adapter` flag parsed correctly; PEFT `PeftModel.from_pretrained` invoked (mocked); SFTTrainer not constructed with `peft_config` when adapter is resumed (mocked); xfail for actual smoke training until Plan 09.3-03 lands
- [ ] `tests/conftest.py` — extend with `anthropic_batch_fixture` (mock batch client for cost-forecast + idempotency tests)

*Wave 0 convention: Plan 09.3-01 lands the batch-generation test scaffolding (mix of GREEN unit + xfail live-API). Plan 09.3-02 lands the knowledge-pool test scaffolding. Plan 09.3-03 lands the continue-train test scaffolding. All xfails turn GREEN as implementations land.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Batch submission pre-confirmation (idempotency + cost check) | D-03, T-09.3-05 | Irreversible $500+ spend — human must confirm each batch_id before API call | Plan 09.3-01 Task 3 surfaces cost forecast + prior-batch fingerprint check; user approves via terminal prompt or `--yes` flag; batch_id written to BATCH-IDS.json before `.create()` call |
| Sample quality spot-check (50-sample visual review after probe batch) | D-05, D-06 | LLM-generated samples can drift in ways automated dedup/format checks miss (tone, realism, task coverage) | Plan 09.3-01 Task 4 after probe batch returns: display 50 random samples across domains; user confirms quality OR aborts and tunes prompts |
| Post-curation audit threshold review | 09.2-07 thresholds | Audit numbers (tool-call domain %, canned-suffix coverage, single-turn ending %) must meet 09.2-07 gates; if not, halt before training | Plan 09.3-02 Task 3 runs `assemble_dataset.py stats --json` + prints threshold table; user confirms all green OR re-curates with adjusted thresholds |
| v5 D-09 strict gate verdict | D-11 (strict), D-12, D-13 | Ship/no-ship decision; `eval_compare` automates the number but the ship decision per D-14 is a human call | Plan 09.3-03 Task 4 runs eval_compare, emits verdict JSON; user reviews and picks: PASS (ship v5) / FAIL-parity (escalate per D-14) / FAIL-below-parity (halt for diagnosis) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (manual-only checkpoints count as "sampled" for this rule)
- [ ] Wave 0 covers all MISSING references (knowledge pool YAML schema, batch cost forecast, continue-train flag parsing)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter after per-task map is populated
- [ ] `wave_0_complete: true` set after all Wave 0 tests land (mix of GREEN + xfail)

**Approval:** pending (flipped to approved when Plan 09.3-03 Task 4 completes with D-09 verdict)
