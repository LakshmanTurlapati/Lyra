---
phase: 06-general-knowledge-dataset
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/generate_knowledge_data.py
  - tests/test_generate_knowledge_data.py
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03]

must_haves:
  truths:
    - "generate_qa_batch() produces factual Q&A samples with concise responses (200-600 token range) that include reasoning markers"
    - "generate_explanation_batch() produces explanation samples with structured responses using analogies/examples (400-1000 token range)"
    - "generate_reasoning_batch() produces reasoning chain samples with numbered steps and explicit conclusions (500-1200 token range)"
    - "Topic domain weighting follows tech 40%, math 25%, science 20%, other 15% distribution"
    - "200+ unique question-response pairs exist across all categories to prevent dedup collapse"
    - "All generated samples pass Conversation.model_validate() from validate_format.py"
  artifacts:
    - path: "scripts/generate_knowledge_data.py"
      provides: "Knowledge generation script with 3 category generators, CLI, validate_batch, write_batch"
      exports: ["generate_qa_batch", "generate_explanation_batch", "generate_reasoning_batch", "validate_batch", "write_batch", "main"]
    - path: "tests/test_generate_knowledge_data.py"
      provides: "Test suite covering all 3 categories, format validation, domain weighting, diversity, style compliance, and CLI"
      min_lines: 100
  key_links:
    - from: "scripts/generate_knowledge_data.py"
      to: "scripts/validate_format.py"
      via: "Conversation.model_validate() in validate_batch()"
      pattern: "from scripts\\.validate_format import Conversation"
    - from: "scripts/generate_knowledge_data.py"
      to: "templates/system-prompts.yaml"
      via: "yaml.safe_load for knowledge_assistant and knowledge_reasoning prompts"
      pattern: "knowledge_assistant|knowledge_reasoning"
---

<objective>
Build the knowledge data generation script (TDD) with 3 category generators (factual Q&A, explanations, reasoning chains), a 200+ unique question-response topic pool, weighted domain selection, and full test coverage.

Purpose: This is the only new code in Phase 6. The generation script must produce diverse enough samples that the existing curation pipeline retains 50%+ (vs Phase 5's 17.6%). The 200+ unique Q&A pairs with varied phrasing are the critical mitigation for dedup collapse.

Output: scripts/generate_knowledge_data.py + tests/test_generate_knowledge_data.py
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-general-knowledge-dataset/06-CONTEXT.md
@.planning/phases/06-general-knowledge-dataset/06-RESEARCH.md

# Reference implementation (Phase 5 -- follow this pattern exactly)
@scripts/generate_code_data.py

# Validation models -- reuse directly
@scripts/validate_format.py

# System prompts (knowledge_assistant, knowledge_reasoning)
@templates/system-prompts.yaml

# Category templates (topics, complexity, generation_notes)
@templates/knowledge.yaml

# Style validator (knowledge domain: min_tokens=200, require_reasoning_markers=true)
@scripts/style_validator.py

# Pipeline config (knowledge domain: min_response_chars=100)
@configs/pipeline.yaml

<interfaces>
<!-- Key types and contracts the executor needs. -->

From scripts/validate_format.py:
```python
class Conversation(BaseModel):
    messages: list[Message]
    # model_validator(mode='after') enforces:
    # - system message first
    # - role ordering rules
    # - at least 2 messages
```

From scripts/style_validator.py:
```python
_REASONING_PATTERNS: list[str] = [
    r"step \d", r"\d+\.", r"because", r"therefore",
    r"first,", r"second,", r"finally,", r"let me", r"this means",
]

def has_reasoning_markers(text: str) -> bool:
    """Requires at least 2 distinct marker matches from _REASONING_PATTERNS."""

def validate_style(sample: dict, domain: str, config: dict) -> bool:
    """Knowledge domain checks: min_tokens >= 200, require_reasoning_markers=true."""
```

From templates/system-prompts.yaml:
```yaml
knowledge_assistant:
  content: |
    You are a knowledgeable assistant. Provide accurate, clear answers.
    For complex topics, break your explanation into steps.
    For simple factual questions, be concise.

knowledge_reasoning:
  content: |
    You are a reasoning assistant. Think through problems step by step.
    Show your reasoning process explicitly before giving a final answer.
```

From scripts/generate_code_data.py (pattern to follow):
```python
def generate_utility_batch(count: int = 50, seed: int = None) -> list[dict]: ...
def validate_batch(samples: list[dict]) -> dict: ...
def write_batch(samples: list[dict], output_path: Path) -> Path: ...
def main(): ...  # CLI with --category, --count, --batch, --seed, --output-dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build generate_knowledge_data.py with 200+ topic pool and 3 category generators (TDD)</name>
  <files>scripts/generate_knowledge_data.py, tests/test_generate_knowledge_data.py</files>
  <read_first>
    - scripts/generate_code_data.py (reference pattern -- follow structure exactly)
    - scripts/validate_format.py (Conversation model for validate_batch)
    - scripts/style_validator.py (has_reasoning_markers patterns -- all responses must pass)
    - templates/system-prompts.yaml (knowledge_assistant, knowledge_reasoning prompts)
    - templates/knowledge.yaml (category definitions)
    - configs/pipeline.yaml (knowledge domain: min_response_chars=100, min_tokens=200, require_reasoning_markers=true)
  </read_first>
  <behavior>
    - Test: generate_qa_batch(count=10, seed=42) returns exactly 10 samples, each with 3 messages (system/user/assistant)
    - Test: generate_explanation_batch(count=10, seed=42) returns 10 valid samples with structured explanations
    - Test: generate_reasoning_batch(count=10, seed=42) returns 10 valid samples with numbered steps and conclusions
    - Test: All samples from each generator pass Conversation.model_validate()
    - Test: All samples from each generator pass has_reasoning_markers() (2+ markers per response)
    - Test: Q&A response lengths fall within 200-600 approximate tokens (154-462 words at 1.3x)
    - Test: Explanation response lengths fall within 400-1000 approximate tokens (308-769 words)
    - Test: Reasoning response lengths fall within 500-1200 approximate tokens (385-923 words)
    - Test: All responses are at least 100 characters (knowledge domain min_response_chars)
    - Test: Domain weight distribution across 1000 samples approximates tech=40%, math=25%, science=20%, other=15% (within 5% tolerance)
    - Test: Topic pool contains 200+ unique question strings across all categories combined
    - Test: Two batches with different seeds produce different sample orderings (diversity)
    - Test: No two responses in a batch of 50 are identical strings (within-batch uniqueness)
    - Test: Q&A uses knowledge_assistant system prompt, reasoning uses knowledge_reasoning system prompt
    - Test: validate_batch() returns correct valid/invalid counts
    - Test: write_batch() creates JSONL file with one JSON object per line
    - Test: CLI main() accepts --category {qa,explanation,reasoning} --count N --batch N --seed N --output-dir
  </behavior>
  <action>
RED phase: Write tests/test_generate_knowledge_data.py with the test cases above. Organize into test classes: TestQABatch, TestExplanationBatch, TestReasoningBatch, TestDomainWeights, TestDiversity, TestValidation, TestCLI. Run tests -- all must fail (module does not exist yet). Commit: test(06-01): add failing tests for knowledge generation script.

GREEN phase: Create scripts/generate_knowledge_data.py implementing:

1. **Topic pool (200+ unique entries):** Build topic dictionaries organized by domain (technology, math, science, other) and category (qa, explanation, reasoning). Each entry is a dict with "q" (question string) and "a" (response string). Per D-07, these are genuinely unique question-response pairs, NOT template substitutions.

   Distribution per D-05:
   - Technology/computing: ~80 entries (~40%)
   - Math/logic: ~50 entries (~25%)
   - Science: ~40 entries (~20%)
   - Other (history, geography, everyday): ~30 entries (~15%)

   Split across categories per D-02:
   - Q&A topics: ~40% of pool entries
   - Explanation topics: ~35% of pool entries
   - Reasoning topics: ~25% of pool entries

   CRITICAL per D-07 and D-08: Every response must be genuinely unique content. Vary:
   - Question phrasing: mix how/what/why/explain/compare forms
   - Response openings: never start two responses with the same phrase
   - Response structure: some use bullet points, some paragraphs, some numbered lists
   - Response length: naturally vary within D-03 token ranges

   CRITICAL per research pitfall 3 and A3: ALL responses (including Q&A) must contain at least 2 reasoning markers from _REASONING_PATTERNS. For Q&A, use patterns like numbered points ("1.", "2.") or "because" explanations naturally. For reasoning chains, use "Step 1:", "Step 2:", "Therefore,". For explanations, use "First,", "Second,", numbered lists.

2. **Domain weight selection:** DOMAIN_WEIGHTS = {"technology": 0.40, "math": 0.25, "science": 0.20, "other": 0.15}. Use rng.choices() per D-05.

3. **Three generator functions:**
   - generate_qa_batch(count=50, seed=None) -> list[dict] -- Uses knowledge_assistant system prompt per D-04
   - generate_explanation_batch(count=50, seed=None) -> list[dict] -- Uses knowledge_assistant system prompt per D-04
   - generate_reasoning_batch(count=50, seed=None) -> list[dict] -- Uses knowledge_reasoning system prompt per D-04

   Each generator:
   - Creates random.Random(seed) for reproducibility
   - Loads system prompts via yaml.safe_load (same as generate_code_data.py)
   - Picks domain via weighted selection per D-05
   - Picks unique question from that domain's category pool (tracking used questions in a set)
   - Builds 3-message conversation: system -> user -> assistant
   - Returns list of conversation dicts

4. **Shared utilities:** load_system_prompts(), _pick_weighted_domain(), _get_unique_qa() following generate_code_data.py pattern exactly.

5. **validate_batch() and write_batch():** Copy pattern from generate_code_data.py. validate_batch uses Conversation.model_validate(). write_batch writes JSONL.

6. **CLI main():**
   - --category: choices=["qa", "explanation", "reasoning"] per D-11
   - --count: default=50, max=10000
   - --batch: required, positive integer
   - --seed: optional
   - --output-dir: default=datasets/knowledge/
   - Output filename: {category}-batch-{batch:02d}.jsonl
   - Same validation, generation, write pattern as generate_code_data.py

Run tests -- all must pass. Commit: feat(06-01): implement knowledge generation script with 200+ topic pool.

REFACTOR (if needed): Clean up topic pool organization, ensure consistent formatting. Run tests -- must still pass. Commit: refactor(06-01): clean up knowledge generation script.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/AI/Claude-Mini && python3 -m pytest tests/test_generate_knowledge_data.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_knowledge_data.py exists with generate_qa_batch, generate_explanation_batch, generate_reasoning_batch, validate_batch, write_batch, main
    - tests/test_generate_knowledge_data.py exists with 15+ passing tests
    - python3 -m pytest tests/test_generate_knowledge_data.py -x -q shows all green
    - python3 -m pytest -x -q (full suite) shows no regressions
    - Topic pool contains 200+ unique question strings (grep -c '"q":' or count in test)
    - All generated responses contain at least 2 reasoning markers
    - All generated responses are at least 100 characters
  </acceptance_criteria>
  <done>
    Knowledge generation script produces valid ShareGPT conversations across 3 categories with 200+ unique topics, weighted domain distribution, and all responses passing style validator requirements. Full test suite green with no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file loading | Templates and system prompts loaded from disk via yaml.safe_load |
| CLI argument parsing | User-provided --count, --batch, --seed, --output-dir values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | YAML loading | mitigate | Use yaml.safe_load() only (already enforced in codebase pattern from T-02-04). Never use yaml.load() or yaml.unsafe_load(). |
| T-06-02 | Denial of Service | CLI --count parameter | mitigate | Cap MAX_COUNT at 10000 (same as generate_code_data.py). Validate count > 0 and count <= MAX_COUNT before generation. |
| T-06-03 | Denial of Service | CLI --output-dir path traversal | accept | Low risk -- CLI tool used by developer locally. Output dir is validated as Path object by argparse. No network exposure. |
| T-06-04 | Information Disclosure | Generated training data content | accept | All data is pre-written in the script (no API calls, no user data). Content is factual knowledge intended for open-source release under MIT. |
</threat_model>

<verification>
- python3 -m pytest tests/test_generate_knowledge_data.py -x -q -- all tests pass
- python3 -m pytest -x -q -- full suite green, no regressions
- python3 -m scripts.generate_knowledge_data --category qa --count 5 --batch 99 --seed 42 -- produces valid JSONL
- grep -c '"q":' scripts/generate_knowledge_data.py shows 200+ topic entries (approximate check)
</verification>

<success_criteria>
- Knowledge generation script exists with 3 category generators matching Phase 4-5 CLI pattern
- 200+ unique question-response pairs across all domains and categories
- All responses pass has_reasoning_markers() (2+ markers)
- All responses meet min_response_chars=100 and min_tokens=200 thresholds
- Test suite has 15+ passing tests covering categories, format, weights, diversity, style, CLI
- Full project test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-general-knowledge-dataset/06-01-SUMMARY.md`
</output>
