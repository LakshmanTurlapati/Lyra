---
phase: 08-fine-tuning
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - scripts/train.py
  - tests/test_train.py
autonomous: false
requirements: [TRNG-01, TRNG-02, TRNG-03]
user_setup: []

must_haves:
  truths:
    - "Training script initializes model, tokenizer, dataset, and trainer without error on a 2-sample subset"
    - "Script produces adapter files (adapter_config.json + adapter_model.safetensors) in output directory after minimal training"
    - "Script produces merged model files in merged directory when not using --no-merge"
    - "User can run full training on assembled dataset and get a working fine-tuned model"
  artifacts:
    - path: "scripts/train.py"
      provides: "Verified training script that passes smoke test on real hardware"
      contains: "train"
    - path: "tests/test_train.py"
      provides: "Integration smoke test verifying end-to-end training on tiny subset"
      contains: "test_training_smoke"
  key_links:
    - from: "scripts/train.py"
      to: "datasets/assembled/train"
      via: "DatasetDict.load_from_disk then .select(range(2))"
      pattern: "select"
    - from: "scripts/train.py"
      to: "models/lyra-adapter/adapter_config.json"
      via: "trainer.save_model"
      pattern: "adapter_config"
    - from: "scripts/train.py"
      to: "models/lyra-merged/"
      via: "merge_and_unload + save_pretrained"
      pattern: "safetensors"
---

<objective>
Validate the training script end-to-end with a smoke test on a tiny data subset (2 samples, 1 step) to confirm it initializes correctly on real hardware (MPS) and produces valid adapter + merged model output files.

Purpose: Proves TRNG-01 (end-to-end training works), TRNG-02 (runs on consumer hardware), TRNG-03 (produces model weights). Does NOT run full training (30-90 minutes) -- just validates the script is correct and produces output.
Output: Verified training script, integration smoke test, human-verified readiness for full training run
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fine-tuning/08-CONTEXT.md
@.planning/phases/08-fine-tuning/08-RESEARCH.md
@.planning/phases/08-fine-tuning/08-01-SUMMARY.md
@scripts/train.py
@tests/test_train.py
@requirements.txt

<interfaces>
<!-- From Plan 01 output -->
From scripts/train.py (created in Plan 01):
- detect_hardware() -> tuple[str, Optional[BitsAndBytesConfig]]
- build_parser() -> argparse.ArgumentParser
- get_lora_config(args) -> LoraConfig
- get_training_args(args, device) -> SFTConfig
- main() -> None (orchestrates full training)

CLI flags include:
- --epochs N (default 3)
- --batch-size N (default 4)
- --max-length N (default 4096)
- --output-dir PATH (default models/lyra-adapter)
- --merged-dir PATH (default models/lyra-merged)
- --dataset-dir PATH (default datasets/assembled)
- --no-merge (skip merge step)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test on tiny data subset</name>
  <files>scripts/train.py, tests/test_train.py</files>
  <read_first>
    - scripts/train.py (full implementation from Plan 01)
    - tests/test_train.py (existing unit tests from Plan 01)
    - .planning/phases/08-fine-tuning/08-RESEARCH.md (Pitfalls section for MPS issues)
    - datasets/assembled/dataset_dict.json (confirm dataset accessible)
  </read_first>
  <action>
    Add an integration smoke test to tests/test_train.py that validates the training script runs end-to-end on a 2-sample subset with 1 training step. This test verifies the full pipeline works on real hardware without running a full training job.

    The smoke test (test_training_smoke) should:
    1. Create a temporary output directory (tmp_path fixture)
    2. Run the training script via subprocess with arguments:
       --dataset-dir datasets/assembled
       --output-dir {tmp_path}/adapter
       --merged-dir {tmp_path}/merged
       --epochs 1
       --batch-size 1
       --grad-accum 1
       --max-length 512
       --no-merge (skip merge for speed in test)
       Plus a custom flag --max-steps 1 to exit after 1 step
    3. Assert the process exits with code 0
    4. Assert adapter_config.json exists in {tmp_path}/adapter/
    5. Assert the adapter_config.json contains expected fields (r, lora_alpha, target_modules)

    To support --max-steps in the training script, add this flag to build_parser() in scripts/train.py:
    --max-steps (default: -1, meaning use epochs). When > 0, set max_steps in SFTConfig and override num_train_epochs to a large number so max_steps takes precedence.

    Also add a separate test (test_merge_produces_model) that:
    1. Uses a pre-saved minimal adapter (or mocks the merge flow)
    2. Verifies merge_and_unload path produces files in merged directory
    3. This can be a unit test that mocks the model loading if full merge is too slow

    Mark smoke tests with @pytest.mark.slow so they can be skipped in CI with -m "not slow".

    IMPORTANT: The smoke test actually downloads SmolLM2-1.7B-Instruct (~3.4GB) on first run. Mark the test so users understand this. Add a docstring noting the download requirement.

    MPS-specific considerations in the test:
    - Set PYTORCH_ENABLE_MPS_FALLBACK=1 in subprocess env
    - Timeout of 300 seconds (model download + 1 training step)
    - Skip test if torch not installed (importorskip pattern)
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/AI/Claude-Mini && python -m pytest tests/test_train.py -x -v -m "not slow" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "max_steps\|max-steps" scripts/train.py returns at least 2 matches (flag + usage)
    - grep "test_training_smoke" tests/test_train.py returns at least 1
    - grep "pytest.mark.slow" tests/test_train.py returns at least 1
    - grep "adapter_config" tests/test_train.py returns at least 1
    - python -m pytest tests/test_train.py -x -m "not slow" passes (unit tests without slow marker)
  </acceptance_criteria>
  <done>
    Integration smoke test added that validates full training pipeline (init, train 1 step, save adapter) on real hardware. --max-steps flag added to scripts/train.py for quick validation runs. All non-slow tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify training script readiness for full training run</name>
  <files>scripts/train.py</files>
  <action>
    Present the completed training script and test results to the user for verification before full training.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/AI/Claude-Mini && python scripts/train.py --help 2>&1 | head -30</automated>
  </verify>
  <done>User confirms script help output looks correct and approves readiness for full training run.</done>
  <what-built>
    Complete fine-tuning training script (scripts/train.py) with:
    - Hardware auto-detection (MPS/CUDA/CPU)
    - Full argparse CLI with all hyperparameters configurable
    - LoRA configuration targeting 7 projection layers
    - SFTTrainer with assistant_only_loss and epoch-based checkpointing
    - Adapter save + model merge logic
    - Unit tests for all configuration logic
    - Integration smoke test for 1-step validation

    The script is ready for a full training run (~30-90 minutes on M3 Pro).
  </what-built>
  <how-to-verify>
    1. Review script help: `python scripts/train.py --help`
       - Expect: All flags listed with defaults matching D-04 (lr=2e-4, epochs=3, batch=4, etc.)

    2. Run the smoke test (requires ~3.4GB model download on first run):
       `python -m pytest tests/test_train.py::test_training_smoke -x -v`
       - Expect: Downloads SmolLM2-1.7B-Instruct, runs 1 training step, saves adapter files
       - Expected time: 2-5 minutes (mostly model download)

    3. (Optional) Run full training when ready:
       `python scripts/train.py`
       - Expected time: 30-90 minutes on M3 Pro
       - Output: models/lyra-adapter/ and models/lyra-merged/

    4. Verify unit tests all pass:
       `python -m pytest tests/test_train.py -x -v -m "not slow"`
       - Expect: All unit tests green
  </how-to-verify>
  <resume-signal>Type "approved" if script help looks correct and unit tests pass, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace Hub -> model download | SmolLM2-1.7B-Instruct weights downloaded over network |
| Assembled dataset -> training input | Arrow files read from local disk |
| Training output -> model files | Adapter and merged model written to local disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | Model download from Hub | accept | Standard HuggingFace Hub model with checksums. SmolLM2-1.7B-Instruct is a verified HuggingFace official model. |
| T-08-06 | Tampering | Smoke test temp directory | mitigate | Use pytest tmp_path fixture (auto-cleanup). Do not write to user directories from tests. |
| T-08-07 | Denial of Service | Model download in CI | accept | Smoke test marked with @pytest.mark.slow. CI can skip with -m "not slow". Download only happens on explicit test invocation. |
</threat_model>

<verification>
1. `python -m pytest tests/test_train.py -x -m "not slow"` -- unit tests pass without GPU
2. `python scripts/train.py --help` -- shows all flags with correct defaults
3. `grep "max_steps\|max-steps" scripts/train.py` -- quick-exit flag present
4. `grep "pytest.mark.slow" tests/test_train.py` -- slow tests properly marked
</verification>

<success_criteria>
- Training script verified to initialize and run 1 step on real hardware without error
- Adapter output files produced correctly from smoke test
- --max-steps flag enables quick validation without full 30-90 min training
- User has confirmed script readiness for full training run
- All unit tests (non-slow) pass on any machine
</success_criteria>

<output>
After completion, create `.planning/phases/08-fine-tuning/08-02-SUMMARY.md`
</output>
