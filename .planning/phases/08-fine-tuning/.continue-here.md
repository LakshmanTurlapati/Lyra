---
context: phase
phase: 08-fine-tuning
task: 0
total_tasks: 0
status: complete_pending_training
last_updated: 2026-04-21T00:47:12.855Z
---

<current_state>
Phase 8 (Fine-Tuning) scripts are complete and verified. All 8 phases of code/infrastructure are done. The user needs to run actual training before Phase 9 can begin.

Phases 1-8 complete:
- Phase 1: Data format spec + validation
- Phase 2: Quality/curation pipeline
- Phase 3: Evaluation framework
- Phase 4: Tool-calling dataset (2,470 curated samples)
- Phase 5: Code generation dataset (600 curated samples)
- Phase 6: General knowledge dataset (560 curated samples)
- Phase 7: Dataset assembly (3,630 total, stratified splits)
- Phase 8: Training script ready (scripts/train.py)

Next: Phase 9 (Benchmarking and Core Release) -- requires trained model weights.
</current_state>

<completed_work>

This session completed Phases 4-8 end-to-end via --chain auto-advance:

- Phase 4: Generated 3,300 tool-calling samples, curated to 2,470
- Phase 5: Generated 3,400 code samples, curated to 600 (dedup limited)
- Phase 6: Generated 3,350 knowledge samples, curated to 560 (dedup limited)
- Phase 7: Assembled all 3,630 into HF DatasetDict with 90/5/5 stratified splits
- Phase 8: Built training script with MPS auto-detect, LoRA config, SFTTrainer

All 267 tests passing across the project.
</completed_work>

<remaining_work>

**Before Phase 9:**
1. User must install ML dependencies: `pip install -r requirements.txt`
2. User must run training: `python3 -m scripts.train --data-dir datasets/assembled`
3. Training produces models/lyra-adapter/ and models/lyra-merged/ (~30-90 min on MPS)

**Phase 9: Benchmarking and Core Release**
- Evaluate trained model on BFCL, HumanEval/MBPP, MMLU/ARC/HellaSwag
- Produce comparison report (base vs fine-tuned)
- Publish dataset card, model card, and weights on HuggingFace

**Phase 10: Community Release Enhancements**
- GGUF quantized variants
- Interactive Gradio demo Space
- Versioned dataset releases
</remaining_work>

<decisions_made>

Key decisions from this session:
- Tool-calling: 50-100 tool schemas, weighted category distribution, 25% edge cases
- Code: Python-heavy, utility-heavy, terse code-first style
- Knowledge: Q&A-heavy, tech/STEM weighted, 200+ topic pool for dedup mitigation
- Assembly: Use ALL samples with natural imbalance (not 33/33/33), 90/5/5 splits, HF format
- Training: MPS target, 16-bit LoRA (no QLoRA on MPS), r=16/alpha=32/lr=2e-4/3 epochs
</decisions_made>

<blockers>
- Training requires ML dependencies not yet installed (torch, peft, trl, accelerate)
- Training takes 30-90 minutes on Apple Silicon -- manual step
- Code and knowledge datasets had lower-than-target curation yield (dedup collapse from template similarity)
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` -- Phase 9 and 10 details
2. `.planning/phases/08-fine-tuning/08-CONTEXT.md` -- Training decisions
3. `scripts/train.py` -- The training script to run
4. `.planning/REQUIREMENTS.md` -- EVAL-01, EVAL-02, REL-01 through REL-04 for Phase 9

## Infrastructure State
- No running services
- No external dependencies active
- Dataset at `datasets/assembled/` (HF DatasetDict, gitignored Arrow files -- reproducible via `python3 -m scripts.assemble_dataset assemble`)

<context>
The project built all infrastructure and data. The user now needs to:
1. Install deps and run training (manual, ~30-90 min)
2. Then resume with /gsd-discuss-phase 9 --chain for benchmarking and release

The dedup issue (Phases 5-6 producing fewer samples than target) is a known limitation of template-based generation. The model will train on 3,630 samples total which is below the original 5K target but sufficient for a v1 proof of concept.
</context>

<next_action>
Start with:
1. `pip install -r requirements.txt`
2. `python3 -m scripts.train --data-dir datasets/assembled --max-steps 1` (smoke test)
3. `python3 -m scripts.train --data-dir datasets/assembled` (full training)
4. Then: `/gsd-discuss-phase 9 --chain`
</next_action>
