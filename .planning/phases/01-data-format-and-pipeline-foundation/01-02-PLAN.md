---
phase: 01-data-format-and-pipeline-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/validate_tokenizer.py
  - scripts/generate_sample.py
  - tests/test_tokenizer_validator.py
  - datasets/tool-calling/.gitkeep
  - datasets/code/.gitkeep
  - datasets/knowledge/.gitkeep
  - .gitignore
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "User can validate any conversation against SmolLM2-1.7B tokenizer and get pass/fail with token count and error details"
    - "Conversations exceeding 2048 tokens after apply_chat_template are rejected"
    - "Missing EOS token at end of tokenized output is detected"
    - "Default system prompt injection (SmolLM string) is detected and flagged"
    - "Chat template markers (<|im_start|>, <|im_end|>) are verified in tokenized output"
    - "User can generate a small sample batch and validate it end-to-end"
  artifacts:
    - path: "scripts/validate_tokenizer.py"
      provides: "SmolLM2 tokenizer alignment validation"
      exports: ["validate_conversation", "validate_file", "load_tokenizer"]
    - path: "scripts/generate_sample.py"
      provides: "Sample conversation generator for testing the pipeline"
      exports: ["generate_samples"]
    - path: "tests/test_tokenizer_validator.py"
      provides: "Unit tests for tokenizer validation covering DATA-02"
      contains: "test_valid_conversation_passes_tokenizer"
    - path: "datasets/tool-calling/.gitkeep"
      provides: "Directory structure for tool-calling data per D-09"
    - path: "datasets/code/.gitkeep"
      provides: "Directory structure for code data per D-09"
    - path: "datasets/knowledge/.gitkeep"
      provides: "Directory structure for knowledge data per D-09"
  key_links:
    - from: "scripts/validate_tokenizer.py"
      to: "HuggingFaceTB/SmolLM2-1.7B-Instruct"
      via: "AutoTokenizer.from_pretrained()"
      pattern: "AutoTokenizer.from_pretrained"
    - from: "scripts/validate_tokenizer.py"
      to: "scripts/validate_format.py"
      via: "Uses Conversation model for pre-validation before tokenizer check"
      pattern: "from scripts.validate_format import"
    - from: "scripts/generate_sample.py"
      to: "scripts/validate_format.py"
      via: "Validates generated samples against Pydantic schema"
      pattern: "from scripts.validate_format import"
    - from: "scripts/generate_sample.py"
      to: "scripts/validate_tokenizer.py"
      via: "Validates generated samples against tokenizer"
      pattern: "from scripts.validate_tokenizer import"
---

<objective>
Build tokenizer alignment validation that catches the most dangerous failure mode: data that looks structurally correct but tokenizes incorrectly for SmolLM2-1.7B. Also create the sample generation script and dataset directory structure.

Purpose: Tokenizer misalignment is a silent total-loss failure -- training loss looks normal but the model produces gibberish at turn boundaries. This plan ensures every conversation is validated through the actual SmolLM2 tokenizer before it enters any training dataset.

Output: Tokenizer validation script, sample generator, dataset directories, and passing test suite for DATA-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-format-and-pipeline-foundation/01-CONTEXT.md
@.planning/phases/01-data-format-and-pipeline-foundation/01-RESEARCH.md
@.planning/phases/01-data-format-and-pipeline-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From scripts/validate_format.py (created in Plan 01): -->

```python
from pydantic import BaseModel, model_validator
from typing import Optional
from pathlib import Path

class FunctionDef(BaseModel):
    name: str
    description: str
    parameters: dict

class ToolSchema(BaseModel):
    type: str = "function"
    function: FunctionDef

class ToolCallFunction(BaseModel):
    name: str
    arguments: dict

class ToolCall(BaseModel):
    type: str = "function"
    function: ToolCallFunction

class Message(BaseModel):
    role: str
    content: Optional[str] = None
    tool_calls: Optional[list[ToolCall]] = None
    name: Optional[str] = None

class Conversation(BaseModel):
    messages: list[Message]
    tools: Optional[list[ToolSchema]] = None

def validate_file(path: Path) -> dict:
    """Returns {"total": int, "valid": int, "invalid": int, "errors": [...]}"""
```

<!-- From tests/conftest.py (created in Plan 01): -->
Fixtures available: valid_conversation, valid_tool_call_conversation, valid_parallel_tool_call_conversation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tokenizer validation script and tests</name>
  <files>scripts/validate_tokenizer.py, tests/test_tokenizer_validator.py</files>
  <read_first>
    - scripts/validate_format.py (current state -- need exact import paths and class names for Conversation model)
    - tests/conftest.py (current fixtures -- reuse valid_conversation, valid_tool_call_conversation for tokenizer tests)
    - .planning/phases/01-data-format-and-pipeline-foundation/01-RESEARCH.md (lines 199-242 for tokenizer validation pattern, lines 302-347 for SmolLM2 tokenizer config and chat template details)
  </read_first>
  <behavior>
    - Test: valid basic conversation passes tokenizer validation with valid=True and token_count > 0 and token_count <= 2048
    - Test: valid tool call conversation passes tokenizer validation with valid=True
    - Test: conversation producing > 2048 tokens returns valid=False with error containing "exceeds 2048 limit"
    - Test: tokenized output contains "<|im_start|>" and "<|im_end|>" markers (verified via decode)
    - Test: tokenized output ends with EOS token (id matching tokenizer.eos_token_id)
    - Test: conversation without system message triggers default prompt detection (error contains "Default system prompt")
    - Test: validate_file on JSONL returns per-line results with token counts
    - Test: validate_file on JSONL with oversized conversation reports it as invalid
  </behavior>
  <action>
    1. Create scripts/validate_tokenizer.py with the following structure:

       Constants at module level:
       ```python
       MODEL_ID = "HuggingFaceTB/SmolLM2-1.7B-Instruct"
       MAX_TOKENS = 2048
       ```

       Function `load_tokenizer()`:
       - Calls `AutoTokenizer.from_pretrained(MODEL_ID)`
       - Returns the tokenizer instance
       - Import: `from transformers import AutoTokenizer`

       Function `validate_conversation(tokenizer, conversation: dict) -> dict`:
       - Takes a tokenizer instance and a conversation dict (with "messages" and optional "tools" keys)
       - Returns dict with keys: `valid` (bool), `token_count` (int), `errors` (list of str), `decoded_preview` (str, first 200 chars of decoded output)
       - Steps:
         a. Extract messages and tools from conversation dict
         b. Call `tokenizer.apply_chat_template(messages, tokenize=True, return_tensors=None)` -- if tools present, pass `tools=tools` as kwarg
         c. If apply_chat_template raises exception, return `{"valid": False, "token_count": 0, "errors": ["Template error: {e}"], "decoded_preview": ""}`
         d. Compute token_count = len(token_ids)
         e. If token_count > MAX_TOKENS (2048), append error: f"Token count {token_count} exceeds {MAX_TOKENS} limit"
         f. If token_ids[-1] != tokenizer.eos_token_id, append error: f"Missing EOS token (expected id={tokenizer.eos_token_id})"
         g. Decode token_ids with tokenizer.decode(token_ids) into decoded string
         h. If "You are a helpful AI assistant named SmolLM" in decoded AND "SmolLM" not in str(messages), append error: "Default system prompt injected -- explicit system message missing or ignored"
         i. For each message in messages, check f"<|im_start|>{msg['role']}" exists in decoded; if missing, append error: f"Missing role marker for '{msg['role']}'"
         j. Return `{"valid": len(errors) == 0, "token_count": token_count, "errors": errors, "decoded_preview": decoded[:200]}`

       Function `validate_file(tokenizer, path: Path) -> dict`:
       - Reads JSONL file line by line
       - For each line: json.loads(), then validate_conversation()
       - Returns `{"total": int, "valid": int, "invalid": int, "results": [{"line": int, "valid": bool, "token_count": int, "errors": list}]}`
       - Also computes and includes: `"token_stats": {"min": int, "max": int, "mean": float}` from valid conversations

       CLI entry point:
       - argparse with positional `jsonl_file` argument
       - `--model` optional argument defaulting to MODEL_ID
       - Loads tokenizer, validates file, prints JSON results to stdout
       - Exit code 0 if all valid, 1 if any invalid
       - No emojis in any output

    2. Create tests/test_tokenizer_validator.py:

       Import: `import pytest`, `import json`, `from pathlib import Path`, `from scripts.validate_tokenizer import validate_conversation, validate_file, load_tokenizer`

       Module-level fixture using `@pytest.fixture(scope="module")`:
       ```python
       @pytest.fixture(scope="module")
       def tokenizer():
           return load_tokenizer()
       ```

       Tests (reuse conftest fixtures where possible):

       - `test_valid_conversation_passes_tokenizer(tokenizer, valid_conversation)`:
         result = validate_conversation(tokenizer, valid_conversation)
         assert result["valid"] is True
         assert result["token_count"] > 0
         assert result["token_count"] <= 2048
         assert len(result["errors"]) == 0

       - `test_valid_tool_call_passes_tokenizer(tokenizer, valid_tool_call_conversation)`:
         result = validate_conversation(tokenizer, valid_tool_call_conversation)
         assert result["valid"] is True
         assert result["token_count"] > 0

       - `test_rejects_oversized_conversation(tokenizer)`:
         Create a conversation with system message + user message containing a very long string (repeat "word " * 3000) to exceed 2048 tokens. Validate and assert result["valid"] is False and any error contains "exceeds 2048 limit".

       - `test_detects_chat_template_markers(tokenizer, valid_conversation)`:
         result = validate_conversation(tokenizer, valid_conversation)
         assert "<|im_start|>" in result["decoded_preview"] or result["valid"] is True
         (The markers should be present in decoded output for valid conversations)

       - `test_detects_default_system_prompt_injection(tokenizer)`:
         Create conversation WITHOUT system message: `{"messages": [{"role": "user", "content": "Hello"}]}` -- note this is intentionally invalid for format validation but we are testing tokenizer-level detection.
         Try to validate -- if apply_chat_template succeeds (it may inject default system), check that errors contain "Default system prompt".
         If apply_chat_template fails, assert the error is captured.

       - `test_validate_file_returns_token_stats(tokenizer, valid_conversation, valid_tool_call_conversation, tmp_path)`:
         Write both fixtures as JSONL lines to tmp_path/"test.jsonl"
         result = validate_file(tokenizer, tmp_path / "test.jsonl")
         assert result["total"] == 2
         assert result["valid"] == 2
         assert "token_stats" in result
         assert result["token_stats"]["min"] > 0
         assert result["token_stats"]["max"] <= 2048

       - `test_validate_file_reports_oversized(tokenizer, valid_conversation, tmp_path)`:
         Write one valid line and one oversized line (user content = "x " * 3000) to JSONL
         result = validate_file(tokenizer, tmp_path / "test.jsonl")
         assert result["invalid"] >= 1
         assert any("exceeds" in str(r["errors"]) for r in result["results"] if not r["valid"])

       Mark all tokenizer tests with `@pytest.mark.slow` decorator (tokenizer download can take time on first run). Add a conftest.py-level marker registration or a pytest.ini update if needed.
  </action>
  <verify>
    <automated>pip install transformers>=5.5.0 && python -m pytest tests/test_tokenizer_validator.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate_tokenizer.py contains "def validate_conversation" and "def validate_file" and "def load_tokenizer"
    - scripts/validate_tokenizer.py contains "MODEL_ID" and "SmolLM2-1.7B-Instruct" and "MAX_TOKENS = 2048"
    - scripts/validate_tokenizer.py contains "apply_chat_template" and "eos_token_id" and "Default system prompt"
    - tests/test_tokenizer_validator.py contains "test_valid_conversation_passes_tokenizer" and "test_rejects_oversized_conversation" and "test_validate_file_returns_token_stats"
    - python -m pytest tests/test_tokenizer_validator.py -x -v exits 0
    - python -m pytest tests/test_tokenizer_validator.py -v output shows at least 7 tests passed
  </acceptance_criteria>
  <done>
    Tokenizer validation script catches oversized conversations, missing EOS tokens, default system prompt injection, and missing role markers. All 7+ tokenizer tests pass with the actual SmolLM2-1.7B-Instruct tokenizer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sample generator and dataset directory structure</name>
  <files>scripts/generate_sample.py, datasets/tool-calling/.gitkeep, datasets/code/.gitkeep, datasets/knowledge/.gitkeep, .gitignore</files>
  <read_first>
    - scripts/validate_format.py (need Conversation model for validation of generated samples)
    - scripts/validate_tokenizer.py (need validate_conversation and load_tokenizer for end-to-end validation)
    - specs/sharegpt-format.md (the format spec -- generated samples must comply)
    - .planning/phases/01-data-format-and-pipeline-foundation/01-RESEARCH.md (lines 590-665 for sample conversation JSON examples to embed as sample templates)
  </read_first>
  <action>
    1. Create dataset directory structure per D-09:
       - datasets/tool-calling/.gitkeep (empty file)
       - datasets/code/.gitkeep (empty file)
       - datasets/knowledge/.gitkeep (empty file)

    2. Create or update .gitignore to include:
       ```
       # Dataset files (large, generated)
       datasets/**/*.jsonl
       datasets/**/*.json
       !datasets/**/.gitkeep

       # Python
       __pycache__/
       *.pyc
       .pytest_cache/

       # Virtual environments
       venv/
       .venv/

       # Model cache
       .cache/
       ```

    3. Create scripts/generate_sample.py:

       Purpose: Generate a small batch of sample conversations for testing the validation pipeline end-to-end. These are NOT training data -- they are test fixtures to verify the format spec and validation scripts work correctly. Per D-05, actual training data generation happens in Claude Code sessions.

       Structure:
       - A list of 10 hardcoded sample conversations covering all three domains:
         - 3 tool-calling samples:
           (a) Single tool call -- get_weather example with tools column
           (b) Parallel tool calls -- get_weather for two cities with tools column
           (c) CLI command -- run_command tool with shell output
         - 4 code samples:
           (a) Simple function generation -- reverse string
           (b) File operation -- read CSV
           (c) Debugging -- fix off-by-one error
           (d) Utility function -- calculate fibonacci
         - 3 knowledge samples:
           (a) Factual Q&A -- explain photosynthesis
           (b) Reasoning chain -- step-by-step math problem
           (c) Explanation -- how does TCP/IP work

       All samples MUST:
       - Start with system role message (per format spec rule 1)
       - Use TRL-native format (role/content/tool_calls keys, NOT from/value)
       - Have tool_calls in assistant messages where applicable (not in content)
       - Include tools column for tool-calling samples
       - Be short enough to stay well under 2048 tokens after tokenization

       Function `generate_samples() -> list[dict]`:
       - Returns the list of 10 sample conversations

       Function `write_samples(output_dir: Path, domain: str, samples: list[dict])`:
       - Writes samples as JSONL to output_dir/domain/samples.jsonl
       - Each sample is one JSON line

       Function `validate_samples(samples: list[dict]) -> dict`:
       - Imports Conversation from scripts.validate_format
       - Validates each sample against Pydantic schema
       - Returns {"total": N, "valid": N, "invalid": N, "errors": [...]}

       CLI entry point:
       - `--output-dir` argument (default: "datasets")
       - `--validate-only` flag to just validate without writing
       - `--tokenizer-check` flag to also run tokenizer validation (loads SmolLM2 tokenizer)
       - Generates all 10 samples
       - Validates with Pydantic schema
       - If --tokenizer-check, also validates with tokenizer
       - Writes to appropriate subdirectories (tool-calling/samples.jsonl, code/samples.jsonl, knowledge/samples.jsonl)
       - Prints summary: total generated, total valid (format), total valid (tokenizer if checked)
       - Exit code 0 if all valid, 1 if any invalid
       - No emojis in any output
  </action>
  <verify>
    <automated>python scripts/generate_sample.py --validate-only && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_sample.py contains "def generate_samples" and "def write_samples" and "def validate_samples"
    - scripts/generate_sample.py contains at least 10 sample conversations (grep -c '"role": "system"' scripts/generate_sample.py returns 10+)
    - datasets/tool-calling/.gitkeep exists
    - datasets/code/.gitkeep exists
    - datasets/knowledge/.gitkeep exists
    - .gitignore contains "datasets/**/*.jsonl" and "__pycache__"
    - python scripts/generate_sample.py --validate-only exits 0 and output contains "valid" and "10"
    - python -m pytest tests/ -x -q exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <done>
    Sample generator produces 10 conversations across all 3 domains. All samples pass Pydantic format validation. Dataset directory structure created with .gitkeep files. .gitignore configured to exclude generated JSONL while keeping directory structure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace Hub -> tokenizer download | External model artifacts loaded from internet |
| JSONL file -> tokenizer validation | Untrusted data passed through apply_chat_template |
| Generated samples -> file system | Script writes files to datasets/ directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | HuggingFace tokenizer download | accept | Transformers library verifies model checksums; SmolLM2 is an official HuggingFace model; risk is standard for any ML workflow |
| T-01-06 | Tampering | apply_chat_template with malicious input | mitigate | Pydantic format validation runs BEFORE tokenizer validation; only structurally valid conversations reach apply_chat_template; Jinja2 template engine in transformers handles escaping |
| T-01-07 | Tampering | generate_sample.py file writes | mitigate | Output directory validated via pathlib; only writes to datasets/ subdirectories; .gitkeep files prevent accidental directory deletion |
| T-01-08 | Information Disclosure | Token counts and decoded previews in output | accept | Local CLI tool; token counts and decoded text contain no secrets beyond the input conversation data |
</threat_model>

<verification>
After both tasks complete:
1. `python -m pytest tests/ -v` -- all tests pass (format + tokenizer)
2. `python scripts/generate_sample.py --validate-only` -- 10/10 samples pass format validation
3. `python scripts/validate_tokenizer.py` on generated samples -- all pass tokenizer check
4. `ls datasets/tool-calling/.gitkeep datasets/code/.gitkeep datasets/knowledge/.gitkeep` -- all exist
5. `grep "datasets" .gitignore` -- gitignore configured
</verification>

<success_criteria>
- Tokenizer validation catches oversized (>2048 token) conversations with specific token count
- Tokenizer validation catches missing EOS tokens and default system prompt injection
- Sample generator produces 10 valid conversations across 3 domains
- All samples pass both Pydantic format validation AND tokenizer alignment validation
- Dataset directory structure exists with proper .gitignore configuration
- All tests pass: format validator tests + tokenizer validator tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-format-and-pipeline-foundation/01-02-SUMMARY.md`
</output>
