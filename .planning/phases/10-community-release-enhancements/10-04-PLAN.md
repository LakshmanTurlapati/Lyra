---
phase: 10-community-release-enhancements
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - README.md
  - datasets/README.md
  - CHANGELOG.md
  - release/dataset-v1.0.0/BENCHMARK-v1.0.0.md
  - .planning/phases/10-community-release-enhancements/10-UAT.md
  - .planning/phases/10-community-release-enhancements/10-VALIDATION.md
autonomous: false
requirements:
  - REL-05
  - REL-07
tags:
  - docs
  - uat
  - release-finalization
user_setup:
  - service: LM Studio
    why: "Manual UAT — required to confirm the Q4_K_M GGUF loads and honors the embedded chat template (no headless equivalent)."
    install:
      - task: "Install LM Studio"
        location: "https://lmstudio.ai/ (macOS dmg / Windows exe / Linux AppImage)"
  - service: llama.cpp llama-cli
    why: "Manual UAT — command-line smoke test with the Q8_0 GGUF."
    install:
      - task: "Install llama.cpp CLI tools"
        location: "macOS: `brew install llama.cpp` (same prereq as Plan 02)"

must_haves:
  truths:
    - "README.md GGUF Variants section has real file sizes + SHA256 checksums for Q4_K_M and Q8_0 (no TBD placeholders)."
    - "datasets/README.md Dataset Versions table has the v1.0.0 row populated with date, sample count, and bundle SHA256."
    - "BENCHMARK-v1.0.0.md snapshot attached to the dataset-v1.0.0 release matches the final Model v1.0.0 evaluation report (or a documented divergence explains the discrepancy)."
    - "LM Studio loads the Q4_K_M GGUF and responds to a tool-call test prompt using the embedded chat template (human-verified)."
    - "llama-cli -m lyra-v1.0-q8_0.gguf produces sensible output for a tool-call prompt (human-verified)."
    - "Downloaded dataset-v1.0.0 bundle SHA256 matches SHA256SUMS.txt (human-verified)."
    - "10-UAT.md captures pass/fail status + evidence (LM Studio screenshot reference, llama-cli output excerpt, SHA256 check output) for every Manual-Only row in 10-VALIDATION.md."
    - "10-VALIDATION.md Per-Task Verification Map is populated with real task IDs (T-10-01-*, T-10-02-*, T-10-03-*, T-10-04-*) and status ✅ green."
  artifacts:
    - path: "README.md"
      provides: "GGUF Variants table with real sizes and SHA256 (finalized from Plan 02 outputs)"
      contains: "Q4_K_M"
    - path: "datasets/README.md"
      provides: "Dataset Versions table with populated v1.0.0 row"
      contains: "v1.0.0"
    - path: ".planning/phases/10-community-release-enhancements/10-UAT.md"
      provides: "Manual UAT results for LM Studio + llama-cli + dataset round-trip"
      min_lines: 30
    - path: ".planning/phases/10-community-release-enhancements/10-VALIDATION.md"
      provides: "Per-Task Verification Map populated with every task from Plans 01-04"
      contains: "✅"
  key_links:
    - from: "README.md GGUF Variants table"
      to: "release/model-v1.0.0/SHA256SUMS.txt"
      via: "file size + SHA256 copied from the Plan 02 output"
      pattern: "[a-f0-9]{64}"
    - from: "datasets/README.md Dataset Versions table"
      to: "release/dataset-v1.0.0/SHA256SUMS.txt"
      via: "bundle SHA256 copied from the Plan 03 output"
      pattern: "[a-f0-9]{64}"
    - from: ".planning/phases/10-community-release-enhancements/10-UAT.md"
      to: "10-VALIDATION.md Manual-Only Verifications"
      via: "one UAT row per Manual-Only row"
      pattern: "(pass|fail)"
---

<objective>
Finalize Phase 10 by (a) backfilling README / datasets/README / CHANGELOG with the real sizes + SHA256 values produced by Plans 02 and 03, (b) reconciling the BENCHMARK-v1.0.0.md snapshot against the final Model v1.0.0 metrics if Plan 03 snapshotted before Plan 02 finalized, (c) running the full Manual-Only UAT from 10-VALIDATION.md and recording results in 10-UAT.md, and (d) populating the 10-VALIDATION.md Per-Task Verification Map with every task from Plans 01-04.

Purpose: Close REL-05 and REL-07 with a verified, downloadable-and-usable release posture. Docs must not carry stale TBD placeholders after this plan; manual checks that automated CI cannot cover are captured in 10-UAT.md.

Output: Finalized README.md, datasets/README.md, CHANGELOG.md; reconciled BENCHMARK-v1.0.0.md (if applicable); 10-UAT.md with pass/fail evidence per manual check; fully-populated 10-VALIDATION.md with `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-community-release-enhancements/10-CONTEXT.md
@.planning/phases/10-community-release-enhancements/10-VALIDATION.md
@.planning/phases/10-community-release-enhancements/10-01-SUMMARY.md
@.planning/phases/10-community-release-enhancements/10-02-SUMMARY.md
@.planning/phases/10-community-release-enhancements/10-03-SUMMARY.md

@README.md
@datasets/README.md
@CHANGELOG.md
@BENCHMARK.md
@release/model-v1.0.0/SHA256SUMS.txt
@release/dataset-v1.0.0/SHA256SUMS.txt
@release/dataset-v1.0.0/BENCHMARK-v1.0.0.md

<interfaces>
<!-- Inputs this plan pulls from Plan 02 and Plan 03 outputs -->

From Plan 02 (release/model-v1.0.0/SHA256SUMS.txt):
  <sha256>  build/gguf/lyra-v1.0-q4_k_m.gguf
  <sha256>  build/gguf/lyra-v1.0-q8_0.gguf

From Plan 03 (release/dataset-v1.0.0/SHA256SUMS.txt):
  <sha256>  train.jsonl
  <sha256>  validation.jsonl
  <sha256>  test.jsonl
  <sha256>  lyra-dataset-v1.0.0-stats.json
  <sha256>  BENCHMARK-v1.0.0.md

README.md GGUF Variants section was scaffolded in Plan 01 Task 3 with TBD placeholders in columns
"Size", "SHA256". This plan fills them in.

datasets/README.md Dataset Versions table was scaffolded in Plan 01 Task 3 with a single v1.0.0 row
holding TBD date, samples, SHA256. This plan fills it in.

10-VALIDATION.md Per-Task Verification Map currently has one TBD row; this plan replaces it with
rows for every task from all 4 plans.

10-UAT.md target structure (NEW file, modeled on .planning/phases/09.2-*/09.2-*-UAT.md if present):

  # Phase 10 — UAT Results

  | Manual-Only Verification | Result | Evidence / Notes |
  |--------------------------|--------|------------------|
  | Q4_K_M loads in LM Studio + honors embedded template | ✅ pass | screenshot ref, tool-call prompt used, output excerpt |
  | Q8_0 loads via llama-cli smoke test | ✅ pass | command + output excerpt |
  | Post-quant perplexity delta < 10% (optional) | ⬜ skipped / ✅ pass | f16 perp / Q4_K_M perp numbers |
  | Tagged dataset release downloads + reconstructs bundle | ✅ pass | gh release download output + SHA256 check |
</interfaces>
</context>

<preconditions>
**Plans 02 AND 03 must be complete** (wave 2 gate): both releases live on GitHub, both CHANGELOG sections populated, SHA256SUMS.txt files present in `release/model-v1.0.0/` and `release/dataset-v1.0.0/`, Plan 02 tool-call-format weights shipped (or documented revert-to-base posture).

If Plan 02 was halted on precondition failure (09.1/09.2 not ready), Plan 04 SHOULD NOT finalize the GGUF section of README — flag to user and await Plan 02 completion. The dataset-v1.0.0 finalization in datasets/README.md can proceed independently.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Finalize README + datasets/README + reconcile BENCHMARK-v1.0.0 snapshot</name>
  <files>
    README.md,
    datasets/README.md,
    CHANGELOG.md,
    release/dataset-v1.0.0/BENCHMARK-v1.0.0.md
  </files>
  <read_first>
    - README.md (GGUF Variants section scaffolded in Plan 01 Task 3 — look for the table with Size/SHA256 TBD placeholders)
    - datasets/README.md (Dataset Versions section scaffolded in Plan 01 Task 3 — look for the v1.0.0 row with TBD placeholders)
    - release/model-v1.0.0/SHA256SUMS.txt (Plan 02 output)
    - release/dataset-v1.0.0/SHA256SUMS.txt (Plan 03 output)
    - release/dataset-v1.0.0/BENCHMARK-v1.0.0.md (pinned snapshot from Plan 03)
    - BENCHMARK.md (current live benchmark report — compare against the pinned snapshot)
    - CHANGELOG.md (both [Dataset v1.0.0] and [Model v1.0.0] sections must be present from Plans 02/03)
    - .planning/phases/10-community-release-enhancements/10-02-SUMMARY.md (Plan 02 output summary — Q4/Q8 sizes)
    - .planning/phases/10-community-release-enhancements/10-03-SUMMARY.md (Plan 03 output summary — whether BENCHMARK snapshot includes final model metrics)
  </read_first>
  <secure_behavior ref="T-10-03">
    T-10-03 (release asset integrity): SHA256 values copied verbatim from the Plan 02/03 SHA256SUMS.txt files (the authoritative source). If a value disagrees between SHA256SUMS.txt and the GitHub Release page, HALT and re-download from GitHub to recompute — the Release asset is what users will hit, not the local file.
  </secure_behavior>
  <action>
STEP 1 — Extract sizes + SHA256 for GGUFs from Plan 02 outputs:
```bash
Q4_SHA=$(awk '/q4_k_m\.gguf/ {print $1}' release/model-v1.0.0/SHA256SUMS.txt)
Q8_SHA=$(awk '/q8_0\.gguf/ {print $1}' release/model-v1.0.0/SHA256SUMS.txt)
Q4_SIZE=$(du -h build/gguf/lyra-v1.0-q4_k_m.gguf | awk '{print $1}')
Q8_SIZE=$(du -h build/gguf/lyra-v1.0-q8_0.gguf | awk '{print $1}')
echo "Q4: $Q4_SIZE $Q4_SHA"
echo "Q8: $Q8_SIZE $Q8_SHA"
```

STEP 2 — Edit README.md GGUF Variants table to replace TBD placeholders. Find the row:
`| Q4_K_M  | TBD | ~4.9 | TBD | Balanced quality/size — recommended for consumer hardware |`
Replace TBD values with the captured Q4_SIZE and Q4_SHA. Same for Q8_0 row.

Target state:
```markdown
| Q4_K_M  | <Q4_SIZE> | ~4.9 | `<Q4_SHA>` | Balanced quality/size — recommended for consumer hardware |
| Q8_0    | <Q8_SIZE> | ~8.5 | `<Q8_SHA>` | Near-original quality |
```

Also remove the disclaimer line `*Sizes and SHA256 checksums are filled at release-cut time by Plan 04. See CHANGELOG.md for the current release's values.*` since they are no longer TBD.

STEP 3 — Extract dataset bundle SHA from Plan 03:
```bash
BUNDLE_SHA_CONCAT=$(sha256sum release/dataset-v1.0.0/SHA256SUMS.txt | awk '{print $1}')
# Or: use the per-file SHAs; the Dataset Versions table typically shows the bundle's SHA256SUMS.txt SHA.
# Extract date from the dataset-v1.0.0 tag commit
TAG_DATE=$(git log -1 --format=%cs dataset-v1.0.0)
# Extract total sample count from stats.json
N_TOTAL=$(python3 -c "import json; d = json.load(open('release/dataset-v1.0.0/lyra-dataset-v1.0.0-stats.json')); print(sum(s['total'] for s in d['splits'].values()))")
```

STEP 4 — Edit datasets/README.md Dataset Versions table. Find the row:
`| v1.0.0 | TBD | TBD | TBD | Initial release; filled at release-cut time by Plan 04. See CHANGELOG.md. |`
Replace with:
```markdown
| v1.0.0 | <TAG_DATE> | <N_TOTAL> | `<BUNDLE_SHA_CONCAT>` (SHA256SUMS.txt) | Initial release from 25K Phase 09.1 re-curation. See CHANGELOG.md. |
```

STEP 5 — Reconcile BENCHMARK-v1.0.0.md snapshot. Read 10-03-SUMMARY.md field "Whether BENCHMARK-v1.0.0.md content includes Model v1.0.0 metrics or only pre-09.1 metrics":
- If it already includes final Model v1.0.0 metrics → no action needed; snapshot is sealed.
- If it was snapshotted before Plan 02 finalized BENCHMARK.md → backfill now:
  ```bash
  cp BENCHMARK.md release/dataset-v1.0.0/BENCHMARK-v1.0.0.md
  python3 <<'PY'
  from pathlib import Path
  p = Path("release/dataset-v1.0.0/BENCHMARK-v1.0.0.md")
  orig = p.read_text()
  header = "> **Pinned snapshot for Dataset v1.0.0.** Backfilled by Plan 04 after Plan 02 finalized the Model v1.0.0 metrics. This file is now immutable.\n\n"
  if not orig.startswith("> **Pinned snapshot"):
      p.write_text(header + orig)
  PY
  # Recompute SHA for release/dataset-v1.0.0/BENCHMARK-v1.0.0.md and update SHA256SUMS.txt + CHANGELOG [Dataset v1.0.0] + datasets/README.md if needed
  (cd release/dataset-v1.0.0 && shasum -a 256 train.jsonl validation.jsonl test.jsonl lyra-dataset-v1.0.0-stats.json BENCHMARK-v1.0.0.md > SHA256SUMS.txt)
  # Re-upload the updated BENCHMARK-v1.0.0.md to the release (gh release upload --clobber)
  gh release upload dataset-v1.0.0 release/dataset-v1.0.0/BENCHMARK-v1.0.0.md release/dataset-v1.0.0/SHA256SUMS.txt --clobber
  # Update CHANGELOG Dataset v1.0.0 entry — replace the BENCHMARK-v1.0.0.md SHA256 with the new value
  NEW_BENCH_SHA=$(awk '/BENCHMARK-v1\.0\.0\.md/ {print $1}' release/dataset-v1.0.0/SHA256SUMS.txt)
  # Use sed or direct edit to update the SHA256 for BENCHMARK-v1.0.0.md in CHANGELOG.md
  # Note the backfill in the Notes subsection: "BENCHMARK-v1.0.0.md backfilled post-release to reflect final Model v1.0.0 metrics."
  ```

STEP 6 — Commit:
```bash
git add README.md datasets/README.md CHANGELOG.md release/dataset-v1.0.0/BENCHMARK-v1.0.0.md release/dataset-v1.0.0/SHA256SUMS.txt
git commit -m "docs(10-04): finalize README GGUF table + datasets Versions row + reconcile BENCHMARK snapshot"
git push
```
  </action>
  <acceptance_criteria>
    - `grep -c "TBD" README.md` in the GGUF Variants table section returns 0 (no remaining TBD in that table)
    - `grep -cE "\`[a-f0-9]{64}\`" README.md` returns at least 2 (Q4 + Q8 SHA256 inline)
    - `grep -c "lyra-v1.0-q4_k_m.gguf" README.md` returns at least 1
    - `grep -c "TBD" datasets/README.md` in the Dataset Versions table returns 0
    - `grep -cE "v1\.0\.0 \| [0-9]{4}-[0-9]{2}-[0-9]{2}" datasets/README.md` returns 1 (real date in the v1.0.0 row)
    - `head -2 release/dataset-v1.0.0/BENCHMARK-v1.0.0.md | grep -c "Pinned snapshot"` returns 1
    - `python3 -c "import json; d = json.load(open('release/dataset-v1.0.0/lyra-dataset-v1.0.0-stats.json')); assert d['dataset_version'] == '1.0.0'"` exits 0
    - If backfill applied: `awk '/BENCHMARK-v1\.0\.0\.md/ {print $1}' release/dataset-v1.0.0/SHA256SUMS.txt` matches `grep -oE 'BENCHMARK-v1\.0\.0\.md.*\\\`[a-f0-9]{64}\\\`' CHANGELOG.md | head -1 | grep -oE '[a-f0-9]{64}'` (SHA in CHANGELOG matches SHA256SUMS.txt)
  </acceptance_criteria>
  <verify>
    <automated>! grep -A5 "^## GGUF Variants" README.md | grep -q "TBD" && ! grep -A5 "^## Dataset Versions" datasets/README.md | grep -q "TBD"</automated>
  </verify>
  <done>
    README + datasets/README tables finalized with real Plan 02/03 outputs; BENCHMARK snapshot reconciled (or confirmed already-final); CHANGELOG consistent with SHA256SUMS.txt in both release directories.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [CHECKPOINT / UAT]: Manual UAT — LM Studio + llama-cli + dataset download, recorded in 10-UAT.md</name>
  <what-built>
    Plans 01-03 + Plan 04 Task 1 produced: LFS rules, scripts, GGUFs, dataset bundle, two live GitHub Releases (model-v1.0.0 and dataset-v1.0.0), and finalized READMEs. The human now verifies the release-consumer flow end-to-end.
  </what-built>
  <how-to-verify>
Execute each Manual-Only verification from `10-VALIDATION.md`, record the result in a new file
`.planning/phases/10-community-release-enhancements/10-UAT.md` using this exact template:

```markdown
# Phase 10 — UAT Results

**Tested:** <YYYY-MM-DD>
**Environment:** macOS / Linux / Windows — specify
**Tester:** <name>

## Summary

| # | Verification | Result | Evidence |
|---|--------------|--------|----------|
| 1 | Q4_K_M loads in LM Studio + embedded chat template honored | ✅ pass / ❌ fail | <link to screenshot or inline excerpt> |
| 2 | Q8_0 loads via llama-cli smoke test | ✅ pass / ❌ fail | <command output excerpt> |
| 3 | Post-quant perplexity delta f16 vs Q4_K_M < 10% (optional) | ✅ pass / ⬜ skipped | <numbers or "skipped per discretion"> |
| 4 | dataset-v1.0.0 release downloads + SHA256 verifies + JSONL parses | ✅ pass / ❌ fail | <shasum -c output + head train.jsonl excerpt> |

## Evidence

### Verification 1: LM Studio + Q4_K_M

Steps:
1. Downloaded `lyra-v1.0-q4_k_m.gguf` from https://github.com/LakshmanTurlapati/Lyra/releases/tag/model-v1.0.0
2. Imported in LM Studio (My Models → Add → select file).
3. Loaded the model; navigated to Chat UI.
4. Sent this tool-call probe prompt:
   ```
   You have access to a tool `get_weather(location: str)`.
   User: What's the weather in San Francisco?
   Assistant:
   ```
5. Observed output.

Expected: Assistant emits a `<tool_call>{"name": "get_weather", "arguments": {"location": "San Francisco"}}</tool_call>` wrapper (matches SmolLM2 chat template trained on Lyra).
Observed: <paste output>
Template verification: Navigated to My Models → model settings → Prompt Template; compared visible Jinja to `models/lyra-merged/chat_template.jinja` — <match / mismatch>.
Result: ✅ pass / ❌ fail + reason

### Verification 2: llama-cli + Q8_0

Command:
```bash
llama-cli -m lyra-v1.0-q8_0.gguf \
    -p "You have access to a tool get_time(). User: What time is it? Assistant:" \
    -n 128
```
Output excerpt (first 200 chars):
<paste>
Result: ✅ pass / ❌ fail

### Verification 3: Perplexity delta (optional, Claude's discretion)

If run: `llama-perplexity -m lyra-v1.0-f16.gguf -f val_samples.txt` vs Q4_K_M. Report deltas.
Otherwise: "Skipped — post-quant perplexity check is Claude's discretion per CONTEXT.md; Verification 1 + 2 already confirmed chat template + basic inference work."

### Verification 4: dataset-v1.0.0 bundle round-trip

```bash
mkdir -p /tmp/lyra-dataset-v1.0.0
cd /tmp/lyra-dataset-v1.0.0
gh release download dataset-v1.0.0 -R LakshmanTurlapati/Lyra
shasum -a 256 -c SHA256SUMS.txt  # must output all files: OK
head -1 train.jsonl | python3 -m json.tool | head -20  # confirm messages/role/content format
```
Expected: all OK from shasum; head -1 shows a TRL-native record.
Observed: <paste output>
Result: ✅ pass / ❌ fail

## Sign-off

All required verifications passed: <yes / no>.
Release posture: <production-ready / needs-fix>.
```

ALSO update `.planning/phases/10-community-release-enhancements/10-VALIDATION.md`:
1. Replace the placeholder Per-Task Verification Map row with one row per task from all 4 plans. Each row:
   - Task ID (`T-10-01-01` through `T-10-04-02`)
   - Plan (01/02/03/04)
   - Wave (1/2/2/3)
   - Requirement (REL-05 / REL-06 / REL-07)
   - Threat Ref (T-10-XX from the plan's threat_model)
   - Secure Behavior (one-line summary from the task's `<secure_behavior>` block)
   - Test Type (unit / integration / manual)
   - Automated Command (from the task's `<verify><automated>` tag)
   - File Exists status (W0 = in Wave 0; ✓ = produced by plan)
   - Status (✅ green / ⬜ pending if Plan 02/03 halted)
2. Set `nyquist_compliant: true` in frontmatter once every task has an automated verify or a documented Wave 0 dependency.
3. Set `wave_0_complete: true` in frontmatter.
4. Under "Validation Sign-Off", tick every checkbox.

Commit:
```bash
git add .planning/phases/10-community-release-enhancements/10-UAT.md .planning/phases/10-community-release-enhancements/10-VALIDATION.md
git commit -m "docs(10-04): capture UAT results + finalize per-task verification map"
git push
```
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 UAT rows are filled and 10-UAT.md is committed; or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Release asset → developer machine (`gh release download`) | downloaded bytes re-verified via SHA256SUMS.txt |
| LM Studio → developer machine | third-party GUI app that loads GGUF from disk — UAT check catches template-override behavior (Pitfall 6) |
| `llama-cli -m <gguf>` → llama.cpp runtime | GGUF parsed by official llama.cpp reader; same trust surface as Plan 02 T-10-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | downloaded release asset integrity | mitigate | UAT Verification 4 runs `shasum -a 256 -c SHA256SUMS.txt` after `gh release download` — any mismatch is a HARD FAIL for the release. |
| T-10-06 | Tampering | LM Studio chat-template override (Pitfall 6) | mitigate | UAT Verification 1 explicitly checks LM Studio's Prompt Template setting against `chat_template.jinja`; tool-call output must use `<tool_call>` wrappers; any mismatch requires manual template paste (documented in README). |
</threat_model>

<verification>
- README GGUF Variants table has no TBD placeholders; Q4 + Q8 sizes and SHA256 match SHA256SUMS.txt from Plan 02.
- datasets/README Dataset Versions table has no TBD placeholders; v1.0.0 row matches Plan 03 outputs.
- BENCHMARK-v1.0.0.md snapshot is consistent with Model v1.0.0 metrics (verified at Plan 04 time, backfilled if needed).
- 10-UAT.md exists with 4 evidence sections filled in, pass/fail + outputs per Manual-Only row.
- 10-VALIDATION.md Per-Task Verification Map is fully populated; `nyquist_compliant: true`.
- Human checkpoint confirms all required manual verifications passed.
</verification>

<success_criteria>
- REL-05 and REL-07 truths all hold (combining must_haves from Plans 02, 03, 04).
- No TBD strings in README.md GGUF Variants section or datasets/README.md Dataset Versions table.
- 10-UAT.md committed with 4 UAT row results.
- 10-VALIDATION.md fully populated (`nyquist_compliant: true`, all signoff checkboxes ticked).
- Phase 10 is shippable: a user can discover the releases on GitHub, download with verified integrity, run the model locally in LM Studio or llama-cli, and reconstruct the dataset.
</success_criteria>

<output>
After completion, create `.planning/phases/10-community-release-enhancements/10-04-SUMMARY.md` documenting:
- Final README GGUF Variants table values (Q4/Q8 sizes + SHA256)
- Final datasets/README Dataset Versions v1.0.0 row values
- Whether BENCHMARK-v1.0.0.md was backfilled (yes/no + rationale)
- Link to 10-UAT.md
- Link to 10-VALIDATION.md with signoff confirmation
- Phase 10 close-out status (REL-05 ✓, REL-06 ✓ via docs-edit, REL-07 ✓)
</output>
</content>
</invoke>