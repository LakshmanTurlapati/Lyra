---
phase: 05-code-generation-dataset
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - datasets/code/utility-batch-01.jsonl
  - datasets/code/utility-batch-34.jsonl
  - datasets/code/file-ops-batch-01.jsonl
  - datasets/code/file-ops-batch-17.jsonl
  - datasets/code/debugging-batch-01.jsonl
  - datasets/code/debugging-batch-17.jsonl
autonomous: true
requirements: [CODE-01, CODE-02, CODE-03]

must_haves:
  truths:
    - "datasets/code/ contains 34 utility batch files totaling ~1,700 samples"
    - "datasets/code/ contains 17 file-ops batch files totaling ~850 samples"
    - "datasets/code/ contains 17 debugging batch files totaling ~850 samples"
    - "Total raw sample count across all batches is ~3,334 per D-01"
    - "Every batch file passes validate_format.py validation"
    - "Generation order was utility first, then file-ops, then debugging per D-10"
  artifacts:
    - path: "datasets/code/utility-batch-01.jsonl"
      provides: "First utility function batch (50 samples)"
      contains: "messages"
    - path: "datasets/code/utility-batch-34.jsonl"
      provides: "Last utility function batch"
      contains: "messages"
    - path: "datasets/code/file-ops-batch-01.jsonl"
      provides: "First file operations batch (50 samples)"
      contains: "messages"
    - path: "datasets/code/file-ops-batch-17.jsonl"
      provides: "Last file operations batch"
      contains: "messages"
    - path: "datasets/code/debugging-batch-01.jsonl"
      provides: "First debugging batch (50 samples)"
      contains: "messages"
    - path: "datasets/code/debugging-batch-17.jsonl"
      provides: "Last debugging batch"
      contains: "messages"
  key_links:
    - from: "datasets/code/*.jsonl"
      to: "scripts/validate_format.py"
      via: "Conversation.model_validate() inline validation during generation"
      pattern: "valid.*total"
    - from: "datasets/code/*.jsonl"
      to: "scripts/curate_pipeline.py"
      via: "Input for Phase 5 Plan 03 curation run"
      pattern: "jsonl"
---

<objective>
Generate all 68 batches of raw code training data across 3 categories using the generation script from Plan 01. This produces ~3,334 raw samples that will be curated in Plan 03.

Purpose: This is the data production step. The generation script is tested and ready from Plan 01. Now we run it systematically to produce the full volume of raw training data for the code domain per D-01 and D-02 distribution targets.

Output: 68 JSONL batch files in datasets/code/ containing ~3,334 validated samples across utility functions (~1,700), file operations (~850), and debugging (~850).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-generation-dataset/05-CONTEXT.md
@.planning/phases/05-code-generation-dataset/05-RESEARCH.md

@scripts/generate_code_data.py
@scripts/validate_format.py

<!-- Reference: Phase 4 Plan 02 generated batches the same way -->
@.planning/phases/04-tool-calling-dataset/04-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (scripts/generate_code_data.py) -->
CLI interface:
```
python3 -m scripts.generate_code_data --category {utility|file-ops|debugging} --count 50 --batch {N} --seed {S} --output-dir datasets/code/
```

Output format: datasets/code/{category}-batch-{batch:02d}.jsonl
Each line: {"messages": [{"role": "system", ...}, {"role": "user", ...}, {"role": "assistant", ...}]}

Batch counts per D-01 and D-02:
- Utility: 34 batches of 50 = 1,700 samples (last batch: 50 to round up to ~1,700)
- File-ops: 17 batches of 50 = 850 samples
- Debugging: 17 batches of 50 = 850 samples
- Total: 68 batches, ~3,400 raw samples

Validation format: "Validation: N/N valid" printed to stdout per batch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 34 utility function batches (~1,700 samples) per D-10 order</name>
  <files>datasets/code/utility-batch-01.jsonl, datasets/code/utility-batch-34.jsonl</files>
  <read_first>
    scripts/generate_code_data.py
    .planning/phases/05-code-generation-dataset/05-CONTEXT.md
    .planning/phases/04-tool-calling-dataset/04-02-SUMMARY.md
  </read_first>
  <action>
    Generate 34 batches of utility function samples per D-02 (50% of ~3,334 = ~1,667, rounded up to 34 batches of 50 = 1,700).

    Per D-10, utility functions are generated FIRST (simplest category, largest volume).

    Run the generation script in a loop. Use sequential seeds for reproducibility (seed = batch_number * 100 to spread the seed space):

    ```bash
    for i in $(seq 1 34); do
      python3 -m scripts.generate_code_data --category utility --count 50 --batch $i --seed $((i * 100))
    done
    ```

    After all 34 batches complete:
    1. Count total files: `ls datasets/code/utility-batch-*.jsonl | wc -l` -- should be 34
    2. Count total samples: `wc -l datasets/code/utility-batch-*.jsonl | tail -1` -- should be ~1,700
    3. Spot-check format: `head -1 datasets/code/utility-batch-01.jsonl | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['messages'][0]['role'])"` -- should print "system"
    4. Validate a sample batch: `python3 -m scripts.validate_format datasets/code/utility-batch-01.jsonl`

    If any batch fails validation (exit code 1), inspect the error and re-run that specific batch with a different seed.

    Commit all utility batches:
    feat(05-02): generate 34 utility function batches (~1,700 samples)
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/AI/Claude-Mini && ls datasets/code/utility-batch-*.jsonl | wc -l && wc -l datasets/code/utility-batch-*.jsonl | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - ls datasets/code/utility-batch-*.jsonl | wc -l returns 34
    - wc -l datasets/code/utility-batch-*.jsonl | tail -1 shows ~1700 total lines
    - head -1 datasets/code/utility-batch-01.jsonl | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['messages'][0]['role'])" prints "system"
    - python3 -m scripts.validate_format datasets/code/utility-batch-01.jsonl exits 0
    - grep -l "messages" datasets/code/utility-batch-01.jsonl returns match (valid JSONL content)
  </acceptance_criteria>
  <done>34 utility batch files exist in datasets/code/ with ~1,700 total validated samples covering Python, JS, TS, Go, Rust per D-03 language distribution</done>
</task>

<task type="auto">
  <name>Task 2: Generate 17 file-ops and 17 debugging batches (~1,700 samples total)</name>
  <files>datasets/code/file-ops-batch-01.jsonl, datasets/code/file-ops-batch-17.jsonl, datasets/code/debugging-batch-01.jsonl, datasets/code/debugging-batch-17.jsonl</files>
  <read_first>
    scripts/generate_code_data.py
    .planning/phases/05-code-generation-dataset/05-CONTEXT.md
  </read_first>
  <action>
    Generate file operations batches SECOND per D-10, then debugging batches THIRD.

    **File operations: 17 batches of 50 = 850 samples (D-02: 25% of ~3,334)**

    ```bash
    for i in $(seq 1 17); do
      python3 -m scripts.generate_code_data --category file-ops --count 50 --batch $i --seed $((i * 100 + 5000))
    done
    ```

    Verify file-ops:
    1. `ls datasets/code/file-ops-batch-*.jsonl | wc -l` -- should be 17
    2. `wc -l datasets/code/file-ops-batch-*.jsonl | tail -1` -- should be ~850
    3. `python3 -m scripts.validate_format datasets/code/file-ops-batch-01.jsonl` -- should exit 0

    **Debugging: 17 batches of 50 = 850 samples (D-02: 25% of ~3,334)**

    ```bash
    for i in $(seq 1 17); do
      python3 -m scripts.generate_code_data --category debugging --count 50 --batch $i --seed $((i * 100 + 10000))
    done
    ```

    Verify debugging:
    1. `ls datasets/code/debugging-batch-*.jsonl | wc -l` -- should be 17
    2. `wc -l datasets/code/debugging-batch-*.jsonl | tail -1` -- should be ~850
    3. `python3 -m scripts.validate_format datasets/code/debugging-batch-01.jsonl` -- should exit 0
    4. Spot-check Bug/Fix format: `head -1 datasets/code/debugging-batch-01.jsonl | python3 -c "import json,sys; d=json.load(sys.stdin); print('Bug:' in d['messages'][-1]['content'] and 'Fix:' in d['messages'][-1]['content'])"` -- should print True

    **Final summary check across all batches:**
    ```bash
    echo "Utility: $(ls datasets/code/utility-batch-*.jsonl | wc -l) batches, $(wc -l datasets/code/utility-batch-*.jsonl | tail -1 | awk '{print $1}') samples"
    echo "File-ops: $(ls datasets/code/file-ops-batch-*.jsonl | wc -l) batches, $(wc -l datasets/code/file-ops-batch-*.jsonl | tail -1 | awk '{print $1}') samples"
    echo "Debugging: $(ls datasets/code/debugging-batch-*.jsonl | wc -l) batches, $(wc -l datasets/code/debugging-batch-*.jsonl | tail -1 | awk '{print $1}') samples"
    echo "Total: $(ls datasets/code/*.jsonl | wc -l) batches, $(wc -l datasets/code/*.jsonl | tail -1 | awk '{print $1}') samples"
    ```

    Expected: 68 total batches, ~3,400 total samples.

    Commit all file-ops and debugging batches:
    feat(05-02): generate 17 file-ops and 17 debugging batches (~1,700 samples)
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/AI/Claude-Mini && echo "file-ops: $(ls datasets/code/file-ops-batch-*.jsonl | wc -l) batches" && echo "debugging: $(ls datasets/code/debugging-batch-*.jsonl | wc -l) batches" && echo "total: $(ls datasets/code/*.jsonl | wc -l) batches, $(wc -l datasets/code/*.jsonl | tail -1 | awk '{print $1}') samples"</automated>
  </verify>
  <acceptance_criteria>
    - ls datasets/code/file-ops-batch-*.jsonl | wc -l returns 17
    - ls datasets/code/debugging-batch-*.jsonl | wc -l returns 17
    - ls datasets/code/*.jsonl | wc -l returns 68 (total batches)
    - wc -l datasets/code/*.jsonl | tail -1 shows ~3400 total lines
    - python3 -m scripts.validate_format datasets/code/file-ops-batch-01.jsonl exits 0
    - python3 -m scripts.validate_format datasets/code/debugging-batch-01.jsonl exits 0
    - head -1 datasets/code/debugging-batch-01.jsonl contains "Bug:" and "Fix:" in assistant message
  </acceptance_criteria>
  <done>All 68 batch files exist in datasets/code/ with ~3,400 total validated samples: 34 utility (~1,700), 17 file-ops (~850), 17 debugging (~850). Generation order followed D-10: utility first, file-ops second, debugging third.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generation script -> JSONL output | Script writes data files consumed by curation pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | Generated JSONL batch files | accept | Files are generated locally by trusted script, validated inline by Conversation.model_validate(). No external input. |
| T-05-07 | Denial of Service | Disk space from 68 batch files | accept | ~3,400 samples at ~500 bytes each = ~1.7MB total. Trivial disk usage. |
</threat_model>

<verification>
- 68 total JSONL files in datasets/code/
- ~3,400 total samples across all files
- Distribution: ~50% utility, ~25% file-ops, ~25% debugging per D-02
- Every batch passes validate_format.py validation
- Debugging samples contain Bug:/Fix: format per D-06
- No validation errors in generation output
</verification>

<success_criteria>
- 34 utility batches, 17 file-ops batches, 17 debugging batches (68 total)
- ~3,400 total raw samples (~1,700 utility + ~850 file-ops + ~850 debugging)
- All samples pass Pydantic format validation
- Generation completed in order: utility -> file-ops -> debugging per D-10
- All batch files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-generation-dataset/05-02-SUMMARY.md`
</output>
